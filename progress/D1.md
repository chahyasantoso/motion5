# D1: Discover consumer packages

- Status: Implementation in progress. Red-green test and scanner fix are on the branch.
- Branch: `fix/D1-boundary-discovery`
- Parent commit: [`c1aeb6ce`](https://github.com/chahyasantoso/motion5/commit/c1aeb6ce77ba312fc4dbb952204889af56f09d8d), the C3 merge tip.
- Invariant: the boundary scanner discovers every package under `packages/` except `core`; a newly added consumer cannot evade the core-internal import rule by not appearing in a hardcoded list.
- Failing-first evidence: `packages/core/test/unit/scripts/boundary-scan.test.ts`, the planted unlisted `packages/vue/src/index.ts` fixture. On the parent, `scan(tempRoot)` ignores `vue`; on this branch it reports `packages/vue/src/index.ts: core source-internal import`.
- Implementation: `scripts/boundary-scan.mjs` now discovers consumer directories dynamically. The existing core scan and public export checks remain unchanged.
- Public surface: none.
- Exit: focused boundary tests and CI pass; Recovery audit failing-first is red on [`c1aeb6ce`](https://github.com/chahyasantoso/motion5/commit/c1aeb6ce77ba312fc4dbb952204889af56f09d8d) and green on this branch.
- Next action: open the D1 PR against `rescue/restore-motionpath-parity`; after green evidence, merge D1 and continue D2's real scanner self-test.
