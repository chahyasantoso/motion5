# C3: DOM metadata, serialization, and clear coverage

- Status: Implementation in progress. This PR closes missing integration coverage; serializer metadata is not claimed.
- Branch: `fix/C3-dom-contract`
- Parent commit: [`5707bc7b`](https://github.com/chahyasantoso/motion5/commit/5707bc7be98ba5fa3e72f1c6c9f9980510714f36), the merged C2 rescue tip.
- Invariant: the existing DOM adapter contract is covered by an integration test: ready patches resolve targets by node id, write only changed renderable values, remove omitted values with `undefined`, ignore blocked/error/unresolved patches, preserve patch immutability, and support explicit cache teardown through `clear(target)`.
- Evidence: [`packages/core/test/integration/dom-patch-apply.test.ts`](../packages/core/test/integration/dom-patch-apply.test.ts).
- Covered behavior: multi-target resolution, CSS custom properties, underscore and `offset` filtering, unchanged-value suppression, omitted-key removal, blocked/error/unresolved no-op, patch immutability, and clear-cache reset.
- Failing-first disposition: the adapter mechanics predate this PR, so the new integration test is expected to pass on both the rescue parent and this branch. This is regression and acceptance coverage, not red-green evidence. The Recovery audit must use a documented exception: `C3 closes the previously missing DOM integration evidence named by the recovery plan; no source regression is introduced and the new test is intentionally green on the parent.`
- Intentionally not claimed: plugin serializer metadata and plugin-declared internal keys. `PluginDefinition` currently has `internalKeys` but no serializer contract, so C3 does not invent renderer behavior that belongs to the later plugin-contract follow-up.
- CI exit: focused CI must pass; the Recovery audit contract job must report `dom-contract: pass`; failing-first may pass only as the documented exception above. Shared acceptance, mutation, end-to-end, and declaration gates remain separate rescue work.
- Next action: dispatch the Recovery audit against this branch with base `5707bc7b` and the documented exception, then update this log with the run URL before merging.
