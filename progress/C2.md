# C2: React hook and public exports

- Status: In progress
- Branch: `fix/C2-react-public-surface`
- Parent commit: [`a3194280`](https://github.com/chahyasantoso/motion5/commit/a31942809a744ca78ffc11cbcc49e0460a1a058b)
- Depends on: C1, merged as [`25cff099`](https://github.com/chahyasantoso/motion5/commit/25cff099a1b2f9a73e990651f73926f7d8b0c3fa)
- Oracle repository and revision: `chahyasantoso/motionpath`, `1bc8d044347fa3b1732e6dad3bc8437ad23e2687`

## Invariant

The public React package exposes a hook backed by `useSyncExternalStore`. It renders the current immutable patch, updates after publication, and does not expose `@motion5/core/internal`, `PatchRegistry`, or graph internals through its public entry.

## Failing-first evidence

Not recorded yet. The slice must add one behavior test that fails on parent `a3194280` because `packages/react/src/index.ts` and the public hook do not exist, then pass on this branch.

## Owner

The React package owns hook wiring and public exports. C1's `createPatchStore` owns source subscription lifetime. Core continues to own patch identity, freezing, and publication.

## Exit

Add the public hook and index, add the React dependency/type surface required by the workspace, add the public import and render/update tests, then run focused CI followed by the recovery audit against the C2 head with `base: a3194280`.

## Known gaps and next action

The workspace currently has no React package dependency or `src/index.ts`. The implementation must add those without hand-waving around the declaration build or leaking internal core paths. C3 follows only after C2's focused CI and failing-first audit pass.
