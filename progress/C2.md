# C2: React hook and public exports

- Status: Red test recorded
- Branch: `fix/C2-react-public-surface`
- Parent commit: [`a3194280`](https://github.com/chahyasantoso/motion5/commit/a31942809a744ca78ffc11cbcc49e0460a1a058b)
- Current commit: `test(C2): add the first failing public React hook test`
- Depends on: C1, merged as [`25cff099`](https://github.com/chahyasantoso/motion5/commit/25cff099a1b2f9a73e990651f73926f7d8c0b0c3fa)
- Oracle repository and revision: `chahyasantoso/motionpath`, `1bc8d044347fa3b1732e6dad3bc8437ad23e2687`

## Invariant

The public React package exposes a hook backed by `useSyncExternalStore`. It renders the current immutable patch, updates after publication, and does not expose `@motion5/core/internal`, `PatchRegistry`, or graph internals through its public entry.

## Failing-first evidence

Test: [`packages/react/test/public-hook.test.ts`](../packages/react/test/public-hook.test.ts).

The test imports `usePatch` from the public `packages/react/src/index.ts` entry and expects a callable export. On parent `a3194280`, that entry does not exist, so the test fails before the export contract can pass. The implementation branch must make this exact test green without changing the test.

This is the first red test only. The next test will prove render/update behavior through `useSyncExternalStore` once the public entry and React dependency surface exist.

## Owner

The React package owns hook wiring and public exports. C1's `createPatchStore` owns source subscription lifetime. Core continues to own patch identity, freezing, and publication.

## Exit

Add the public hook and index, add the React dependency/type surface required by the workspace, add the render/update test, then run focused CI followed by the recovery audit against the C2 head with `base: a3194280`.

## Known gaps and next action

The archived CI log for run [31496303805](https://github.com/chahyasantoso/motion5/actions/runs/31496303805) showed the first C2 branch failure was only `prettier --check` on `progress/STATUS.md`; that status-table formatting needs correction in the next docs update. The C2 implementation must still add the public hook and React dependency surface, then prove public import and render/update behavior. C3 follows only after C2's focused CI and failing-first audit pass.
