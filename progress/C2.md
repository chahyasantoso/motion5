# C2: React hook and public exports

- Status: Implementation in progress
- Branch: `fix/C2-react-public-surface`
- Parent commit: [`a3194280`](https://github.com/chahyasantoso/motion5/commit/a31942809a744ca78ffc11cbcc49e0460a1a058b)
- Current implementation: render/update proof added, lockfile regeneration pending
- Depends on: C1, merged as [`25cff099`](https://github.com/chahyasantoso/motion5/commit/25cff099a1b2f9a73e990651f73926f7d8b0c3fa)
- Oracle repository and revision: `chahyasantoso/motionpath`, `1bc8d044347fa3b1732e6dad3bc8437ad23e2687`

## Invariant

The public React package exposes `usePatch` backed by `useSyncExternalStore`. It renders the current immutable patch, updates after publication, and does not expose `PatchRegistry` or graph internals through its public entry.

## Failing-first evidence

The original red test is [`packages/react/test/public-hook.test.ts`](../packages/react/test/public-hook.test.ts). On parent `a3194280`, importing `../src/index` failed because the public entry did not exist. The test remains unchanged and now resolves through the public entry.

The render/update proof is [`packages/react/test/public-hook-render.test.ts`](../packages/react/test/public-hook-render.test.ts). It uses real React and `react-test-renderer`, asserts the initial empty snapshot, then asserts updates for two published patches. It cannot run until the lockfile is regenerated with the new React packages.

## Owner

The React package owns hook wiring and public exports. C1's `createPatchStore` owns source subscription lifetime. Core continues to own patch identity, freezing, and publication.

## Exit

Run Bootstrap lockfile manually with `branch: fix/C2-react-public-surface`; then CI should rerun automatically. After green focused CI, rerun Recovery Audit with `ref: fd4a4071ee314354d276937c67034ab56879bae9` and `base: a31942809a744ca78ffc11cbcc49e0460a1a058b`. The final audit head will change after this commit and the lockfile commit, so use the latest PR head SHA.

## Known gaps and next action

No C2 completion claim yet. The hook implementation is now exercised by a real render/update test; the next manual action is lockfile generation.
