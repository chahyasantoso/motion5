# E2: Real end-to-end product path

- Status: **Complete on evidence.** Every gate E2 owns is green; ready to merge.
- Branch: `fix/E2-end-to-end-proof-rescue`
- PR: [#62](https://github.com/chahyasantoso/motion5/pull/62)
- Parent commit: [`fb38313e`](https://github.com/chahyasantoso/motion5/commit/fb38313e76c411dee6ce25b8fee5c3aa307fe068), the merged E1 rescue tip, used as the audit base.
- Audited ref: [`30c7be24`](https://github.com/chahyasantoso/motion5/commit/30c7be24953f44ee31eec5fb732a6dfe4511f537)
- Invariant: an authored project reaches a real numeric DOM value through the Engine, real GSAP interpolation, the existing patch pipeline, and the DOM adapter.
- Acceptance test: `packages/core/test/integration/end-to-end.test.ts`, registered in `docs/acceptance-map.json` as `end-to-end`. D3 deferred that entry until this slice existed.
- Oracle alignment: motionpath's `Track.progress()` updates the interpolation timeline, notifies, and invalidates; the consumer reads the canonical published snapshot. The test mounts, seeds once, seeks, flushes the changed graph state, then reads the published patch from the registry before handing it to the DOM adapter.
- Motion5 boundary: no new clock, no new renderer, no sampler on the interpolator, and `PatchRegistry` stays package-internal. It reuses the manual clock, `Engine`, the GSAP adapter's proxy state, the patch pipeline, and the DOM patch adapter.
- Motion5 files changed: `packages/core/src/adapters/interpolator/gsap.ts`, `packages/core/src/domain/track.ts`.

## Audit

- Recovery audit: [run #31594467503](https://github.com/chahyasantoso/motion5/actions/runs/31594467503), ref `30c7be24`, base `fb38313e`, `failing_first_exception` left empty.
- contract surfaces: **pass**, including the `end-to-end` suite.
- acceptance mapping: **pass**.
- failing-first evidence: **pass**, verdict `pass` rather than `exception`.
- declaration build: **pass**.
- mutation baseline: **fail**, `stryker.config.json` absent. E3 owns this gate; no score was invented.

### Failing-first detail

Base leg red on real behavior, not compilation:

- `gsap-multi-stop.test.ts`: `x` was 37.5 at progress 0.25 where linear authored interpolation requires 25. That is GSAP's implicit `power1.out` bending authored positions.
- `end-to-end.test.ts`: `patch.values.opacity` came back `undefined`, so no authored value reached the published patch at all.

Ref leg green: 2 files, 3 tests, all passing.

### CI

[run #31594035066](https://github.com/chahyasantoso/motion5/actions/runs/31594035066) on the audited ref: quality, integration, boundaries, and performance all pass. This is the first run where quality cleared format check and therefore actually executed typecheck and the full `npm test`; earlier runs skipped both after the format step failed on `progress/STATUS.md`.

## Findings

- Publication ownership: reading a value straight after `seek()` asserted against Track state rather than the canonical publication. The test now flushes and asserts `runtime.graph.registry.get("hero/arm")`, which is what a real consumer observes.
- GSAP internals: the adapter's proxy carries `_gsap` bookkeeping. `Track` strips underscore-prefixed keys before composition ([`51fb91e7`](https://github.com/chahyasantoso/motion5/commit/51fb91e72411788d02047ac64e87fb6702212b4d)) so adapter internals never enter the immutable value contract.
- Easing default: segments now compile with `ease: "none"` unless a stop authors one ([`698e4193`](https://github.com/chahyasantoso/motion5/commit/698e41935de5b748f7c65fb911750615ae922bfb)). The authored schema describes interpolation positions, so an unspecified ease must be linear.
- Eased expectation: GSAP's `power2` is cubic, not quadratic, so `power2.in` at progress 0.5 is 0.125 and `x` is 12.5, not 25 ([`4b04eee1`](https://github.com/chahyasantoso/motion5/commit/4b04eee1d35fc46e3fcb1acce8976fd8fe1507cc)).
- Board drift: this branch was cut before rescue recorded the D3 and E1 merges, so it carried a stale table and then diverged from rescue's corrected one. `progress/STATUS.md` was reconciled to be identical on both branches, which cleared the PR conflict and the format failure at once.

## Deletions

- `e2-gsap-multistop-test-fix.patch` removed from the repository root. The diff it carried is already committed, so keeping it left a second, unexecuted copy of a test expectation.

## Known gaps

- The mutation gate stays red. It belongs to E3, not this slice.

## Next exact action

Merge PR #62 into `rescue/restore-motionpath-parity`, then open E3 for the mutation baseline and ratchet.
