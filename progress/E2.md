# E2: Real end-to-end product path

- Status: Implementation in progress; the first two runs exposed publication timing assumptions, now made explicit in the fixture.
- Branch: `fix/E2-end-to-end-proof-rescue`
- PR: [#62](https://github.com/chahyasantoso/motion5/pull/62)
- Parent commit: [`fb38313e`](https://github.com/chahyasantoso/motion5/commit/fb38313e76c411dee6ce25b8fee5c3aa307fe068), the merged E1 rescue tip.
- Invariant: an authored project reaches a real numeric DOM value through the Engine, real GSAP interpolation, the existing patch, and the DOM adapter.
- Failing-first test: `packages/core/test/integration/end-to-end.test.ts` must fail on the E1 parent because the acceptance path does not exist, then pass here.
- Failure diagnosis: mounting does not itself publish a patch. The fixture now performs an initial flush, seeks to `0.5`, then performs an explicit post-seek flush before asserting the changed patch and DOM write. This keeps the test on the existing manual graph path rather than inventing a second clock or renderer.
- Scope: no new clock or renderer; reuse the existing manual clock, Engine, GSAP adapter, patch pipeline, and DOM adapter.
- Acceptance map: registers `end-to-end` now that the test exists; D3 intentionally deferred this entry until E2.
- Next action: rerun focused CI; do not audit until the E2 test is green.
