# D3: Acceptance evidence gates

- Status: Implementation in progress.
- Branch: `fix/D3-evidence-gates`
- Parent commit: [`24096724`](https://github.com/chahyasantoso/motion5/commit/2409672471cf673594c9970864dc8ff9a93184cd), the merged D1 rescue tip.
- Invariant: every acceptance-map item has a string id and points to an existing test path; missing evidence produces a deterministic failure instead of a documentation-only green.
- Failing-first evidence: `packages/core/test/unit/scripts/acceptance-scan.test.ts` imports the shipped scanner, proves the repository map passes, and proves a temporary map with a missing test reports the exact failure. The parent fails because the scanner and map do not exist; D3 passes.
- Implementation: `docs/acceptance-map.json` records the current contract surfaces; `scripts/acceptance-scan.mjs` validates map shape and test paths and provides a CLI exit code.
- Public surface: none.
- Known limitation: this first gate verifies evidence presence and deterministic mapping, while the Recovery audit remains the authority for whether a test actually ran. It does not invent a fake test-run ledger.
- Next action: open the D3 PR against rescue and run the Recovery audit with base `2409672471cf673594c9970864dc8ff9a93184cd`, no exception.
