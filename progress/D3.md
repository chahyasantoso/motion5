# D3: Acceptance evidence gates

- Status: Implementation in progress on a clean branch from the merged D2 rescue tip.
- Branch: `fix/D3-evidence-gates-rescue`
- Parent commit: [`64178c0d`](https://github.com/chahyasantoso/motion5/commit/64178c0d0c751ca12f277f93c0ac7ed198722d05), the merged D2 rescue tip.
- Invariant: every acceptance-map item has a unique string id and points to an existing test path; missing or duplicate evidence fails deterministically.
- Failing-first evidence: `packages/core/test/unit/scripts/acceptance-scan.test.ts` exercises the shipped scanner, proves the repository map passes, and proves missing and duplicate entries fail. The D2 rescue parent has no acceptance scanner or map, so the new test is red there and green on this branch.
- Implementation: `docs/acceptance-map.json` records only evidence that exists today: GSAP, DOM, React lifecycle, and boundaries. The future end-to-end item is intentionally deferred to E2 until its test exists.
- Known limitation: this gate verifies evidence presence and deterministic mapping; the Recovery audit remains the authority for whether a mapped test actually ran. It does not invent a test-run ledger.
- Next action: run focused CI, then the Recovery audit with base `64178c0d`, no exception.
