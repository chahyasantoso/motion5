# B1: prepare-stage plugin contribution

- Status: Green, pending wave gate
- Branch: `fix/B1-track-contribution`
- Parent commit: [`bec08ded`](https://github.com/chahyasantoso/motion5/commit/bec08ded24da9b5febdca19be81be6edd84cea53)
- Final commit: [`fcafa2d`](https://github.com/chahyasantoso/motion5/commit/fcafa2d41fde3601757a9ff15fa5057ed0bd8534)
- Merged into rescue: [`74f885ca`](https://github.com/chahyasantoso/motion5/commit/74f885ca08f957bea1d552182fcdf33a2b62e70f)
- Oracle repository and revision: `chahyasantoso/motionpath`, `1bc8d044347fa3b1732e6dad3bc8437ad23e2687`
- Oracle files inspected: contribution and compilation paths used to define the prepare boundary
- Motion5 files changed: `packages/core/src/domain/track.ts`, plugin contract, and `packages/core/test/unit/domain/track-contribution.test.ts`

## Invariant

A plugin with `stage: "prepare"` contributes to the interpolation config once during Track construction, before the adapter timeline is created. `Track.compose()` must never rerun preparation.

## Failing-first evidence

Red test commit: [`9f2390f9`](https://github.com/chahyasantoso/motion5/commit/9f2390f9a1ce992f35c938ccf7526ba08e51c085), [test](../packages/core/test/unit/domain/track-contribution.test.ts).

The test expects a prepare plugin returning `{ y: 2 }` to merge into `{ keyframes: { x: 1 } }` before `interpolator.create()` receives the config, and expects `contribute()` to run once after two composes. The parent Track passes the raw config and never calls `contribute()`.

The implementation passed the slice CI run [#31472986155](https://github.com/chahyasantoso/motion5/actions/runs/31472986155), but no recovery-audit failing-first dispatch has yet been recorded. The next audit must use `ref: 74f885ca` and `base: bec08ded`.

## Owner

`Track` owns the one-time preparation boundary. The plugin registry only resolves and orders plugins; the interpolator owns the resulting timeline.

## Exit

Slice CI is green. Recovery exit is pending the failing-first audit result and the Wave B gate. No local command is an exit criterion because GitHub Actions is the only runner.

## Known gaps and next action

No deletions were required. Dispatch the recovery audit against the merged B1 commit with its rescue parent, then start B2 from the latest rescue tip if the base leg fails and the ref leg passes.
