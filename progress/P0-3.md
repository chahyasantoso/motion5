# P0-3: absolute multi-property stop compilation

- Status: In progress, CI red on test-model bug
- Branch: `fix/P0-3-absolute-multi-property-stops`
- Parent commit: [`2f38b5b0`](https://github.com/chahyasantoso/motion5/commit/2f38b5b0f7e6994ba1fe4fb3eb13ab4813acfd3a)
- Latest implementation commit: [`1d838acd`](https://github.com/chahyasantoso/motion5/commit/1d838acd92cde5b76b0a90f9997ac9765084dc05)
- Latest test-model repair: [`92f9f8b2`](https://github.com/chahyasantoso/motion5/commit/92f9f8b2a14f099ca258a2b573d34d4fba64f262)
- PR: [#67](https://github.com/chahyasantoso/motion5/pull/67)
- Red CI: [run #31616782139](https://github.com/chahyasantoso/motion5/actions/runs/31616782139)

## Invariant

Authored stop positions are absolute normalized timeline positions. Each property keeps its own stop grid, easing, leading hold, and trailing interval. One property's stops must not overwrite another property's timing.

## Failing-first evidence

Test: [`packages/core/test/contract/gsap-absolute-stops.test.ts`](../packages/core/test/contract/gsap-absolute-stops.test.ts).

The first test version correctly caught the implementation bug, but its deterministic timeline double incorrectly let a later chained tween overwrite the completed value at an exact boundary. CI exposed that model defect at `p=0.25`: expected `{x:25,y:25}`, received `{x:25,y:20}`. The assertion remains unchanged. Commit [`92f9f8b2`](https://github.com/chahyasantoso/motion5/commit/92f9f8b2a14f099ca258a2b573d34d4fba64f262) fixes only the fake timeline's chained-segment semantics by selecting the active segment per property and using the prior segment's endpoint as its start value.

This is the right kind of red: the test did not get weakened; the test oracle was repaired.

## Implementation

`compileKeyframes()` now emits one segment per authored interval with:

- property key and endpoint value;
- absolute normalized start position;
- independent interval duration;
- authored easing metadata.

The adapter scales both start and duration by the track duration and passes the position to the single paused GSAP timeline. This preserves leading holds and prevents cross-property ordinal timing corruption.

## Oracle and boundary

The reference is motionpath's percent-keyframe behavior and its per-property authored intervals. The implementation stays inside motion5's adapter boundary: proxy state remains adapter-owned, Track still reads `state`, and the project still owns the only clock.

## Current evidence

- CI run [#31616782139](https://github.com/chahyasantoso/motion5/actions/runs/31616782139): integration, boundaries, and performance passed; quality failed only on the deterministic test model at `gsap-absolute-stops.test.ts:81`.
- Formatter run is green. The repaired test model is committed and must rerun quality before the recovery audit.

## Exit

CI green on the final head, then recovery audit with `ref` set to the final head and `base: 2f38b5b0f7e6994ba1fe4fb3eb13ab4813acfd3a`. The audit must show failing-first red on the parent and green on this slice.

## Next action

Wait for the repaired test-model commit to complete CI. Do not merge or mark P0-3 done from the red run.
