# E3: Mutation baseline and ratchet

- Status: Configuration and failing-first proof in progress.
- Branch: `fix/E3-mutation-gate`
- Base: merged rescue tip [`4b4abdd1`](https://github.com/chahyasantoso/motion5/commit/4b4abdd1c3ddcb57bc1ce6c2444a54ce65bf38de).
- Invariant: the recovery audit runs Stryker over runtime and adapters and emits a durable JSON mutation report.
- Red-green test: `packages/core/test/unit/scripts/mutation-config.test.ts` must fail on the base because `stryker.config.json` is absent, then pass on this branch.
- Scope: `packages/core/src/runtime/**/*.ts` and `packages/core/src/adapters/**/*.ts`; no invented score or threshold.

## Manual dependency step

The branch adds the two Stryker packages to `package.json`. Run the manual **Bootstrap lockfile** workflow for branch `fix/E3-mutation-gate` so npm generates the matching `package-lock.json` and verifies `npm ci`.

After bootstrap, run focused CI, then the Recovery audit with base `4b4abdd1` and no failing-first exception. The audit should make the config test green and produce `reports/mutation/mutation.json`; record the measured baseline before setting any nonzero ratchet threshold.
