# A3: guard subscriber-triggered reentrancy

Slice branch: `fix/A3-publisher-reentrancy` (from the `rescue/restore-motionpath-parity` tip
[`1230dc81`](https://github.com/chahyasantoso/motion5/commit/1230dc819d0e17337bd0820935f9a832de0f8d5b)).

Status in [`STATUS.md`](STATUS.md) is authoritative. This file is evidence only.

## Invariant

A flush requested while patch subscribers are being notified never runs inside that flush: it
is coalesced into exactly one follow-up invalidation that the next flush drains.

## The defect

`PatchRegistry.closeBatch()` clears `#batchOpen` before notifying listeners so that listener
failures cannot leave the batch open (A2). That correct fix left the notification window
unguarded: a subscriber could call back into `GraphRuntime.invalidate()`, open a second batch,
and publish from inside the first notification. Consequences observed by the failing-first
test on the parent commit:

- the nested batch reached batch listeners **before** the batch that triggered it;
- `#lastTick` advanced inside another flush, so the next real clock tick failed the strictly
  increasing check with `RangeError`;
- nothing named the behavior, so "the follow-up ran" and "the follow-up was lost" were
  indistinguishable.

## Chosen policy

One policy, three owners, no second clock:

1. **`PatchRegistry`** owns the notification window. `notifying` is true only while a settled
   batch is being delivered, and `beginBatch()` refuses to open during it
   (`REENTRANT_BATCH_MESSAGE`). The window closes in a `finally`, so the registry is reusable
   even when a listener throws (A2 stays intact).
2. **`GraphPublisher`** fails fast at the same boundary before touching batch state. Traversal
   is not a scheduler and holds no queueing policy.
3. **`GraphRuntime`** owns the policy. While flushing, a further `flush()`/`invalidate()` adds
   its seeds to one pending set and returns an empty batch carrying a
   `reentrant-flush-deferred` warning diagnostic. The next flush (normally the next clock
   tick) unions the pending seeds into its own seeds and clears them, so the follow-up runs
   once. If that flush throws, the carried seeds are restored.

`ProjectRuntime.seek()` inherits this for free: it delegates to `GraphRuntime.invalidate()`.

## Motionpath oracle

motionpath's scheduler drives progress from one ticker and treats a change raised during
notification as work for the next frame rather than a nested traversal. That timing behavior
is what is ported. Not copied: motionpath's `GraphPublisher.js` traversal, its Track-owned
graph walk, and any additional RAF/ticker ownership.

## Motion5 boundary

Unchanged owners: `GraphBinding` transactions, publisher traversal/failure containment, the
single clock subscription in `GraphRuntime`, and the A1 final-value memo. No new public
export: `notifying` and `pendingSeeds` are runtime-internal reads.

## Deletions

None. No placeholder or duplicate owner existed for this behavior; the gap was a missing
guard.

## Failing-first evidence

- Red commit: [`9d16a0a1`](https://github.com/chahyasantoso/motion5/commit/9d16a0a139189b8d77db3a399bbd707313f1921b)
  adds [`publisher-reentrancy.test.ts`](../packages/core/test/unit/runtime/publisher-reentrancy.test.ts)
  with no implementation change.
- Implementation commit: [`bc240bab`](https://github.com/chahyasantoso/motion5/commit/bc240bab509973b46e19d75197b44487153ee625).

Assertions that must fail on the red commit's parent
[`1230dc81`](https://github.com/chahyasantoso/motion5/commit/1230dc819d0e17337bd0820935f9a832de0f8d5b):

| Assertion                                         | Parent behavior                                        |
| ------------------------------------------------- | ------------------------------------------------------ |
| batch listener order is `[1]` after one tick      | `[2, 1]`: nested batch delivered first                 |
| `runtime.tick === 1` after one tick               | `2`: reentrant call advanced the tick                  |
| deferred batch carries `reentrant-flush-deferred` | no such batch; a real publication is returned          |
| `runtime.pendingSeeds` queues the follow-up       | member does not exist                                  |
| second `clock.tick()` still delivers tick `2`     | `RangeError`: clock tick no longer strictly increasing |
| `beginBatch()` inside a subscriber throws         | opens a nested batch silently                          |
| `publisher.flush()` inside a subscriber throws    | nests a full publication                               |

## Exit

There is no local checkout for this recovery, so **CI is the evidence source**, not a
developer machine. The slice runs on [PR #45](https://github.com/chahyasantoso/motion5/pull/45)
via the `CI` workflow, whose triggers were widened to `rescue/**` and `fix/**` in the same PR
because slice PRs into rescue previously ran no checks at all.

Commands the workflow runs, which are also the local exit commands when a checkout exists:

```bash
npm run check # format:check, typecheck, npm test
npm run test:integration
npm run test:boundaries
```

### Green run

[Run 31471235572](https://github.com/chahyasantoso/motion5/actions/runs/31471235572) on
[`e81e9a3b`](https://github.com/chahyasantoso/motion5/commit/e81e9a3b03a6541198c4b35eb836d16a0ea78b72),
the head of this slice branch: `quality` (all three steps), `integration`, and `boundaries`
all green, `performance` advisory green. This is the first run in which `npm test` actually
executed the reentrancy test, so it is the authoritative green for A3 and also re-proves A1
and A2 on top of the rescue tip.

### Red run

[Run 31471934722](https://github.com/chahyasantoso/motion5/actions/runs/31471934722) on
[`fix/A3-red-evidence`](https://github.com/chahyasantoso/motion5/tree/fix/A3-red-evidence),
via [PR #46](https://github.com/chahyasantoso/motion5/pull/46): `quality` **failed** while
`integration`, `boundaries`, and `performance` stayed green. That branch is the rescue tip
plus the test file plus the CI trigger change, and nothing under `packages/*/src`, so the
failure is caused by the missing guard and nothing else.

Two obstacles had to be cleared to get that run at all. Both will recur on later slices:

1. **The evidence branch inherited the rescue tip's CI triggers**, which only fire on `main`.
   A test-only branch therefore produced zero checks and looked indistinguishable from "not
   run yet". Cut evidence branches with the widened triggers, or dispatch the workflow
   manually.
2. **A strictly typed failing-first test cannot compile against the code it indicts.** The
   first attempt died in `typecheck` with 7 errors for `runtime.pendingSeeds` and
   `registry.notifying`, so `npm test` never ran and the evidence proved only that the API was
   absent. The evidence copy on that branch now reads both members structurally, so the suite
   executes and fails on behavior. The shipped copy on this branch stays strictly typed; the
   two files differ only in those two accessors.

## Known gaps

- Step-level log text is not pasted here, only run and job links, because the runs are read
  through the GitHub API rather than a terminal. The run pages remain the artifact.
- Failing-first is still proven by a hand-cut branch rather than a gate. Slice D3 automates
  the parent comparison; until it exists, every slice needs this manual red branch, and
  `recovery-audit.yml` can be dispatched for a red/green artifact pair.
- `PatchRegistry.publish()` is still not guarded against being called during notification.
  Publishing outside an open batch is a separate pre-existing looseness; it is not part of A3
  and needs its own failing-first slice before any behavior changes.
