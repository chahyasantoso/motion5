# B2: real GSAP multi-stop compilation

- Status: In progress
- Branch: `fix/B2-gsap-stop-compilation-clean`
- Parent commit: [`3c311415`](https://github.com/chahyasantoso/motion5/commit/3c3114154567dda92abe4f48b4da2fcc66f7be06)
- Oracle repository and revision: `chahyasantoso/motionpath`, `1bc8d044347fa3b1732e6dad3bc8437ad23e2687`

## Invariant

Authored multi-stop properties compile into an adapter-owned GSAP timeline. The proxy starts at the first stop and reaches the authored values at progress `0`, `0.5`, and `1`; the adapter never initializes from the final stop or passes only that stop to GSAP.

## Acceptance test

`packages/core/test/contract/gsap-multi-stop.test.ts` injects a real GSAP timeline and asserts authored values `0`, `50`, and `100` at progress `0`, `0.5`, and `1`.

## Current handoff

This branch intentionally does not carry a hand-built lockfile. Run the **Prepare GSAP lockfile** workflow with `branch: fix/B2-gsap-stop-compilation-clean` and `gsap_version: 3.15.0`. That workflow adds `gsap` to `package.json`, generates a valid `package-lock.json`, and commits both files. Then run CI on this branch.

Recovery audit comes after CI: use this branch's head SHA as `ref` and rescue commit `3c3114154567dda92abe4f48b4da2fcc66f7be06` as `base`.
