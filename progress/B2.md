# B2: real GSAP multi-stop compilation

- Status: In progress
- Branch: `fix/B2-gsap-stop-compilation`
- Parent commit: rescue tip before the B2 branch work
- Current implementation: [`bbc9eb3`](https://github.com/chahyasantoso/motion5/commit/bbc9eb3ec729356965529811921a478af51904bd)
- Latest test commit: [`7cbfc04`](https://github.com/chahyasantoso/motion5/commit/7cbfc04ccb93d80665587e44114a285a33932e1d)
- Oracle repository and revision: `chahyasantoso/motionpath`, `1bc8d044347fa3b1732e6dad3bc8437ad23e2687`
- Motion5 files changed: `packages/core/src/adapters/interpolator/gsap.ts`, `packages/core/test/contract/adapters.test.ts`, `package.json`, and lockfile

## Invariant

Authored multi-stop properties compile into an adapter-owned GSAP timeline. The proxy starts at the first stop and reaches the authored values at progress `0`, `0.5`, and `1`; the adapter never initializes from the final stop or passes only that stop to GSAP.

## Acceptance test

`packages/core/test/contract/adapters.test.ts` now injects the real `gsap.timeline()` and asserts the proxy value for authored stops `0 -> 50 -> 100` at progress `0`, `0.5`, and `1`. The test also verifies the engine is hidden behind the interpolator port.

## Failing-first evidence

The B2 test was developed failing-first on the branch. Recovery audit evidence is still required against the B2 head with the rescue parent. The audit must show the new contract test failing on rescue and passing on this branch.

## CI and next action

Run the branch CI checks from GitHub Actions. Then dispatch Recovery audit from the rescue workflow with `ref` set to the B2 head SHA and `base` set to the latest rescue tip. Record both run URLs here before merging B2 into rescue.

No local command is an exit criterion because GitHub Actions is the only runner.
