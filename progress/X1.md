# X-1 flat projected input observations

- Status: In progress, implementation complete, rerun pending after CI fixes
- Branch: `fix/X1-flat-projected-inputs`
- Parent commit: `59f5ba3` (`rescue/restore-motionpath-parity`)
- Final commit: pending CI rerun
- Oracle repository and revision: `motionpath`, `v5-pr-15-observation-graph` (`c629e9b`)
- Oracle files inspected: `GraphPublisher.js`, `Track.js`
- Motion5 files changed: `packages/core/src/contract/v5.ts`, `packages/core/src/graph/ir.ts`, `packages/core/src/runtime/graph-publisher.ts`, X1 graph and publisher tests
- Invariant: input observations project upstream authored keys into the observer source object as flat keys before local composition
- Acceptance test IDs and paths: `flat projected input observations (X-1)` in `packages/core/test/unit/runtime/publisher-flat-inputs.test.ts`; projection validation in `packages/core/test/unit/graph/observation-projection-validation.test.ts`
- CI workflow run (slice checks): red run [#31663378125](https://github.com/chahyasantoso/motion5/actions/runs/31663378125) on test-only commit; prior behavioral green was recorded in PR discussion, but the final head exposed a typecheck and boundary-scan regression
- Recovery audit dispatch (ref, base, run URL): not dispatched for the final head
- Failing-first verdict from that run: red leg was real: the test-only flat-input contract failed against the rescue parent
- Artifact links: [PR #70](https://github.com/chahyasantoso/motion5/pull/70)
- Known gaps: final head still needs a clean CI run; non-record inputs are diagnosed during publication rather than at load time
- Next exact action: rerun CI after the projection type narrowing and boundary vocabulary fixes, then update this log and `progress/STATUS.md` with the final green run
