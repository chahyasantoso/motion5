# E1: Required declaration build

- Status: Implementation in progress.
- Branch: `fix/E1-required-build-v2`
- Parent commit: [`83eb44d2`](https://github.com/chahyasantoso/motion5/commit/83eb44d2da7e118e284d96c890f82cd90c84f960), the merged D3 rescue tip.
- Invariant: every package declares a public type entry and an exports map, while the Recovery audit emits declarations before accepting the build gate.
- Changes: add explicit `types` and conditional `exports` entries to `@motion5/core` and `@motion5/react`; keep the existing source entrypoints so current workspace consumers remain valid.
- Evidence target: Recovery audit `declaration build` must pass and report declaration emission plus present package type and exports entries.
- Known limitation: generated declaration files remain audit artifacts, not committed build output; packaging/bundling is deferred.
- Next action: open the E1 PR, run focused CI, then run the Recovery audit with base `83eb44d2`, no exception.
