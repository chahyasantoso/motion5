diff --git a/docs/ADR-050-bare-authored-leaf.md b/docs/ADR-050-bare-authored-leaf.md
index a0e7bf5..c440838 100644
--- a/docs/ADR-050-bare-authored-leaf.md
+++ b/docs/ADR-050-bare-authored-leaf.md
@@ -86,3 +86,3 @@ Not an empty stop list. That is a field accepted and then ignored, which rule 6
 
-Not one function moves layers, and the layering ADR-049 established is unchanged. Leaf shape is `contract/authored-leaf`. Authored legality is `contract/validate-v5`. Section names, group detection, and the legacy predicate stay in `contract/keyframe-shape`. Compilation, and therefore the static bypass, is `domain/keyframe-compiler`. Which plugin owns a leaf stays in `domain/plugins`. Topology stays in `graph/ir.ts`, whose diff is **empty** again.
+Not one function moves layers, and the layering ADR-049 established is unchanged. Leaf shape is `contract/authored-leaf`. Authored legality is `contract/validate-v5`. Section names, group detection, and the legacy predicate stay in `contract/keyframe-shape`. Compilation, and therefore the static bypass, is `domain/keyframe-compiler`. Which plugin owns a leaf stays in `domain/plugins`. Topology stays in `graph/ir.ts`, whose diff is **empty** again. **Refined by [ADR-105](./ADR-105-adapters-do-not-depend-on-domain.md), 2026-09-23.** Compilation, and with it the static bypass, now lives in `contract/keyframe-compiler.ts` beside the leaf-shape owner, and `domain/keyframe-compiler.ts` no longer exists. The reasoning above is unchanged; only the module moved.
 
diff --git a/docs/ADR-060-animated-live-values.md b/docs/ADR-060-animated-live-values.md
index 0eae8d1..d604e0b 100644
--- a/docs/ADR-060-animated-live-values.md
+++ b/docs/ADR-060-animated-live-values.md
@@ -126,3 +126,5 @@ spellings of one operation.
   `project-runtime`, or the plugins is touched. `domain/keyframe-compiler.ts` and
-  `contract/authored-leaf.ts` gain no import, and `domain/track.ts` gains no GSAP type.
+  `contract/authored-leaf.ts` gain no import, and `domain/track.ts` gains no GSAP type. **Refined by
+  [ADR-105](./ADR-105-adapters-do-not-depend-on-domain.md), 2026-09-23:** the compiler now lives in
+  `contract/keyframe-compiler.ts`, and the domain path no longer exists.
 
diff --git a/docs/ADR-097-a-rule-id-is-a-closed-union.md b/docs/ADR-097-a-rule-id-is-a-closed-union.md
index 78d6536..27d847c 100644
--- a/docs/ADR-097-a-rule-id-is-a-closed-union.md
+++ b/docs/ADR-097-a-rule-id-is-a-closed-union.md
@@ -12,3 +12,3 @@ The inventory this record is built on was taken at `1cf86fbb3f9c5fb8b8c9619e8dae
 
-Many more ids are assigned to `Diagnostic.ruleId` than the handful the issue sketches, and they are constructed across more modules than the twelve the first inventory named. No total is stated, here or anywhere else in this record. Three inventories of this union disagreed with each other, in both directions, so a number written down here would pin the error rather than catch it, and the coverage case in `packages/core/test/contract/rule-id.test.ts` is the authority instead; it asserts coverage and never a total, for the same reason. The modules named below are where a reader looks, not the proof. Fourteen construct a diagnostic directly: `packages/core/src/contract/validate-v5.ts`, `contract/migrate-v4-to-v5.ts`, `graph/ir.ts`, `graph/order.ts`, `graph/references.ts`, `domain/plugins.ts`, `domain/keyframe-compiler.ts`, `domain/track.ts`, `adapters/graph-builder/incremental.ts`, `adapters/trigger-factory/default.ts`, `runtime/graph-publisher.ts`, `runtime/graph-runtime.ts`, `runtime/project-runtime.ts`, and `runtime/report.ts`. The fifteenth, `runtime/publisher-outcome.ts`, mints ids on the error classes `graph-publisher.ts` forwards. `contract/diagnostics.ts` was named in that list and belongs out of it: it declares the constructor and no id. Seven base ids carry severity `warning` and the rest carry `error`; the prefixed spellings of `stop-missing-start` and `stop-missing-end` inherit it, so nine members of the union are warnings. Six and eight were the answer when the inventory was taken, and the seventh base warning is `reentrant-flush-deferred-frame`, minted by the follow-up below rather than missed by a count.
+Many more ids are assigned to `Diagnostic.ruleId` than the handful the issue sketches, and they are constructed across more modules than the twelve the first inventory named. No total is stated, here or anywhere else in this record. Three inventories of this union disagreed with each other, in both directions, so a number written down here would pin the error rather than catch it, and the coverage case in `packages/core/test/contract/rule-id.test.ts` is the authority instead; it asserts coverage and never a total, for the same reason. The modules named below are where a reader looks, not the proof. Fourteen construct a diagnostic directly: `packages/core/src/contract/validate-v5.ts`, `contract/migrate-v4-to-v5.ts`, `graph/ir.ts`, `graph/order.ts`, `graph/references.ts`, `domain/plugins.ts`, `domain/keyframe-compiler.ts` (now `contract/keyframe-compiler.ts`, moved by [ADR-105](./ADR-105-adapters-do-not-depend-on-domain.md)), `domain/track.ts`, `adapters/graph-builder/incremental.ts`, `adapters/trigger-factory/default.ts`, `runtime/graph-publisher.ts`, `runtime/graph-runtime.ts`, `runtime/project-runtime.ts`, and `runtime/report.ts`. The fifteenth, `runtime/publisher-outcome.ts`, mints ids on the error classes `graph-publisher.ts` forwards. `contract/diagnostics.ts` was named in that list and belongs out of it: it declares the constructor and no id. Seven base ids carry severity `warning` and the rest carry `error`; the prefixed spellings of `stop-missing-start` and `stop-missing-end` inherit it, so nine members of the union are warnings. Six and eight were the answer when the inventory was taken, and the seventh base warning is `reentrant-flush-deferred-frame`, minted by the follow-up below rather than missed by a count.
 
diff --git a/packages/core/src/domain/plugins.md b/packages/core/src/domain/plugins.md
index 0063aa1..68d4e49 100644
--- a/packages/core/src/domain/plugins.md
+++ b/packages/core/src/domain/plugins.md
@@ -4,4 +4,5 @@
 `ResolvedPlugins` extends it here so the registry remains the owner of the resolved serializer map,
-while adapters consume the port without reaching into this domain module. The exported type name is
-unchanged. See ADR-105.
+while adapters consume the port without reaching into this domain module. This module no longer
+exports `RenderMetadata`; the adapter barrel and the internal entry re-export it from the port under
+the same name, so there is one declaration and one import path. See ADR-105.
 
diff --git a/packages/core/test/contract/ids-ownership-at-the-producer.test.ts b/packages/core/test/contract/ids-ownership-at-the-producer.test.ts
index 81b02ec..745a4c8 100644
--- a/packages/core/test/contract/ids-ownership-at-the-producer.test.ts
+++ b/packages/core/test/contract/ids-ownership-at-the-producer.test.ts
@@ -19,4 +19,5 @@ import { code } from "../helpers/source-region";
  * Two rather than five, and the three that went were not weakened away. `graph/ir.ts`'s `diag` and
- * the private `diagnostic` in `domain/plugins.ts` and `domain/keyframe-compiler.ts` each declared the
- * constructor's whole signature and then forwarded to it unchanged, so what this suite read in those
+ * the private `diagnostic` in `domain/plugins.ts` and in the keyframe compiler (then under
+ * `domain/`, now `contract/keyframe-compiler.ts`) each declared the constructor's whole signature
+ * and then forwarded to it unchanged, so what this suite read in those
  * three files was a restatement rather than a second mechanism. `OwnedIds<Rule>` is referenced from
