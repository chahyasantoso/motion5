{
"message": "docs(status): the local signal seams are gone",
"target": 260,
"edits": [
{"path": "docs/SESSION-STATUS.md", "find": "`06388bd` on `main`, which is [#293](https://github.com/chahyasantoso/motion5/pull/293) merged, plus the ordering correction for issue #294 on top of it.", "replace": "`5d67749` on `main`, which is [#295](https://github.com/chahyasantoso/motion5/pull/295) merged, plus this slice's test-file cleanup on top of it."},
{"path": "docs/SESSION-STATUS.md", "find": "- **Just landed.** The alternation's ordering has one owner and a true reason,", "replace": "- **Just landed.** The signal seams reference the shipped members, [issue #260](https://github.com/chahyasantoso/motion5/issues/260). `immediate-verb-refusal.test.ts` declared `SignalSeam` and `SignalMotionOption` locally, under a docblock promising the commit that landed the source would delete them, and both `ProjectRuntime.signal` and `ProjectRuntimeOptions.signalMotion` have been real members since ADR-064's amendment. The aliases were not merely dead indirection: `signalling()` cast to a hand-written `signal` shape and the rig cast its options through a hand-written `signalMotion` shape, so `typecheck` checked both against the alias rather than against the declared member, and a signature change on either could not have turned this file red. Both aliases and both casts are deleted and the seam assertion stays, on `editing()`'s rule that failing-first means a failing assertion rather than a call that could not run. No behaviour and no new case: `RA-83` owns `signal` in both directions and is unchanged, and what the slice buys is measured by `quality` instead.\n- **Landed before it.** The alternation's ordering has one owner and a true reason,"},
{"path": "docs/SESSION-STATUS.md", "find": "\n- **Landed before it.** The evidence-id gate refuses a series its pattern cannot see, [issue #289](https://github.com/chahyasantoso/motion5/issues/289). Two decisions rather than two widenings. The scan root is the tree `vitest` walks rather than one package's test directory, so `H-4` in `packages/react/test/public-hook-render.test.ts` is in the collision map at all; and an id at the head of a title declares a case unless it belongs to a space this project spends elsewhere, which is a plan slice id carrying a digit in its prefix or an architecture invariant `I-n`. That second half is measured rather than anticipated: the first red run refused eight `I-` ids across five files, three of them declared twice, and reading them is what turned a false refusal into the rule. It earns no ADR: the gate's own header already owns what each series covers.\n\n## Next in line", "replace": "\n\n## Next in line"}
]
}
