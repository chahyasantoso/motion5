{
  "message": "fix(runtime): report a disposed runtime before a stale handle on every write",
  "target": 299,
  "edits": [
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #liveId(motionId: string, token: number): string {\n    return this.#liveMotion(motionId, token).definition.id;\n  }",
      "replace": "  #liveId(motionId: string, token: number): string {\n    return this.#liveMotion(motionId, token).definition.id;\n  }\n\n  #writableEntry(id: string, token: number): TrackEntry {\n    this.#assertLive();\n    return this.#liveEntry(id, token);\n  }\n\n  #writableMotion(id: string, token: number): MotionEntry {\n    this.#assertLive();\n    return this.#liveMotion(id, token);\n  }\n\n  #writableId(motionId: string, token: number): string {\n    return this.#writableMotion(motionId, token).definition.id;\n  }"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #removeTrack(id: string, token: number): void {\n    this.#assertLive();\n    this.#liveEntry(id, token);",
      "replace": "  #removeTrack(id: string, token: number): void {\n    this.#writableEntry(id, token);"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #replaceTrack(id: string, token: number, next: TrackDefinition): void {\n    const entry = this.#liveEntry(id, token);",
      "replace": "  #replaceTrack(id: string, token: number, next: TrackDefinition): void {\n    const entry = this.#writableEntry(id, token);"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    const entry = this.#liveEntry(id, token);\n    const observations = [...(entry.track.observes ?? [])];",
      "replace": "    const entry = this.#writableEntry(id, token);\n    const observations = [...(entry.track.observes ?? [])];"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    const entry = this.#liveEntry(id, token);\n    const { keyframes, bound } = this.#boundGroup(id, entry, plugin);",
      "replace": "    const entry = this.#writableEntry(id, token);\n    const { keyframes, bound } = this.#boundGroup(id, entry, plugin);"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    const entry = this.#liveEntry(id, token);\n    const keyframes = entry.track.keyframes ?? EMPTY_KEYFRAMES;",
      "replace": "    const entry = this.#writableEntry(id, token);\n    const keyframes = entry.track.keyframes ?? EMPTY_KEYFRAMES;"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    const entry = this.#liveEntry(nodeId, token);\n    this.#refuseInsideRecipe(\"setKeyframe\");",
      "replace": "    const entry = this.#writableEntry(nodeId, token);\n    this.#refuseInsideRecipe(\"setKeyframe\");"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    const entry = this.#liveEntry(nodeId, token);\n    this.#refuseInsideRecipe(\"removeKeyframe\");",
      "replace": "    const entry = this.#writableEntry(nodeId, token);\n    this.#refuseInsideRecipe(\"removeKeyframe\");"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "      overrideValues: (next: AuthoredValues) =>\n        runtime.#writeValues(id, runtime.#liveEntry(id, token), next, false),\n      setValues: (next: AuthoredValues) =>\n        runtime.#writeValues(id, runtime.#liveEntry(id, token), next, true),",
      "replace": "      overrideValues: (next: AuthoredValues) =>\n        runtime.#writeValues(id, runtime.#writableEntry(id, token), next, false),\n      setValues: (next: AuthoredValues) =>\n        runtime.#writeValues(id, runtime.#writableEntry(id, token), next, true),"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#refuseInsideRecipe(\"setTrigger\");\n    const entry = this.#liveMotion(id, token);",
      "replace": "    this.#refuseInsideRecipe(\"setTrigger\");\n    const entry = this.#writableMotion(id, token);"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#refuseInsideRecipe(\"setStagger\");\n    const entry = this.#liveMotion(id, token);",
      "replace": "    this.#refuseInsideRecipe(\"setStagger\");\n    const entry = this.#writableMotion(id, token);"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "      addTrack: (track: TrackDefinition) =>\n        runtime.#addTrack(track, runtime.#schemaOwner, { motionId: runtime.#liveId(id, token) }),",
      "replace": "      addTrack: (track: TrackDefinition) =>\n        runtime.#addTrack(track, runtime.#schemaOwner, {\n          motionId: runtime.#writableId(id, token),\n        }),"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "      destroy: () => runtime.#removeMotion(runtime.#liveId(id, token)),",
      "replace": "      destroy: () => runtime.#removeMotion(runtime.#writableId(id, token)),"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "The resolver every handle member and every private mutation path goes through, so the contract is uniform by construction rather than by four call sites agreeing. See ADR-056.",
      "replace": "The resolver every reading member of a track handle goes through, so the contract is uniform by construction rather than by four call sites agreeing. It was every private mutation path's resolver too, until a disposed runtime was found reported as a stale handle on seventeen of the eighteen members that write. That order is `#writableEntry`'s below: this member still owns the staleness and no longer owns which of the two questions is answered first. See ADR-056 and its amendment of 2026-09-04."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "This handle's motion id, once the handle is known to be live. Reading the id and refusing a stale handle are the same call, so there is no order for a member to get wrong.",
      "replace": "This handle's motion id, once the handle is known to be live. Reading the id and refusing a stale handle are the same call, so there is no order for a member to get wrong.\n\n## #writableEntry\n\nThe resolver every writing member of a track handle goes through, and the one owner of the order between two answers to one condition.\n\nA disposed runtime and a stale handle can both be true of one call, and `dispose` empties the retained maps, so the token lookup misses and staleness answers first unless something asks about the runtime before the lookup. Which of the two the caller is told is ADR-056's, and that record decided the runtime's lifecycle outranks one handle's. This is where the ordering lives, so it is asked once rather than at each writing member, and the deletion that proves it is `#removeTrack`'s own guard. See ADR-056's amendment of 2026-09-04 and `SH-8`.\n\nBeside `#liveEntry` rather than inside it, because a read is answered the other way round and deliberately: `definition` and `requires` still refuse under `StaleHandleError`, which is the family that record asked a caller to catch, and asking liveness in the shared resolver would push a bare `Error` past every one of those narrowings. So the ladder now has three rungs with three readerships: the probe `live` reads, the resolver a read goes through, and this.\n\n## #writableMotion\n\nThe same question about the other map, and the same reason.\n\nTwo members rather than one, for the reason `#liveEntry` and `#liveMotion` are already two: two maps and two refusals. `#assertLive` is therefore named twice in this file, which is the floor rather than a copy left to remove.\n\n## #writableId\n\nThis handle's motion id, once the runtime is known to be live and the handle known to be current. The writing twin of `#liveId`, and it delegates to `#writableMotion` rather than asking `#assertLive` again, so the rung above stays the only place the order is stated."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "Staleness first, through the resolver every member of the handle reads. Then the unbound-group refusal, answered from the retained record on this node.",
      "replace": "The runtime's liveness first and this handle's staleness with it, through the resolver every writing member goes through. Then the unbound-group refusal, answered from the retained record on this node."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "Staleness first, through the resolver every member of the handle reads. Then the property-entry refusal,",
      "replace": "The runtime's liveness first and this handle's staleness with it, through the resolver every writing member goes through. Then the property-entry refusal,"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "The order is the whole contract, and ADR-061's amendment owns why: the recipe refusal, then staleness, then `validateMotionTrigger`, which is the owner `addMotion` already asks, then the redundant edit, then the seam, whose failure is reported verbatim, and the retained definition last, once nothing that can refuse is left. See ADR-035 and ADR-061.",
      "replace": "The order is the whole contract, and ADR-061's amendment owns why: the recipe refusal, then the runtime's liveness and this handle's staleness together, then `validateMotionTrigger`, which is the owner `addMotion` already asks, then the redundant edit, then the seam, whose failure is reported verbatim, and the retained definition last, once nothing that can refuse is left. Liveness joined that sequence at the staleness step rather than as a step of its own, and no case can place it anywhere else: `dispose` clears `#open`, so the recipe refusal and the disposal never both have something to say. See ADR-035, ADR-056's amendment of 2026-09-04 and ADR-061."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "Drops one node from the pair, and names no hook. The eviction, the dispose and the Motion deregistration are one settle step `#derive` owns, derived from the id being absent from the committed pair. See ADR-064.",
      "replace": "Drops one node from the pair, and names no hook. The eviction, the dispose and the Motion deregistration are one settle step `#derive` owns, derived from the id being absent from the committed pair. See ADR-064.\n\nIts own `#assertLive` is deleted rather than kept beside `#writableEntry`'s. This was the one writing member that already reported a disposal instead of a staleness, which is why ADR-056's Consequences could name it as where that rule shows, and one owner of the rule is the whole of what its amendment buys. See ADR-056's amendment of 2026-09-04."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "One call site of its own now, `#commit`, so this member has what `replaceGraph`, `rejectAfterRollback` and every hook already had. It never starts against a runtime its caller disposed, and the re-ask that guarantees that belongs to `edit` rather than here: a guard here could only ever fire for the one caller that can reach it while disposed, placed where all six pass, and it could not answer, which is the whole point.",
      "replace": "One call site of its own now, `#commit`, so this member has what `replaceGraph`, `rejectAfterRollback` and every hook already had. It never starts against a runtime its caller disposed, and the re-ask that guarantees that belongs to `edit` rather than here. A guard here could fire for no caller at all: `addMotion` and `#addTrack` ask `#assertLive` themselves, `#removeTrack`, `#removeMotion` and `#replaceTrack` are reached through resolvers that ask it before they resolve, and `edit` re-asks after its recipe returned. It could not answer either, which is the whole point. The one caller that could reach this member while disposed arrived through a handle that answered staleness, and that is `#writableEntry`'s and `#writableMotion`'s now rather than a reason to guard here."
    },
    {
      "path": "docs/ADR-056-uniform-stale-track-handle.md",
      "find": "`SH-6` passed as a guard by design.",
      "replace": "`SH-6` passed as a guard by design.\n\n## Amendment, 2026-09-04: the lifecycle outranks the handle on every member that writes, not on the one that asked\n\nThe Consequences above state that rule and name one member as where it shows: `remove()` reports the disposal rather than the staleness, because `#assertLive()` runs first. It ran first there because `#removeTrack` asked it, and asking it was every caller's job. Seventeen of the eighteen writing members this runtime issues across `TrackHandle` and `MotionHandle` never asked, and `dispose()` empties the retained maps, so the token lookup misses and the resolver answers `StaleTrackHandleError` or `StaleMotionHandleError` instead. One condition with two public failure contracts, decided here and implemented once. Issue #298.\n\n**The invariant.** Every handle member that writes reports `ProjectRuntime is disposed.`, and reports it before it asks whether the handle is stale.\n\n**A read is unchanged, and the asymmetry is the decision rather than the amendment's scope.** Staleness on a write is misleading in a specific way: it says the thing the caller holds is out of date, which invites it to resolve a fresh handle and try again, and on a disposed runtime nothing it can do will work. Staleness on a read is true, because the entry the handle captured is gone, and it is the family this record exists to let a caller catch: changing `definition`, `requires` or `trackIds` would push a bare `Error` past every narrowing on `StaleHandleError` this record asked for. `live` still never throws, and `if (handle.live) handle.write()` is correct on either side of that line.\n\n**The order has one owner, and it is a rung on the resolver ladder rather than a line per caller.** `#writableEntry` and `#writableMotion` ask `#assertLive` and then delegate to `#liveEntry` and `#liveMotion`, and `#writableId` delegates to `#writableMotion`. Every writing path resolves through one of the three, and every reading member keeps the resolver it already had, so the probe, the reader and the writer are three rungs with three readerships rather than three copies of one comparison. `#assertLive` is named twice in the class, which is the floor: two retained maps and two refusals, the same reason `#liveEntry` and `#liveMotion` were already two members. The count `SH-7` pins is untouched, because none of the three compares a token and none adds a branch to the handle factory.\n\n**Refused: asking `#assertLive` inside `#liveEntry` and `#liveMotion`.** It is the cheapest change available, one owner and two lines, and it takes the read contract with it silently, for the reason above. `#entryIfLive`, `#motionIfLive` and `#liveOf` are refused harder: `live` reads them, and `live` may never throw.\n\n**Refused: a line at the top of each affected path.** Seventeen copies of one ordering, and the eighteenth path added later is wrong by default. The defect being fixed is one condition with two failure contracts, so a fix whose correctness depends on every future author remembering a line is that same defect with a longer fuse. `#removeTrack`'s own `#assertLive()` is deleted for the same reason rather than left beside the resolver's, which is why `remove()` comes out of the slice behaviourally identical and with one owner fewer.\n\n**Nothing else in the class moves, and that is measured rather than hoped.** `dispose()` clears `#open`, so a disposed runtime never has an open recipe and `#assertLive` and `#refuseInsideRecipe` can never both have something to say. Every per-verb order the class documents therefore survives, including ADR-061's amendment's contract for tier 0: liveness joins that sequence at the staleness step rather than as a step of its own. The one sequence in which both are lexically reachable is a `dispose()` inside a recipe followed by a handle write in the same recipe, and it answers the disposal, which is the answer ADR-064's amendment of the same date reached for `edit` itself.\n\n**Evidence.** `SH-8` in `stale-track-handle.test.ts` and `RA-111` in `handle-base.test.ts`, one per handle family because there are two refusals and one file owns each surface. Each adds a writers-and-readers partition of the member record its own file already owns, `MEMBER_ARGUMENTS` and `MOTION_MEMBER_ARGUMENTS`, and asserts that the partition covers that record's keys exactly, so a member added to a handle later has to be classified rather than escaping this invariant by omission. Both collect per member rather than asserting one at a time, so a red run names every member that reports the wrong condition: `SH-8` was red on thirteen of fourteen and `RA-111` on all four. `remove` was green in `SH-8` on both sides, which is this record's own sentence being already true of exactly one member. The accepting direction is `SH-1` and `RA-32`, unchanged and green throughout, and it is not restated here: a handle made stale on a live runtime still refuses under `StaleHandleError` on every member.\n\n**Consequences.** Seventeen members change which error they throw on a disposed runtime and none changes its signature, so no export, type or schema moves. A caller catching `StaleHandleError` around a write in order to swallow a disposal stops swallowing it, which is the behaviour this record already called correct for `remove()`. ADR-064's amendment of 2026-09-04 is corrected where it argued that two of `#apply`'s callers arrive through handle members answering staleness: they arrive through these resolvers now, and its conclusion holds for six callers rather than five. [LIVE-EDIT-COST.md](./LIVE-EDIT-COST.md) gains the caller-facing half, and [GUARDRAILS.md](./GUARDRAILS.md) the general rule, that a precondition outranking a lookup belongs to the resolver both readers share rather than to each caller of it."
    },
    {
      "path": "docs/ADR-064-schema-transaction-derived-hooks.md",
      "find": "Its five other callers cannot reach it while disposed. `addMotion`, `#addTrack` and `#removeTrack` ask `#assertLive` themselves. `#removeMotion` and `#replaceTrack` ask nothing of the kind: they are reached from `MotionHandle.destroy`, `TrackHandle.replace`, `#writeKeyframes` and `#replaceWithObservation`, which resolve through `#liveMotion` and `#liveEntry` and answer staleness instead, and staleness answers only because `dispose` empties the retained maps and the token lookup misses. That is worth stating rather than rounding to liveness, because it is the same fact that turns this defect from a wrong message into a compile for every track in the project. Either way a guard in the shared write path could only ever fire for the one caller that can reach it, placed where all six pass, and a guard like that hides a real defect on the day it fires for a different reason.",
      "replace": "Its five other callers cannot reach it while disposed. `addMotion` and `#addTrack` ask `#assertLive` themselves, and `#removeTrack`, `#removeMotion` and `#replaceTrack` are reached through resolvers that ask it before they resolve, `#writableEntry` and `#writableMotion`. On the day this amendment was written the last three asked nothing of the kind: they were reached from `MotionHandle.destroy`, `TrackHandle.replace`, `#writeKeyframes` and `#replaceWithObservation`, which resolved through `#liveMotion` and `#liveEntry` and answered staleness, and staleness answered only because `dispose` empties the retained maps and the token lookup misses. Stating that rather than rounding it to liveness is what made it citable, because it is the same fact that turns this defect from a wrong message into a compile for every track in the project, and because it was a second defect: one condition with two public failure contracts, which is issue #298 and ADR-056's amendment of 2026-09-04. Either way a guard in the shared write path could only ever have fired for the one caller that could reach it, placed where all six pass, and a guard like that hides a real defect on the day it fires for a different reason. It can now fire for none of them."
    },
    {
      "path": "docs/LIVE-EDIT-COST.md",
      "find": "- Every capability handle has one shape and one failure family. `Handle` carries `id`, `live` and `definition`; `live` never throws, on either side of any invalidation and on a disposed project, and every other member refuses with a named error under one abstract `StaleHandleError`. A caller that does not care which kind of handle went stale catches the base; one that does branches on `ruleId`.",
      "replace": "- Every capability handle has one shape, and one failure family per question. `Handle` carries `id`, `live` and `definition`; `live` never throws, on either side of any invalidation and on a disposed project. Every member that reads refuses with a named error under one abstract `StaleHandleError`: a caller that does not care which kind of handle went stale catches the base, and one that does branches on `ruleId`. Every member that writes reports a disposed project as `ProjectRuntime is disposed.` instead, before it asks whether the handle is stale, because the project's own lifecycle outranks one handle's and re-resolving a handle against a project that is gone cannot help. `if (handle.live) handle.write()` is correct on either side of that line. See ADR-056 and its amendment of 2026-09-04."
    },
    {
      "path": "docs/GUARDRAILS.md",
      "find": "The rule about a narrowed surface handed to a callback is this same defect read from the other end: that one is about what a callback can reach, and this one is about what the caller may no longer assume once the callback has returned. [Issue #288](https://github.com/chahyasantoso/motion5/issues/288).",
      "replace": "The rule about a narrowed surface handed to a callback is this same defect read from the other end: that one is about what a callback can reach, and this one is about what the caller may no longer assume once the callback has returned. [Issue #288](https://github.com/chahyasantoso/motion5/issues/288).\n- A precondition that outranks a lookup belongs to the resolver both readers share, not to each caller of it. ADR-056 decided that a disposed runtime outranks a stale handle and named `TrackHandle.remove` as where that shows, and it showed there because `#removeTrack` asked `#assertLive` itself. Asking it was every caller's job, so seventeen of the eighteen handle members that write never did, and because `dispose` empties the retained maps the token lookup missed and each of them reported the disposal as staleness: one condition with two public failure contracts, and a caller invited to re-resolve a handle against a project that no longer exists. The fix is one rung on the resolver ladder rather than seventeen lines, and the deletion that proves the rule has one owner is the eighteenth caller's own guard. Where the two answers must not be reordered together, the rung is a third member rather than a change to the shared resolver: a read is answered the other way round on purpose, and asking liveness in the resolver both share would have pushed a bare `Error` past every narrowing that record asked callers to write. The general form is the one this project keeps arriving at from different directions: where two answers to one condition exist, the order between them is a thing that needs an owner, and an order enforced by convention at n call sites is enforced at the first of them only. [Issue #298](https://github.com/chahyasantoso/motion5/issues/298)."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Verified on:** `0e268ad` on `main`, which is [#296](https://github.com/chahyasantoso/motion5/pull/296) merged, plus this slice on top of it. It is the first slice since `ebad1ab`, [#282](https://github.com/chahyasantoso/motion5/pull/282), to change runtime behaviour at all: everything between them was this file's own shape, two evidence gates, and prose, and none of it reached `packages/core/src`.",
      "replace": "- **Verified on:** `bbbb314` on `main`, which is [#297](https://github.com/chahyasantoso/motion5/pull/297) merged, plus this slice on top of it. It is the second slice in a row to change runtime behaviour, after a stretch from `ebad1ab`, [#282](https://github.com/chahyasantoso/motion5/pull/282), through `0e268ad` in which nothing reached `packages/core/src` at all: that stretch was this file's own shape, two evidence gates, and prose."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Just landed.** `edit(recipe)` answers a runtime the recipe disposed, [issue #288](https://github.com/chahyasantoso/motion5/issues/288).",
      "replace": "- **Just landed.** A handle write reports the disposal rather than the staleness, [issue #298](https://github.com/chahyasantoso/motion5/issues/298). ADR-056 decided that a disposed runtime outranks a stale handle and named `TrackHandle.remove` as where that shows, and it showed there because `#removeTrack` asked `#assertLive` itself. Asking it was every caller's job, so seventeen of the eighteen handle members that write never did, and because `dispose` empties the retained maps the token lookup missed and each of them reported the disposal as `StaleTrackHandleError` or `StaleMotionHandleError`: one condition with two public failure contracts, and a caller invited to re-resolve a handle against a project that no longer exists. Three members now own that order, `#writableEntry`, `#writableMotion` and `#writableId`, one rung above the resolvers they delegate to, so every writing path asks liveness before the lookup while every reading member keeps the stale-handle family ADR-056 asked callers to catch. `#removeTrack`'s own guard is deleted rather than kept beside them. `SH-8` and `RA-111` are the red cases, each a writers-and-readers partition of the member record its own file already owns, and [GUARDRAILS.md](./GUARDRAILS.md) gains the rule that a precondition outranking a lookup belongs to the resolver both readers share.\n- **Landed before it.** `edit(recipe)` answers a runtime the recipe disposed, [issue #288](https://github.com/chahyasantoso/motion5/issues/288)."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "\n- **Landed before it.** The signal seams reference the shipped members, [issue #260](https://github.com/chahyasantoso/motion5/issues/260). `immediate-verb-refusal.test.ts` declared `SignalSeam` and `SignalMotionOption` locally, under a docblock promising the commit that landed the source would delete them, and both `ProjectRuntime.signal` and `ProjectRuntimeOptions.signalMotion` have been real members since ADR-064's amendment. The aliases were not merely dead indirection: `signalling()` cast to a hand-written `signal` shape and the rig cast its options through a hand-written `signalMotion` shape, so `typecheck` checked both against the alias rather than against the declared member, and a signature change on either could not have turned this file red. Both aliases and both casts are deleted and the seam assertion stays, on `editing()`'s rule that failing-first means a failing assertion rather than a call that could not run. No behaviour and no new case: `RA-83` owns `signal` in both directions and is unchanged, and what the slice buys is measured by `quality` instead.",
      "replace": ""
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Five in-place verbs reached through a handle never ask `#assertLive`.** `#setTrigger`, `#setStagger`, `#writeValues`, `#setKeyframe` and `#removeKeyframe` resolve through `#liveMotion` or `#liveEntry` and ask liveness nowhere, so on a disposed runtime they answer `StaleMotionHandleError` or `StaleTrackHandleError` rather than `ProjectRuntime is disposed.`, and they answer at all only because `dispose` empties the retained maps and the token lookup misses. One condition with two failure contracts, and a disposal reported as staleness. Found by #288's slice and deliberately not in it: the same root cause, a different invariant and a different refusal set. Captured 2026-09-04, verified on `0e268ad` on `main`.",
      "replace": "- **A read through a handle still reports a disposed runtime as a stale handle.** `definition`, `requires`, `trackIds`, `track` and `tryTrack` answer `StaleTrackHandleError` or `StaleMotionHandleError` on a disposed runtime, and #298's slice pinned that in `SH-8` and `RA-111` rather than leaving it undecided: ADR-056's amendment argues the read is telling the truth, because the entry the handle captured is gone, and that widening it would push a bare `Error` past every narrowing that record asked callers to write. What is genuinely open is which side `MotionHandle.track` and `tryTrack` belong on, because they resolve a handle rather than write through one, and no case draws that line today. Captured 2026-09-04, verified on `bbbb314` on `main` plus #298's slice."
    }
  ]
}
