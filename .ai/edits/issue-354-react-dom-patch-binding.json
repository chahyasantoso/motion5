{
  "version": 1,
  "operation": "apply",
  "expected_head": "cff33a0bfd29331f7a37149654eef6a5e0eb0872",
  "expected_blobs": {
    "packages/core/src/adapters/dom.ts": "c6c5712a7bff9e4e207944d207283e1f01343b24",
    "packages/react/src/index.ts": "d4d0fc05a1b3c3382bc27999182eceeda3e45c49",
    "packages/react/src/use-patch.ts": null,
    "packages/react/src/use-dom-patch.ts": null,
    "packages/react/test/dom-patch-hook.test.ts": null,
    "apps/ik-playground/src/components/IkStage.tsx": "9c325b2f75350c0df19122bf3a82a4614ff04063",
    "docs/guide/rendering-patches.md": "f01d1ce08fc191fd3817046eda20bb1a7839056d",
    "docs/guide/api-reference.md": "a933826245633a4f1b9d29082004a782b325b823",
    "docs/SESSION-STATUS.md": "6a023e9ac5929eb66597b87e67d46a7cac34aa34"
  },
  "message": "feat(react): bind patches directly to DOM targets",
  "target": 354,
  "edits": [
    {
      "path": "packages/core/src/adapters/dom.ts",
      "find": "export interface StageLike {\n  style: { perspective?: string; [key: string]: unknown };\n  [key: string]: unknown;\n}\nexport interface DomTarget {\n  style: { removeProperty?: (property: string) => void; [key: string]: unknown };\n  [key: string]: unknown;\n}",
      "replace": "export interface DomStyle {\n  perspective?: string;\n  transform?: string;\n  removeProperty?: (property: string) => unknown;\n}\nexport interface StageLike {\n  style: DomStyle;\n}\nexport interface DomTarget {\n  style: DomStyle;\n}\nfunction styleValues(target: StageLike | DomTarget): Record<string, unknown> {\n  return target.style as Record<string, unknown>;\n}\nfunction targetValues(target: DomTarget): Record<string, unknown> {\n  return target as unknown as Record<string, unknown>;\n}"
    },
    {
      "path": "packages/core/src/adapters/dom.ts",
      "find": "  else target.style[key] = undefined;",
      "replace": "  else styleValues(target)[key] = undefined;"
    },
    {
      "path": "packages/core/src/adapters/dom.ts",
      "find": "    else if (key in target.style || key.startsWith(\"--\")) target.style[key] = value;\n    else target[key] = value;",
      "replace": "    else if (key in target.style || key.startsWith(\"--\")) styleValues(target)[key] = value;\n    else targetValues(target)[key] = value;"
    },
    {
      "path": "packages/core/src/adapters/dom.ts",
      "find": "  if (Object.keys(state).length > 0) target.style.transform = composeTransform(state);",
      "replace": "  if (Object.keys(state).length > 0) styleValues(target).transform = composeTransform(state);"
    },
    {
      "path": "packages/core/src/adapters/dom.ts",
      "find": "    stage.style.perspective = `${perspective}px`;",
      "replace": "    styleValues(stage).perspective = `${perspective}px`;"
    },
    {
      "path": "packages/react/src/index.ts",
      "find": "import { useMemo, useSyncExternalStore } from \"react\";\nimport type { Patch, PatchSource } from \"@motion5/core/internal\";\nimport { createPatchStore } from \"./patch-store\";\n\n// A consumer must be able to type the hook's argument and result from this entry alone.\n// Re-exporting the observation contract keeps @motion5/core/internal a private channel\n// between the two packages instead of a required import for anyone using the hook.\nexport type { Patch, PatchListener, PatchSource } from \"@motion5/core/internal\";\n\n/** Subscribe to one published node from a React component. */\nexport function usePatch(source: PatchSource, nodeId: string): Patch | undefined {\n  const store = useMemo(() => createPatchStore(source, nodeId), [source, nodeId]);\n  return useSyncExternalStore(store.subscribe, store.getSnapshot, store.getSnapshot);\n}",
      "replace": "// A consumer must be able to type the hooks' arguments and results from this entry alone.\n// Re-exporting the observation contract keeps @motion5/core/internal a private channel\n// between the two packages instead of a required import for anyone using a hook.\nexport type { Patch, PatchListener, PatchSource } from \"@motion5/core/internal\";\nexport { usePatch } from \"./use-patch\";\nexport { useDomPatch } from \"./use-dom-patch\";"
    },
    {
      "path": "packages/react/src/use-patch.ts",
      "create": "import { useMemo, useSyncExternalStore } from \"react\";\nimport type { Patch, PatchSource } from \"@motion5/core/internal\";\nimport { createPatchStore } from \"./patch-store\";\n\n/** Subscribe to one published node from a React component. */\nexport function usePatch(source: PatchSource, nodeId: string): Patch | undefined {\n  const store = useMemo(() => createPatchStore(source, nodeId), [source, nodeId]);\n  return useSyncExternalStore(store.subscribe, store.getSnapshot, store.getSnapshot);\n}\n"
    },
    {
      "path": "packages/react/src/use-dom-patch.ts",
      "create": "import { useCallback, useLayoutEffect, useMemo, useRef, type RefCallback } from \"react\";\nimport { createDomPatchAdapter, type DomTarget } from \"@motion5/core/adapters\";\nimport type { PatchSource } from \"@motion5/core/internal\";\n\n/** Bind one published node directly to a DOM or SVG target without rendering through React. */\nexport function useDomPatch<T extends DomTarget>(\n  source: PatchSource,\n  nodeId: string,\n): RefCallback<T> {\n  const target = useRef<T | null>(null);\n  const adapter = useMemo(\n    () => createDomPatchAdapter({ style: {} }, undefined, () => target.current ?? undefined),\n    [],\n  );\n\n  useLayoutEffect(() => {\n    const apply = (patch: NonNullable<ReturnType<PatchSource[\"get\"]>>): void => {\n      adapter.apply(patch);\n    };\n    const unsubscribe = source.subscribeNode(nodeId, apply);\n    const current = source.get(nodeId);\n    if (current) apply(current);\n    return unsubscribe;\n  }, [adapter, nodeId, source]);\n\n  return useCallback(\n    (next) => {\n      const previous = target.current;\n      if (previous && previous !== next) adapter.clear(previous);\n      target.current = next;\n      const current = next ? source.get(nodeId) : undefined;\n      if (current) adapter.apply(current);\n    },\n    [adapter, nodeId, source],\n  );\n}\n"
    },
    {
      "path": "packages/react/test/dom-patch-hook.test.ts",
      "create": "import { createElement } from \"react\";\nimport { act, create } from \"react-test-renderer\";\nimport { describe, expect, it } from \"vitest\";\nimport { useDomPatch } from \"@motion5/react\";\nimport type { Patch, PatchListener, PatchSource } from \"@motion5/react\";\n\nconst NODE_ID = \"hero/arm\";\n\ninterface FakeSource extends PatchSource {\n  listenerCount(): number;\n  publish(patch: Patch): void;\n}\n\nfunction createFakeSource(initial?: Patch): FakeSource {\n  const listeners = new Set<PatchListener>();\n  let latest = initial;\n  return {\n    get: () => latest,\n    subscribeNode(nodeId, listener) {\n      if (nodeId !== NODE_ID) throw new Error(`Unexpected node ${nodeId}`);\n      listeners.add(listener);\n      return () => listeners.delete(listener);\n    },\n    listenerCount: () => listeners.size,\n    publish(patch) {\n      latest = patch;\n      for (const listener of [...listeners]) listener(patch);\n    },\n  };\n}\n\nfunction patch(\n  revision: number,\n  values: Readonly<Record<string, unknown>>,\n  status: Patch[\"status\"] = \"ready\",\n): Patch {\n  return {\n    nodeId: NODE_ID,\n    revision,\n    values,\n    sourceProgress: 0,\n    sourceRevisions: {},\n    status,\n    diagnostics: [],\n  };\n}\n\ndescribe(\"useDomPatch\", () => {\n  it(\"binds retained and future complete patches through the public package entry\", () => {\n    const source = createFakeSource(patch(1, { x: 4, y: 8, scale: 2, opacity: 0.5 }));\n    const style: Record<string, unknown> & { removeProperty(property: string): void } = {\n      opacity: undefined,\n      removeProperty(property) {\n        delete this[property];\n      },\n    };\n    const target = { style };\n\n    function Consumer() {\n      return createElement(\"div\", { ref: useDomPatch(source, NODE_ID) });\n    }\n\n    let renderer: { unmount(): void } | undefined;\n    act(() => {\n      renderer = create(createElement(Consumer), { createNodeMock: () => target });\n    });\n    expect(style).toMatchObject({\n      opacity: 0.5,\n      transform: \"translate3d(4px, 8px, 0px) scale(2)\",\n    });\n\n    act(() => source.publish(patch(2, { x: 99, opacity: 1 }, \"blocked\")));\n    expect(style).toMatchObject({\n      opacity: 0.5,\n      transform: \"translate3d(4px, 8px, 0px) scale(2)\",\n    });\n\n    act(() => source.publish(patch(3, { x: 12, y: 16 })));\n    expect(style.opacity).toBeUndefined();\n    expect(style.transform).toBe(\"translate3d(12px, 16px, 0px)\");\n    expect(source.listenerCount()).toBe(1);\n\n    act(() => renderer?.unmount());\n    expect(source.listenerCount()).toBe(0);\n  });\n});\n"
    },
    {
      "path": "apps/ik-playground/src/components/IkStage.tsx",
      "find": "import { usePatch, type Patch } from \"@motion5/react\";",
      "replace": "import { useDomPatch, usePatch, type Patch } from \"@motion5/react\";"
    },
    {
      "path": "apps/ik-playground/src/components/IkStage.tsx",
      "find": "  const patch = useLivePatch(handle, id);\n  if (!patch) return null;\n\n  return (\n    <g transform={`translate(${Number(patch.values.x ?? 0)}, ${Number(patch.values.y ?? 0)})`}>",
      "replace": "  const target = useDomPatch<SVGGElement>(handle, id);\n\n  return (\n    <g ref={target}>"
    },
    {
      "path": "docs/guide/rendering-patches.md",
      "find": "## React\n\n```tsx\nimport { usePatch } from \"@motion5/react\";\n\nfunction Title({ handle }: { handle: ProjectHandle }) {\n  const patch = usePatch(handle, \"hero/title\");\n  if (patch?.status !== \"ready\") return null;\n  return <div style={{ transform: `translateX(${String(patch.values.x)}px)` }} />;\n}\n```\n\n`usePatch` takes any `PatchSource`, which is exactly `{ get, subscribeNode }`, so a project handle satisfies it. It is built on `useSyncExternalStore`, so it is tear-free under concurrent rendering and safe in strict mode. One hook subscribes to one node; render a component per animated node rather than subscribing to the project and re-rendering the tree.",
      "replace": "## React\n\n```tsx\nimport { useDomPatch } from \"@motion5/react\";\n\nfunction Title({ handle }: { handle: ProjectHandle }) {\n  const target = useDomPatch<HTMLDivElement>(handle, \"hero/title\");\n  return <div ref={target} />;\n}\n```\n\n`useDomPatch` binds one node to one DOM or SVG target and routes complete patches through the DOM adapter. It subscribes before reading the retained patch, writes future batch notifications directly without a React render, ignores non-ready patches through the adapter contract, and clears target state on replacement or unmount. A new renderable plugin output therefore reaches the target without a component learning its key.\n\nUse `usePatch` when React must derive markup, combine nodes, or display diagnostics. It takes any `PatchSource`, which is exactly `{ get, subscribeNode }`, and is built on `useSyncExternalStore`, so it is tear-free under concurrent rendering and safe in strict mode. A bone joining two nodes is derived geometry, not a one-patch-to-one-target binding; keeping that projection explicit avoids teaching a generic renderer application semantics."
    },
    {
      "path": "docs/guide/api-reference.md",
      "find": "`usePatch(source, nodeId)` and the re-exported `Patch`, `PatchListener`, and `PatchSource` types.",
      "replace": "`usePatch(source, nodeId)`, `useDomPatch(source, nodeId)`, and the re-exported `Patch`, `PatchListener`, and `PatchSource` types. `useDomPatch` returns a callback ref for a one-patch-to-one-DOM-or-SVG-target binding; derived markup and multi-node projections stay on `usePatch`."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "## Next in line\n\n- **Unmerged formatter parity:**",
      "replace": "## Next in line\n\n- **Unmerged React DOM binding:** issue [#354](https://github.com/chahyasantoso/motion5/issues/354) has an implementation branch adding a direct one-node-to-one-target React binding over the existing DOM adapter. The PR and exact-head CI remain gates; no landed public surface is claimed.\n- **Unmerged formatter parity:**"
    }
  ]
}
