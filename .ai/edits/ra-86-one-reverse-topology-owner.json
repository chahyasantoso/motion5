{
  "message": "fix(runtime): read reverse topology from its one owner in dependantsOf",
  "target": 261,
  "edits": [
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  dependantsOf(nodeId: string): readonly string[] {",
      "replace": "  /**\n   * Every node that reads this one, for an editor's preflight rather than for enforcement.\n   *\n   * Read from `GraphIR.dependents`, which is the one derivation of reverse topology, and it names\n   * both kinds of reader: the observer of an edge, and a solver that reads this node as a chain\n   * member. Enforcement stays where ADR-050 put it, in the candidate graph, which refuses a removal\n   * that would orphan a live dependant; this answers what a caller is told before it tries, and a\n   * preflight that is wrong in the permissive direction is worse than none, because it is read.\n   */\n  dependantsOf(nodeId: string): readonly string[] {"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    return Object.freeze(\n      this.#graph.graph.nodes\n        .filter((node) => node.edges.some((edge) => edge.sourceId === nodeId))\n        .map((node) => node.id),\n    );",
      "replace": "    // Read, never rederived. `finalizeGraph` freezes `dependents` onto the `GraphIR` once per\n    // graph, from the walk that already had the solver fan-in in hand, so the filter this replaces\n    // was both a duplicate and the weaker of the two derivations: it saw `edges` alone, and a solver\n    // reads its members through `solves` with no edge pointing that way, so every member of every\n    // chain was missing from the one query an editor asks before it deletes a node.\n    //\n    // Deduplicated here rather than in the derivation. `deriveDependents` keeps a reader once per\n    // dependency deliberately, because a consumer walking it guards on its own visited set; this\n    // answers a set of readers, so the collapse belongs to the consumer that wants a set. First\n    // occurrence wins, which is the committed node order the filter produced. See `RA-86`, `RA-87`.\n    const readers: readonly string[] | undefined = this.#graph.graph.dependents[nodeId];\n    return Object.freeze(readers === undefined ? [] : [...new Set(readers)]);"
    },
    {
      "path": "docs/guide/api-reference.md",
      "find": "- `dependantsOf(nodeId)` lists observers for editor preflight.",
      "replace": "- `dependantsOf(nodeId)` lists every reader of a node for editor preflight: the observer of an edge, and a solver that reads it as a chain member."
    },
    {
      "path": "docs/guide/runtime-changes.md",
      "find": "`handle.dependantsOf(nodeId)` is a read-only query for editor preflight: it tells you who observes a node before you try to delete it. It is not the enforcement.",
      "replace": "`handle.dependantsOf(nodeId)` is a read-only query for editor preflight: it tells you who reads a node before you try to delete it, which is every observer of an edge plus any solver that reads it as a chain member. It is not the enforcement."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- Every earlier slice's red and green run ids live in the pull request that landed it.",
      "replace": "- One owner of reverse topology on the public query: `RA-86` through `RA-88` in `packages/core/test/unit/runtime/dependants-of-reverse-topology.test.ts`. Red run [33699901385](https://github.com/chahyasantoso/motion5/actions/runs/33699901385), archived at `logs/33699901385/` on `ci-logs`. `typecheck` and `format:check` passed first, then 2 failed and 863 passed, both on assertions, with the other five checks green. `RA-88` is green on purpose, because it is the equivalence half and reading the frozen map may not move an answer the edge walk already got right. The run before it is not evidence in either direction and is cited in the pull request body: `RA-88` failed there on a wrong premise of mine about the disposed read, which refuses with the plain disposed-runtime error rather than a `TypeError`, so the case moved and the source did not. The green run and its head sha live in the pull request body.\n- Every earlier slice's red and green run ids live in the pull request that landed it."
    }
  ]
}
