{"version":1,"operation":"apply","expected_head":"6f85aa0fbaa0a5cd29aacdedc7636838e620c280","expected_blobs":{"packages/core/src/contract/value-transaction.ts":null,"packages/core/src/runtime/value-batch.ts":null,"docs/ADR-078-value-tier-batch.md":null,"packages/core/src/runtime/schema-refusals.ts":"ee3c12527f53e88e03e37431eb31dfba9c298dbc","packages/core/src/runtime/project-runtime.ts":"d2a8210a51889663ab2c52b7221d129060d8573f","packages/core/src/runtime/project-runtime.md":"3e4aa2d5c09af5ee467ea9d33d027dfcda54f504","packages/core/src/engine.ts":"4b348a914ff77f1e712f3dd5762c14ec87ca3ea5","packages/core/src/index.ts":"216c2724b1e73c5ad6d534cbc9442138efffa277","packages/core/test/unit/runtime/value-batch.test.ts":"9a71c268148f3e22beb7c2b0458c0ce932d01afc","apps/ik-playground/src/scroll-reach.ts":"2ebbbc21d7efcfdfe4a797158cc2b728d014c5fb","docs/LIVE-EDIT-COST.md":"38d3329d0b4117378f1aa05b93acb6ddbbfbecc3","docs/guide/api-reference.md":"7dd26fbf7550b26df993558be59e08690f6a87fc","docs/guide/runtime-changes.md":"692970ba9ff68b173a2ff31066794b80c2800e23","docs/SESSION-STATUS.md":"464daed9cd1de41ac956beaa830f0e86e6df260d"},"message":"feat(runtime): publish once for a value batch, issue #362 phase 6","edits":[{"path":"packages/core/src/contract/value-transaction.ts","create":"import type { AuthoredValues, TrackHandle } from \"./track-handle\";\nimport type { PatchBatch } from \"./v5\";\n\n/**\n * The value authoring surface one batch is handed, and the whole of what a batch is for.\n *\n * `ProjectHandle.values(recipe)` opens one batch, hands this to the recipe, and publishes once when\n * the recipe returns. Every verb reached inside it still applies its own write immediately and still\n * refuses on its own terms, so \"each step individually correct\" is unchanged; what changes is that\n * publication is a separate thing from the write. `n` writes across `n` nodes cost one invalidate,\n * one notification per affected node and one sequence move, where the same sequence spelled one\n * write at a time costs `n` of each.\n *\n * Every member here is a projection of a verb this runtime already has, deliberately: a second\n * implementation of `setValues` would be a second owner of what a live write costs, and the whole\n * point of the tier is that there is one. What the interface buys is the narrowing, and only that.\n * It is not a fence, for the reason `SchemaTransaction`'s own record gives: a recipe closes over the\n * `ProjectHandle` it was called on, so what it may not do while a batch is open is answered by the\n * verbs themselves. Everything that publishes or mounts refuses by name with\n * `value-batch-immediate`, and every structural verb refuses with `value-batch-structural` at the\n * one member all of them reach.\n *\n * `track` and `tryTrack` answer the ordinary `TrackHandle`, whose value members join this batch\n * exactly as the three node-addressed verbs above do. That is why no node-addressed `setKeyframe` is\n * projected here: a `(nodeId, plugin, key, value)` spelling would lose the lifetime token the handle\n * captured, which is the staleness gate ADR-056 made total, and it would be a second owner of a verb\n * `TrackHandle` already owns.\n *\n * What a staged verb answers in the meantime is the deferred batch: its own node as the seed, no\n * patches, and one `value-batch-deferred` warning saying the publication was queued. Answering\n * nothing instead was refused, because `TrackHandle` is one interface across batched and unbatched\n * use, and a member with two return contracts, a nullable nobody checks, or a second narrowed handle\n * interface are all shapes this project refuses by name.\n *\n * Declared in `contract/` rather than in `runtime/`, on the rule the declaration-surface gate\n * enforces: the entry may not name a `runtime/` or a `graph/` module, and `ProjectHandle.values`\n * takes a function of this type, so a caller that cannot import it cannot write the recipe at all.\n * See ADR-078.\n */\nexport interface ValueTransaction {\n  /**\n   * Scrubs one leaf to `progress`, staging its publication rather than performing it.\n   *\n   * The verb `ProjectHandle.seek` is, unchanged, including that a driver-backed Motion overwrites a\n   * seeked value on its next emission.\n   */\n  seek(nodeId: string, progress: number): PatchBatch;\n  /**\n   * Rewrites one node's authored values, so its retained definition moves with them.\n   *\n   * `ProjectHandle.setValues` with its publication deferred, and nothing else about it moves: the\n   * refusals, the mask, the animated escalation and the retained definition are all where ADR-059\n   * and ADR-060 put them.\n   */\n  setValues(nodeId: string, values: AuthoredValues): PatchBatch;\n  /** The revertible half, leaving the authored definition exactly as it was. See ADR-060. */\n  overrideValues(nodeId: string, values: AuthoredValues): PatchBatch;\n  /**\n   * Resolves one track by qualified node id, refusing an id this project does not have.\n   *\n   * The ordinary handle, so `setValues`, `overrideValues`, `setKeyframe` and `removeKeyframe` on it\n   * join this batch while its structural members refuse.\n   */\n  track(nodeId: string): TrackHandle;\n  /** The same lookup without the refusal, for a caller whose miss is expected. */\n  tryTrack(nodeId: string): TrackHandle | undefined;\n}\n"},{"path":"packages/core/src/runtime/value-batch.ts","create":"import type { Diagnostic, PatchBatch } from \"../contract/v5\";\n\nexport const DEFERRED_VALUE_BATCH_RULE = \"value-batch-deferred\";\n\n/**\n * The batch a value verb answers while one is open: its own seed, no patches, one warning.\n *\n * The shape is `GraphRuntime`'s own `deferredBatch` under `reentrant-flush-deferred`, reused rather\n * than reinvented, because the answer a caller needs is identical: a publication was asked for, it\n * did not happen here, and this is the seed it will happen for. Owned by `ProjectRuntime` rather\n * than by `GraphRuntime` and given a rule id of its own, because the condition is different: that\n * one is a flush asked for while subscribers are being notified, and this one is a write inside a\n * batch the caller opened on purpose. Two conditions with one rule id would be one diagnostic a\n * consumer cannot branch on.\n *\n * Seeded rather than empty, because the seed is the one thing a caller can still act on: it names\n * the node whose publication is owed. `tick` carries the sequence the graph is still on, so a\n * consumer comparing it against the batch the enclosing `values()` answers can see that no\n * publication happened in between. See ADR-078.\n */\nexport function deferredValueBatch(sequence: number, seeds: readonly string[]): PatchBatch {\n  const ids = Object.freeze([...seeds]);\n  const diagnostic: Diagnostic = Object.freeze({\n    ruleId: DEFERRED_VALUE_BATCH_RULE,\n    path: \"value-batch\",\n    message:\n      \"A value write inside an open batch staged its seed; the batch publishes once when the recipe returns.\",\n    severity: \"warning\",\n    ids,\n  });\n  return Object.freeze({\n    tick: sequence,\n    seeds: ids,\n    patches: Object.freeze([]),\n    diagnostics: Object.freeze([diagnostic]),\n  }) as PatchBatch;\n}\n/**\n * The batch a recipe that staged nothing answers.\n *\n * No diagnostic, deliberately: nothing was queued and nothing was skipped, so there is nothing to\n * report. A recipe that staged nothing publishes nothing at all, on ADR-064's rule that an empty\n * seed list costs a whole publication rather than a cheap one, and this is what the call answers\n * instead of a publication it declined to make.\n */\nexport function emptyValueBatch(sequence: number): PatchBatch {\n  return Object.freeze({\n    tick: sequence,\n    seeds: Object.freeze([]),\n    patches: Object.freeze([]),\n    diagnostics: Object.freeze([]),\n  }) as PatchBatch;\n}\n"},{"path":"docs/ADR-078-value-tier-batch.md","create":"# ADR-078: The value tier gets a batch, and `#invalidateOne` stops being its only ending\n\n**Status:** Accepted, 2026-09-13\n\n**Context.** [Issue #362](https://github.com/chahyasantoso/motion5/issues/362) scanned the runtime surface for convenience verbs and found that the interesting gap was not either of the two the issue opened with. It was the value tier. `setValues`, `overrideValues`, `setKeyframe`, `removeKeyframe` and `seek` each return the `PatchBatch` of their own single invalidate, each ends at `#invalidateOne`, which is single-node by construction, and every one of them is refused inside a recipe by name. So a caller editing `n` nodes per frame publishes `n` times, notifies every subscriber `n` times and moves the sequence `n` times. `apps/ik-playground/src/scroll-reach.ts` said so in its own `commit()`, in a comment that read \"Each value write owns its invalidate; this is not a multi-track transaction\", while looping `setValues` and `setKeyframe` over `RIGS` on every qualifying scroll update. The structural tier got a transaction in ADR-064; the value tier never did.\n\nPhases 1a through 5 of that issue are what made this landable. Phases 1a and 1b bought the read budget back and gated it, phase 2 landed the enumeration readers, phase 3 and 4 subtracted three duplicate members and one wrong guide line, and phase 5 refused a membership batch on a measurement. This is the one phase with a capability in it, and it is the one the issue did not ask for.\n\n**Decision.** A second tier, `ProjectHandle.values(recipe)`, spelled like the one that already works. It returns the single `PatchBatch` of one invalidate, and it hands the recipe a frozen `ValueTransaction` declared in `contract/value-transaction.ts` carrying `seek`, `setValues` and `overrideValues` node-addressed plus `track` and `tryTrack` answering the ordinary `TrackHandle`. Three further things are decided here rather than left to the implementation, because each of them moves an ownership that another record already named.\n\n**One: `#invalidateOne` keeps its body, and the decision goes above it.** The plan for this phase named the DRY move as `#invalidateSeeds(ids)` with `#invalidateOne(id)` delegating to it. Measured against the tree rather than reasoned about, that is the expensive spelling. `#invalidateOne` is named as the value tier's publication owner by ADR-069's \"Where the report goes\", by ADR-070's single-responsibility list, by ADR-071's explicit decision that it and `#flush` stay separate, by two sections of `runtime/project-runtime.md`, by a comment in `motion-driver-edit.test.ts`, and by `LV-5` and `PK-17`, which read its declaration and assert that `this.#graph.invalidate([nodeId])` and `this.#diagnostics.recordAll(batch.diagnostics)` are inside it. Moving those two lines one member down turns two green cases red for no behavioural reason and makes four records name a member that answers a different question.\n\nSo the batch decision is a new member in front of it, `#publishValue(nodeId)`: with no batch open it delegates and nothing about the single-node path moves; with one open it asserts liveness, records the node as a seed, and answers the deferred batch. Its callers are both endings of `#completeWrite`, `#removeKeyframe`'s no-op, and `seek`, which stops copying `invalidate` plus `recordAll` inline and routes through the owner instead. One condition, read once, at one place, and adding the tier deleted a copy rather than adding one. `#invalidateOne` is still the only place a single-node value publication happens. It is no longer the only ending a value write can have, and that is the amendment this record makes to ADR-069, ADR-070 and ADR-071 rather than an edit to any of them.\n\nThe multi-seed publication owner is not invented either. `#flush(touched)` already invalidates a seed list in one call and records the batch's diagnostics, and `#apply` already uses it for exactly this, so the batch reuses that one. Its return widens from `void` to the batch it published, or nothing when it published none, which is what lets `values` report an empty recipe from the one owner of the empty-seed decision rather than restating it. Its three existing callers own no answer and ignore the return, and no test reads its declaration.\n\n**Two: the reentrancy rung splits rather than moves.** Every verb that publishes, mounts or commits has to refuse while a batch is open, because a narrowed surface handed to a callback is not a fence and the recipe closes over the handle. The value verbs must not, because they are the point. Adding the condition to `#refuseReentrant` refuses the tier it exists for; moving the rung out of `#writeValues` up to its four entry points spreads one ordering across five sites, which is the shape issues #298 and #310 both closed.\n\nSo it is a ladder, which is the idiom `#liveEntry` and `#writableEntry` already are. `#refuseValueReentrant(verb)` asks the existing two conditions in the existing order, an open recipe then a boundary in flight. `#refuseReentrant(verb)` calls it and adds the third, `value-batch-immediate`. The four value verbs and `seek` ask the first rung; `mount`, `unmount`, `signal`, `invalidate`, `#setTrigger` and `#setStagger` keep asking the second and gain the new refusal with no call site edited. `#commit` gains the structural twin, `value-batch-structural`, at the one member every structural verb reaches rather than at the six that reach it, and it goes after that member's existing in-flight condition so every answer that exists today is unchanged and only the new case is added. `values` inside `values` is refused by the same rung with no second spelling.\n\n**Three: `invalidate` is refused inside a batch rather than joined to it.** It is a bare publication with no bookkeeping of its own, so inside a batch that publishes once it is either a no-op or a second publication, and it is the verb a Motion driver reaches, which is precisely the reentrancy this rung exists to refuse. Joining it would also make the batch's seed list depend on which hook a host happened to wire.\n\n**Alternatives rejected.**\n\n- A flag on the existing verbs, or a variadic `setValues` taking a record of nodes. Rejected because it gives one member two publication contracts decided by an argument, which is the shape this project has now refused on `setMotion`, on `setTrack`, on the boolean that meant two things, and on the alias phase 3 deleted.\n- Staged verbs returning nothing. Rejected because `track(nodeId)` answers a `TrackHandle` whose value members are typed `PatchBatch` across batched and unbatched use, and `TrackHandle` is one interface. \"Nothing\" needs a member with two return contracts, a nullable nobody checks, or a second narrowed handle interface, and all three are refused by name. The deferred batch is the answer that already exists in this tree for exactly this question.\n- Reachability inside `edit()`. Rejected on the rule that a verb which applies immediately is refused by name inside a transaction rather than deferred into it: a settle step has no inverse, so admitting a fallible value write into a structural recipe moves its refusal to after the graph committed. Two tiers that do not nest, refused by name in both directions.\n- Widening `SchemaTransaction` with the value verbs instead of adding a second surface. Rejected for the same reason phase 2 declined to widen it with the readers: it would give one recipe two failure contracts, and interface segregation is why each tier gets the narrowed surface it needs.\n- Deduplicating seeds in `#valueSeeds`. Rejected because `GraphRuntime.flush` already deduplicates, and a second owner of that is a second answer.\n- Answering the recipe's own return value, as `edit` does. Rejected because the batch is the answer to this call: a member that answered both would need the caller to ask for the publication separately, and then the tier has two mechanisms again.\n\n**Consequences.** One public member and one exported type are added, `ProjectHandle.values` and `ValueTransaction`. Nothing is removed and no existing signature or refusal set moves. A disposed project reached from inside a recipe is reported rather than answered, with `ProjectRuntime is disposed.` in place of the batch, which is the string the value tier already answers, so the two tiers do not start answering one condition two ways. Publication runs inside `#boundary`, because subscriber notification is injected code and a listener that disposes must not release the graph out from under the notification it is inside. `apps/ik-playground/src/scroll-reach.ts` becomes one `values(...)` call and its comment about owning its own invalidate is deleted rather than reworded, which is the lie detector under this whole record: the consumer the scan found is the consumer that stops looping publications.\n\n**Evidence.** `RA-162` through `RA-169` in `packages/core/test/unit/runtime/value-batch.test.ts`, pushed as their own commit before the source, so the red run is a real run.\n\n- `RA-162` is the counter and the oracle in one rig, on the optimisation guardrail. Three nodes, one publisher flush and one sequence move; then the same three edits one at a time against a second runtime, asserting three flushes and three sequence moves and asserting the two runtimes published identical patches. A counter alone cannot see a stale composer, and an oracle alone is green against a tier that never batched.\n- `RA-163` asserts the deferred batch names its node, carries no patch, carries one `value-batch-deferred` diagnostic, and answers while the sequence is still unmoved.\n- `RA-164` refuses a batch inside a batch and a batch inside a recipe, and asserts the accepting direction in the same rig.\n- `RA-165` refuses `invalidate`, `mount`, `unmount` and `signal` as `value-batch-immediate` and `addTrack` and `TrackHandle.remove` as `value-batch-structural`, asserts nothing the refused structural verbs staged was committed, and asserts each verb applies normally once the batch has returned.\n- `RA-166` drives both endings of `#completeWrite`, the escalated one that stages a Track and re-seeks and the direct one every static write takes, because the escalated ending is the one with a settle collector in front of its publication.\n- `RA-167` is the empty recipe and the read-only recipe: no invalidate at all, sequence unmoved, an empty batch as the answer.\n- `RA-168` is a throwing recipe: the caller's throw unwrapped, nothing published, and the next batch ordinary rather than a refusal against a batch nothing closed.\n- `RA-169` is a recipe that disposes: the disposal reported in place of the batch, and no publication.\n\nOne scope correction against the plan for this phase, recorded because a plan's prescription is measured before it is implemented. The plan named `setKeyframe` joining through both of its paths, including the recompile a new key takes, as one of the eight cases. `RA-166` instead drives both endings of `#completeWrite`, which is the mechanism `setKeyframe`, `removeKeyframe` and the recompile all join through, and it does so without authoring a plugin group whose registry-free resolution is a second subject. Nothing on that path is left unowned; what is not asserted here is the group-authoring precondition, which `RA-106` already owns.\n"},{"path":"packages/core/src/runtime/schema-refusals.ts","find":"export function commitInFlight(): never {\n  const use = \"Ask for it once this one has returned.\";\n  throw new TypeError(`schema-commit-reentrant: A structural commit is already in flight. ${use}`);\n}\n","replace":"export function commitInFlight(): never {\n  const use = \"Ask for it once this one has returned.\";\n  throw new TypeError(`schema-commit-reentrant: A structural commit is already in flight. ${use}`);\n}\n/**\n * Refuses an immediate verb asked for while a value batch is open.\n *\n * The third condition on the shared immediate rung, and a third rather than a widening of the first\n * because which callback the caller wrote is what differs. `schema-transaction-immediate` names a\n * verb that cannot travel with a structural recipe at all; this one names a verb that cannot travel\n * with a value batch, which the four value verbs and `seek` can, since staging their publication is\n * the whole point of opening one. So the message names the verb, exactly as the recipe refusal does:\n * the caller wrote both the `values()` and the call, and has to know which of the two to move.\n *\n * `invalidate` is refused here rather than joined to the batch, deliberately. It carries no\n * bookkeeping of its own, so inside a batch that publishes once it is either a no-op or a second\n * publication, and it is the verb a Motion driver reaches, which is the reentrancy this condition\n * exists to refuse. A batch asked for inside a batch reaches this same refusal, so nesting needs no\n * second spelling. See ADR-078.\n */\nexport function valueBatchImmediate(verb: string): never {\n  const detail = `\"${verb}\" publishes or mounts and cannot travel with a value batch.`;\n  throw new TypeError(`value-batch-immediate: ${detail} Call it outside values().`);\n}\n/**\n * Refuses a structural commit asked for while a value batch is open.\n *\n * `commitInFlight`'s twin, at the one member every structural verb reaches and for the same reason\n * it names the condition rather than the verb: the caller wrote no verb list, and a per-verb\n * spelling would cost a string literal at six call sites to say what the stack already says.\n *\n * A refusal rather than a deferral, because the two tiers do not nest in either direction. A\n * structural commit derives effects, replaces the graph and adopts a pair; a value batch holds a\n * seed list and nothing else, so there is no pair for one to stage inside the other and no inverse\n * for a settle step to run. See ADR-078.\n */\nexport function valueBatchStructural(): never {\n  const use = \"Ask for it once values() has returned.\";\n  throw new TypeError(`value-batch-structural: A value batch is open. ${use}`);\n}\n"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  reservedGoalSlot,\n  unboundGroup,\n} from \"./schema-refusals\";","replace":"  reservedGoalSlot,\n  unboundGroup,\n  valueBatchImmediate,\n  valueBatchStructural,\n} from \"./schema-refusals\";"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"import type { SchemaTransaction } from \"../contract/schema-transaction\";","replace":"import type { SchemaTransaction } from \"../contract/schema-transaction\";\nimport type { ValueTransaction } from \"../contract/value-transaction\";"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"import { collect, report, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";","replace":"import { collect, report, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";\nimport { deferredValueBatch, emptyValueBatch } from \"./value-batch\";"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  #open: OpenTransaction | undefined;\n  readonly #diagnostics: Diagnostics;","replace":"  #open: OpenTransaction | undefined;\n  #valueSeeds: string[] | undefined;\n  readonly #diagnostics: Diagnostics;"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  #refuseReentrant(verb: string): void {\n    if (this.#open !== undefined) immediateInTransaction(verb);\n    if (this.#inFlight > 0) commitInFlight();\n  }","replace":"  #refuseValueReentrant(verb: string): void {\n    if (this.#open !== undefined) immediateInTransaction(verb);\n    if (this.#inFlight > 0) commitInFlight();\n  }\n\n  #refuseReentrant(verb: string): void {\n    this.#refuseValueReentrant(verb);\n    if (this.#valueSeeds !== undefined) valueBatchImmediate(verb);\n  }"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  #commit(plan: SchemaPlan): void {\n    if (this.#open !== undefined) return;\n    if (this.#inFlight > 0) commitInFlight();\n    this.#apply(plan);\n  }","replace":"  #commit(plan: SchemaPlan): void {\n    if (this.#open !== undefined) return;\n    if (this.#inFlight > 0) commitInFlight();\n    if (this.#valueSeeds !== undefined) valueBatchStructural();\n    this.#apply(plan);\n  }"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  #flush(touched: readonly string[]): void {\n    if (this.#disposed || touched.length === 0) return;\n    const batch = this.#graph.invalidate(touched);\n    this.#diagnostics.recordAll(batch.diagnostics);\n  }","replace":"  #flush(touched: readonly string[]): PatchBatch | undefined {\n    if (this.#disposed || touched.length === 0) return undefined;\n    const batch = this.#graph.invalidate(touched);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"    this.#refuseReentrant(rebase ? \"setValues\" : \"overrideValues\");","replace":"    this.#refuseValueReentrant(rebase ? \"setValues\" : \"overrideValues\");"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"    if (staged === undefined && progress === undefined) return this.#invalidateOne(nodeId);","replace":"    if (staged === undefined && progress === undefined) return this.#publishValue(nodeId);"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"      () => {\n        batch = this.#invalidateOne(nodeId);\n      },","replace":"      () => {\n        batch = this.#publishValue(nodeId);\n      },"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  #invalidateOne(nodeId: string) {\n    this.#assertLive();\n    const batch = this.#graph.invalidate([nodeId]);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }","replace":"  #invalidateOne(nodeId: string) {\n    this.#assertLive();\n    const batch = this.#graph.invalidate([nodeId]);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }\n\n  #publishValue(nodeId: string): PatchBatch {\n    const open = this.#valueSeeds;\n    if (open === undefined) return this.#invalidateOne(nodeId);\n    this.#assertLive();\n    open.push(nodeId);\n    return deferredValueBatch(this.#graph.sequence, [nodeId]);\n  }"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"    this.#refuseReentrant(\"setKeyframe\");","replace":"    this.#refuseValueReentrant(\"setKeyframe\");"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"    this.#refuseReentrant(\"removeKeyframe\");","replace":"    this.#refuseValueReentrant(\"removeKeyframe\");"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"    if (edited === keyframes) return this.#invalidateOne(nodeId);","replace":"    if (edited === keyframes) return this.#publishValue(nodeId);"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  seek(nodeId: string, progress: number) {\n    this.#assertLive();\n    this.#refuseReentrant(\"seek\");\n    this.#setProgress(nodeId, progress);\n    const batch = this.#graph.invalidate([nodeId]);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }","replace":"  seek(nodeId: string, progress: number) {\n    this.#assertLive();\n    this.#refuseValueReentrant(\"seek\");\n    this.#setProgress(nodeId, progress);\n    return this.#publishValue(nodeId);\n  }"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  setValues(nodeId: string, values: AuthoredValues) {\n    this.#assertLive();\n    return this.#writeValues(nodeId, () => this.#entryOf(nodeId), values, true);\n  }\n  invalidate(nodeIds: readonly string[]) {","replace":"  setValues(nodeId: string, values: AuthoredValues) {\n    this.#assertLive();\n    return this.#writeValues(nodeId, () => this.#entryOf(nodeId), values, true);\n  }\n  /**\n   * Runs `recipe` as one value batch and publishes what it staged exactly once.\n   *\n   * The second tier, and what `edit(recipe)` is for the structural one. Every verb reached inside\n   * still applies its own write immediately and still refuses on its own terms; what the batch owns\n   * is the publication. So `n` writes across `n` nodes cost one invalidate, one notification per\n   * affected node and one sequence move, where the same sequence spelled one write at a time costs\n   * `n` of each, and the answer is the batch that one publication produced. What a staged verb\n   * answers in the meantime is a deferred batch naming its own node.\n   *\n   * A recipe that staged nothing publishes nothing and answers an empty batch, because an empty seed\n   * list still opens a batch, notifies every batch subscriber and moves the sequence. A recipe that\n   * disposed the project is reported rather than answered, with `ProjectRuntime is disposed.` in\n   * place of the batch, because the batch is the answer to this call and there is no batch: that\n   * keeps the value tier's one failure contract. See ADR-078, ADR-064 and issue #288.\n   */\n  values(recipe: (transaction: ValueTransaction) => void): PatchBatch {\n    this.#assertLive();\n    this.#refuseReentrant(\"values\");\n    const seeds: string[] = [];\n    this.#valueSeeds = seeds;\n    try {\n      recipe(this.#valueTransaction());\n    } finally {\n      this.#valueSeeds = undefined;\n    }\n    this.#assertLive();\n    return this.#boundary(() => this.#flush(seeds) ?? emptyValueBatch(this.#graph.sequence));\n  }\n\n  #valueTransaction(): ValueTransaction {\n    const runtime = this;\n    return Object.freeze({\n      seek: (nodeId: string, progress: number) => runtime.seek(nodeId, progress),\n      setValues: (nodeId: string, values: AuthoredValues) => runtime.setValues(nodeId, values),\n      overrideValues: (nodeId: string, values: AuthoredValues) =>\n        runtime.overrideValues(nodeId, values),\n      track: (nodeId: string) => runtime.track(nodeId),\n      tryTrack: (nodeId: string) => runtime.tryTrack(nodeId),\n    });\n  }\n  invalidate(nodeIds: readonly string[]) {"},{"path":"packages/core/src/runtime/project-runtime.md","find":"## #inFlight\n","replace":"## #valueSeeds\n\nThe seed list one value batch collects, present only while its recipe is running. A plain array rather than a record, because a wrapper that exists only to hold one value is deleted and the thing it held becomes the value, and named for what it holds rather than for the first verb that filled it. values clears it in finally before re-asking liveness and publishing, so a recipe that threw leaves no batch open and the next call is an ordinary one rather than a reentrancy refusal against a batch nothing closed.\n\nA batch stages publication rather than state, so every read inside one answers exactly what it answers outside one and no accessor gains a second meaning. Duplicate seeds stay in place because GraphRuntime.flush already deduplicates and a second owner of that would be a second answer. Structural verbs and the immediate family read this field through their own rungs rather than inspecting it. See ADR-078.\n\n## #inFlight\n"},{"path":"packages/core/src/runtime/project-runtime.md","find":"## #refuseReentrant\n\nThe shared rung for immediate mutation/publication: first refuse an open recipe with schema-transaction-immediate naming the verb, then an in-flight boundary with schema-commit-reentrant. The first condition concerns caller-authored recipes; the second concerns callbacks an owning operation invoked. They currently cannot both be true, but the precedence remains explicit.\n","replace":"## #refuseValueReentrant\n\nThe lower rung of a two-step ladder, and the two conditions the immediate family has always asked, in the order it has always asked them: an open recipe as schema-transaction-immediate naming the verb, then an in-flight boundary as schema-commit-reentrant. The four value verbs and seek ask this one and stop here, because staging their publication is what a value batch is for.\n\nA split rather than a third condition inside the existing rung, and rather than the same condition copied up to each entry point. Adding it below would refuse the tier the batch exists for; moving the rung out of #writeValues up to its four callers would spread one ordering across five sites, which is the shape issues #298 and #310 both closed. A ladder is the idiom #liveEntry and #writableEntry already are. See ADR-078.\n\n## #refuseReentrant\n\nThe shared rung for immediate mutation/publication, and the upper rung of that ladder: it asks #refuseValueReentrant first, then refuses an open value batch with value-batch-immediate naming the verb. The first condition concerns caller-authored recipes, the second concerns callbacks an owning operation invoked, and the third concerns a caller-authored value batch. The precedence stays explicit even where two of them cannot both be true, because two conditions nothing orders are two conditions the next slice picks between by accident.\n\nmount, unmount, signal, invalidate, #setTrigger and #setStagger reach the new refusal through this member with no call site edited, and values asks it too, so a batch inside a batch needs no second spelling. invalidate is refused rather than joined: it carries no bookkeeping of its own, so inside a batch that publishes once it is either a no-op or a second publication, and it is the verb a Motion driver reaches. See ADR-078.\n"},{"path":"packages/core/src/runtime/project-runtime.md","find":"A recipe started by a hook can stage normally but is refused when it attempts to commit; this keeps one owner for structural reentrancy.","replace":"A recipe started by a hook can stage normally but is refused when it attempts to commit; this keeps one owner for structural reentrancy. An open value batch refuses after that, as value-batch-structural, at this one member rather than at the six verbs that reach it: the two tiers do not nest in either direction, because a commit derives effects, replaces the graph and adopts a pair while a batch holds a seed list and nothing else. Placing the new condition after the in-flight one leaves every answer that existed before unchanged and adds only the case where a structural verb is reached from the recipe body itself. See ADR-078."},{"path":"packages/core/src/runtime/project-runtime.md","find":"Unlike #invalidateOne it returns no batch and skips disposal instead of asserting. Structural commits and accepted stagger changes share this mechanism, not their topology semantics. See ADR-064, ADR-069 and ADR-071.","replace":"Unlike #invalidateOne it skips disposal instead of asserting. It answers the batch it published, or nothing when it published none, which is what lets values report an empty recipe from this one owner of the empty-seed decision rather than restating it; the three callers that own no answer ignore the return. Structural commits, accepted stagger changes and the value batch share this mechanism, not their topology semantics. See ADR-064, ADR-069, ADR-071 and ADR-078."},{"path":"packages/core/src/runtime/project-runtime.md","find":"After adoption, attempt staged finalization, optional re-seek to captured progress, and #invalidateOne through runSettleSteps. A static/no-escalation path uses the existing flush directly.","replace":"After adoption, attempt staged finalization, optional re-seek to captured progress, and #publishValue through runSettleSteps. A static/no-escalation path reaches #publishValue directly. Both endings therefore route through the one member that decides whether a value publication happens now or joins an open batch, so neither of them owns that condition."},{"path":"packages/core/src/runtime/project-runtime.md","find":"The value tier's single-node flush and disposal report. Assert runtime liveness before invalidating; a disposed project must not publish, advance sequence or drain pending seeds, and an empty batch would falsely claim publication. Records returned diagnostics and returns the actual batch. Both value-write paths use this owner rather than inline copies or public invalidate. See ADR-064, ADR-069 and ADR-070.\n","replace":"The value tier's single-node flush and disposal report. Assert runtime liveness before invalidating; a disposed project must not publish, advance sequence or drain pending seeds, and an empty batch would falsely claim publication. Records returned diagnostics and returns the actual batch. Both value-write paths reach this owner rather than inline copies or public invalidate.\n\nIts body is unchanged by the value batch, deliberately. Four records and two shipped cases name this member as the published single-node value flush, so the batch decision goes in front of it in #publishValue rather than moving these two lines one member down, which would have turned two green cases red for no behavioural reason. It is still the only place a single-node value publication happens; it is no longer the only ending a value write can have. See ADR-064, ADR-069, ADR-070 and ADR-078.\n\n## #publishValue\n\nThe one owner of whether a value write publishes now or joins an open batch. With no batch open it delegates to #invalidateOne and nothing about the single-node path moves. With one open it asserts runtime liveness, records the node as a seed, and answers the deferred batch: the node named, no patches, and one value-batch-deferred warning saying the publication was queued.\n\nIts callers are both endings of #completeWrite, #removeKeyframe's no-op, and seek, which stops copying invalidate plus recordAll inline and routes through this owner instead, so adding the tier deleted a copy rather than adding one. Liveness is asserted on the staging path too, because a recipe may dispose the project and a seed pushed onto a dead runtime would be published by a batch that must not publish at all. See ADR-078.\n"},{"path":"packages/core/src/runtime/project-runtime.md","find":"## #teardown\n","replace":"## #valueTransaction\n\nA frozen projection of existing runtime verbs, not another implementation, exactly as #transaction is. Narrowing is not a fence: a recipe closes over the project handle, so every verb enforces its own refusal regardless of how it was reached, and what may not run while a batch is open is answered at the verb rather than by the absence of a member here.\n\ntrack and tryTrack hand back the ordinary TrackHandle, whose value members join the batch through #publishValue while its structural members refuse at #commit. No node-addressed setKeyframe is projected: that spelling would lose the lifetime token the handle captured, and it would be a second owner of a verb TrackHandle already owns. See ADR-056, ADR-064 and ADR-078.\n\n## #teardown\n"},{"path":"packages/core/src/engine.ts","find":"import type { SchemaTransaction } from \"./contract/schema-transaction\";","replace":"import type { SchemaTransaction } from \"./contract/schema-transaction\";\nimport type { ValueTransaction } from \"./contract/value-transaction\";"},{"path":"packages/core/src/engine.ts","find":"  edit<T>(recipe: (transaction: SchemaTransaction) => T): T;\n  addTrack(track: TrackDefinition, options?: { motionId?: string }): TrackHandle;","replace":"  edit<T>(recipe: (transaction: SchemaTransaction) => T): T;\n  /**\n   * Runs `recipe` as one value batch and publishes what it staged exactly once.\n   *\n   * The value tier's transaction, and `edit`'s counterpart rather than its cousin: this one defers\n   * publication instead of deferring the write, so every verb inside still applies immediately and\n   * still refuses on its own terms. `ValueTransaction` is the narrowed surface the recipe is handed,\n   * and it is a surface rather than a fence for exactly the reason `edit`'s is: the recipe closes\n   * over this handle too, so a verb that publishes, mounts or commits refuses by name while a batch\n   * is open, with `value-batch-immediate` for the immediate family and `value-batch-structural` for\n   * every structural verb. What one costs and what it answers are ADR-078's. See ADR-078.\n   */\n  values(recipe: (transaction: ValueTransaction) => void): PatchBatch;\n  addTrack(track: TrackDefinition, options?: { motionId?: string }): TrackHandle;"},{"path":"packages/core/src/engine.ts","find":"    edit<T>(recipe: (transaction: SchemaTransaction) => T) {\n      return runtime.edit(recipe);\n    },","replace":"    edit<T>(recipe: (transaction: SchemaTransaction) => T) {\n      return runtime.edit(recipe);\n    },\n    values: (recipe) => runtime.values(recipe),"},{"path":"packages/core/src/index.ts","find":"export type { SchemaTransaction } from \"./contract/schema-transaction\";","replace":"export type { SchemaTransaction } from \"./contract/schema-transaction\";\n/**\n * The value authoring surface one batch is handed, exported on the same half of the\n * declaration-surface rule as the structural one above: `ProjectHandle.values` takes a function of\n * this type, and a member the entry exposes is unusable if its argument type cannot be imported.\n * Declared in `contract/` for the reason the gate enforces, so naming it here adds no `runtime/`\n * module to the entry's declaration closure. Its two refusals travel as `TypeError` under\n * `value-batch-immediate` and `value-batch-structural`, and the diagnostic a staged write carries is\n * `value-batch-deferred`, so nothing new has to be exported to catch or to branch on them. See\n * ADR-078.\n */\nexport type { ValueTransaction } from \"./contract/value-transaction\";"},{"path":"packages/core/test/unit/runtime/value-batch.test.ts","find":"import type { AuthoredValues, TrackHandle } from \"../../../src/contract/track-handle\";\n","replace":""},{"path":"packages/core/test/unit/runtime/value-batch.test.ts","find":" * The whole block is red the day it lands, and that is a property of the slice rather than a target.\n * This is a capability, so nothing answered before: eight of eight fail on assertions rather than on\n * a missing member, which is what the locally declared seam below is for. That declaration is\n * deleted in the commit that lands the source.\n","replace":" * The whole block was red the day it landed, and that is a property of the slice rather than a\n * target. This is a capability, so nothing answered before: eight of eight failed on assertions\n * rather than on a missing member, because the seam was declared locally and cast until the source\n * arrived. That declaration is deleted here and the cases read the shipped member instead, which is\n * what the first assertion in each of them is still asking about.\n"},{"path":"packages/core/test/unit/runtime/value-batch.test.ts","find":"interface ValueRecipe {\n  seek(nodeId: string, progress: number): PatchBatch;\n  setValues(nodeId: string, values: AuthoredValues): PatchBatch;\n  overrideValues(nodeId: string, values: AuthoredValues): PatchBatch;\n  track(nodeId: string): TrackHandle;\n  tryTrack(nodeId: string): TrackHandle | undefined;\n}\ninterface ValueBatchSeam {\n  values(recipe: (batch: ValueRecipe) => void): PatchBatch;\n}\n\nconst NODES","replace":"const NODES"},{"path":"packages/core/test/unit/runtime/value-batch.test.ts","find":"  }) as ProjectRuntime & ValueBatchSeam;\n","replace":"  });\n"},{"path":"packages/core/test/unit/runtime/value-batch.test.ts","find":"        expect(() => runtime.signal(\"rig\", { type: \"start\" })).toThrow(\"value-batch-immediate\");","replace":"        expect(() => runtime.signal(\"rig\", { type: \"manual\" })).toThrow(\"value-batch-immediate\");"},{"path":"packages/core/test/unit/runtime/value-batch.test.ts","find":"      expect(() => runtime.signal(\"rig\", { type: \"start\" })).not.toThrow();","replace":"      expect(() => runtime.signal(\"rig\", { type: \"manual\" })).not.toThrow();"},{"path":"apps/ik-playground/src/scroll-reach.ts","find":"export function createScrollReach(project: Pick<ProjectHandle, \"track\">) {","replace":"export function createScrollReach(project: Pick<ProjectHandle, \"values\">) {"},{"path":"apps/ik-playground/src/scroll-reach.ts","find":"    commit(): void {\n      // Only pending intent is authored here. Motion owns progress and interpolated FK weights.\n      // Each value write owns its invalidate; this is not a multi-track transaction.\n\n      for (const rig of RIGS) {\n        const next = goals[rig.goalTrack]!;\n        const previous = appliedGoals[rig.goalTrack]!;\n        if (next.x !== previous.x || next.y !== previous.y) {\n          project.track(nodeId(rig.goalTrack)).setValues({ x: next.x, y: next.y });\n        }\n        const flip = flips.get(rig.solverTrack)!;\n        if (flip !== appliedFlips.get(rig.solverTrack)) {\n          project.track(nodeId(rig.solverTrack)).setKeyframe(\"ik\", \"flip\", flip);\n        }\n      }\n      appliedGoals = goals;","replace":"    commit(): void {\n      // Only pending intent is authored here. Motion owns progress and interpolated FK weights.\n      // One batch per qualifying scroll update, so every rig that moved publishes together.\n      project.values((batch) => {\n        for (const rig of RIGS) {\n          const next = goals[rig.goalTrack]!;\n          const previous = appliedGoals[rig.goalTrack]!;\n          if (next.x !== previous.x || next.y !== previous.y) {\n            batch.track(nodeId(rig.goalTrack)).setValues({ x: next.x, y: next.y });\n          }\n          const flip = flips.get(rig.solverTrack)!;\n          if (flip !== appliedFlips.get(rig.solverTrack)) {\n            batch.track(nodeId(rig.solverTrack)).setKeyframe(\"ik\", \"flip\", flip);\n          }\n        }\n      });\n      appliedGoals = goals;"},{"path":"docs/LIVE-EDIT-COST.md","find":"both of those are answerable from `mountedNodeIds()` and the three readers above before the call, so a partial loop is discoverable rather than something a collector has to report. See ADR-077.","replace":"both of those are answerable from `mountedNodeIds()` and the three readers above before the call, so a partial loop is discoverable rather than something a collector has to report. See ADR-077.\n- The value tier has a transaction of its own, and what it batches is the publication rather than the write. `values(recipe)` opens one, hands the recipe a narrowed `ValueTransaction` carrying `seek`, `setValues` and `overrideValues` node-addressed plus `track` and `tryTrack`, and publishes once when the recipe returns, so `n` writes across `n` nodes cost one `invalidate`, one notification per affected node and one sequence move where the same sequence spelled one write at a time costs `n` of each. Everything else about each verb is unchanged: it validates on entry, it applies its write immediately, its refusals are where they were, and the escalation to a staged Track and a re-seek still happens per node, because what an interpolator can patch is not a property of how many nodes are being written. So the saving is the publication and only the publication, and that is the whole of it: a batch does not make a write cheaper, it makes `n - 1` publications not happen. A verb reached inside one answers a deferred batch naming its own node with no patches and one `value-batch-deferred` warning, rather than answering nothing, because a `TrackHandle` is one interface across batched and unbatched use. A recipe that staged nothing publishes nothing at all and answers an empty batch, on the same rule that stops a commit with no seeds from calling `invalidate`. A recipe that disposed the project is answered with `ProjectRuntime is disposed.` in place of the batch rather than with a batch nobody can read, which is the same string every value verb already answers. Everything that publishes or mounts is `value-batch-immediate` from inside one, named at the verb, and that includes `invalidate` and a second `values`; every structural verb is `value-batch-structural` at the one member all of them reach, and a refused structural verb stages nothing, so the batch commits none of it. A value verb inside `edit(recipe)` stays `schema-transaction-immediate`: the two tiers do not nest in either direction. See ADR-078."},{"path":"docs/guide/api-reference.md","find":"- `edit(recipe)` runs one `SchemaTransaction` and commits what it staged exactly once, answering with whatever the recipe returned.","replace":"- `edit(recipe)` runs one `SchemaTransaction` and commits what it staged exactly once, answering with whatever the recipe returned.\n- `values(recipe)` runs one `ValueTransaction` and publishes what it staged exactly once, answering the `PatchBatch` of that single publication."},{"path":"docs/guide/api-reference.md","find":"`edit(recipe)` on `ProjectHandle` runs `n` authored ops across `m` tracks as one transaction: one candidate build, one graph replacement, one `ObservationState` commit and one flush. The recipe is handed a `SchemaTransaction`, which carries `addMotion`, `motion`, `tryMotion`, `addTrack`, `track` and `tryTrack` and nothing else, so `mount`, `seek`, `subscribeNode` and `dispose` are not reachable through it. Reads inside the recipe resolve against what it has staged, so a two-step edit sees its own first step, and a throw commits nothing: no hook is reached and a handle issued inside the recipe never becomes live. A recipe inside a recipe is `schema-transaction-nested`, and a verb that applies immediately rather than committing is `schema-transaction-immediate`, named at the verb. See ADR-064.\n","replace":"`edit(recipe)` on `ProjectHandle` runs `n` authored ops across `m` tracks as one transaction: one candidate build, one graph replacement, one `ObservationState` commit and one flush. The recipe is handed a `SchemaTransaction`, which carries `addMotion`, `motion`, `tryMotion`, `addTrack`, `track` and `tryTrack` and nothing else, so `mount`, `seek`, `subscribeNode` and `dispose` are not reachable through it. Reads inside the recipe resolve against what it has staged, so a two-step edit sees its own first step, and a throw commits nothing: no hook is reached and a handle issued inside the recipe never becomes live. A recipe inside a recipe is `schema-transaction-nested`, and a verb that applies immediately rather than committing is `schema-transaction-immediate`, named at the verb. See ADR-064.\n\n`values(recipe)` is the same idea for the cheap tier, and it defers the publication rather than the write. The recipe is handed a `ValueTransaction`, which carries `seek`, `setValues` and `overrideValues` addressed by node id, plus `track` and `tryTrack` answering the ordinary `TrackHandle` so that its `setKeyframe` and `removeKeyframe` join the batch too. Every verb still validates on entry, still applies its write when you call it, and still refuses exactly what it refused before; what changes is that `n` writes cost one `invalidate` and one sequence move instead of `n`. The call answers the `PatchBatch` of that one publication, and a verb reached inside answers a deferred batch naming its own node with no patches and one `value-batch-deferred` warning, because a `TrackHandle` is one interface across batched and unbatched use and a member with two return contracts is not. A recipe that staged nothing publishes nothing and answers an empty batch. Everything that publishes or mounts refuses inside one as `value-batch-immediate`, including `invalidate` and a nested `values`, and every structural verb refuses as `value-batch-structural`; a value verb inside `edit(recipe)` stays `schema-transaction-immediate`, because the two tiers do not nest in either direction. A recipe that disposes the project is answered with `ProjectRuntime is disposed.` rather than with a batch nobody can read. See ADR-078.\n"},{"path":"docs/guide/runtime-changes.md","find":"## Changing values without rebuilding the graph\n","replace":"## Many value writes, one publication\n\n`handle.edit(recipe)` batches structural work. `handle.values(recipe)` batches the other tier, and what it batches is the publication rather than the write:\n\n```ts\nhandle.values((batch) => {\n  for (const rig of rigs) {\n    batch.track(rig.goal).setValues({ x: rig.x, y: rig.y });\n    batch.track(rig.solver).setKeyframe(\"ik\", \"flip\", rig.flip);\n  }\n});\n```\n\nOutside a batch each of those writes publishes on its own, so `n` rigs cost `n` invalidations, `n` rounds of subscriber notification and `n` sequence moves. Inside one they cost one of each, and the call answers the `PatchBatch` that single publication produced. That is the whole saving, stated as narrowly as it deserves: a batch does not make a write cheaper. Each verb still validates on entry, still applies its write when you call it, and still escalates to a staged Track and a re-seek per node when the interpolator declines a per-key write, because what a backend can patch has nothing to do with how many nodes you are writing.\n\nThe recipe is handed a `ValueTransaction` carrying `seek`, `setValues` and `overrideValues` addressed by node id, plus `track` and `tryTrack`. Those two answer the ordinary `TrackHandle`, so `setValues`, `overrideValues`, `setKeyframe` and `removeKeyframe` on it join the batch as well, and there is deliberately no node-addressed `setKeyframe` here: that spelling would throw away the lifetime token the handle captured, which is the whole mechanism behind `StaleTrackHandleError`.\n\nA verb reached inside a batch answers a deferred batch rather than nothing: your own node as the only seed, no patches, and one warning whose `ruleId` is `value-batch-deferred`, saying the publication was queued. A `TrackHandle` is one interface whether or not a batch is open, so a member that answered `undefined` in one case would be a value nobody checks. A recipe that staged nothing publishes nothing at all and answers an empty batch, for the same reason a commit with no seeds does not call `invalidate`: an empty batch still opens one, notifies every batch subscriber and moves the sequence.\n\nTwo refusals, and they name where the call was made rather than what it does. Everything that publishes or mounts is `value-batch-immediate` inside a batch, named at the verb, which covers `seek`, `invalidate`, `mount`, `unmount`, `signal`, `setTrigger`, `setStagger` reached on the handle itself, and a second `values`. Every structural verb is `value-batch-structural`, at the one member all of them reach, and it stages nothing, so a refused `addTrack` or `remove()` leaves the project exactly as it was. Going the other way, a value verb inside `edit(recipe)` is still `schema-transaction-immediate`: the two tiers do not nest, in either direction. A recipe that disposes the project is answered with `ProjectRuntime is disposed.` in place of the batch, because the batch is the answer to the call and there is no batch. See ADR-078.\n\n## Changing values without rebuilding the graph\n"},{"path":"docs/SESSION-STATUS.md","find":"- **Inspected main:** `4ee7c67ff2401174fa0c14d68c40698e9a7968db`, after the surface subtraction [#366](https://github.com/chahyasantoso/motion5/pull/366). Exact CI and operation evidence belong to their PRs.","replace":"- **Inspected main:** `8ea61a3f2a77639be174c3fb1eafdf3b2e437976`, after the membership measurement [#367](https://github.com/chahyasantoso/motion5/pull/367). Exact CI and operation evidence belong to their PRs."},{"path":"docs/SESSION-STATUS.md","find":"- **Runtime surface work landed, phases 2 through 4 of [#362](https://github.com/chahyasantoso/motion5/issues/362):**","replace":"- **Runtime surface work landed, phases 2 through 5 of [#362](https://github.com/chahyasantoso/motion5/issues/362):**"},{"path":"docs/SESSION-STATUS.md","find":"ADR-076 records the removal and ADR-020 is superseded rather than left contradicting it.","replace":"ADR-076 records the removal and ADR-020 is superseded rather than left contradicting it. [#367](https://github.com/chahyasantoso/motion5/pull/367) then refuses `mountAll` and `unmountAll` on a measurement rather than adding them, so phase 5 ships four cases and no runtime code: the membership revision is the key the publisher snapshot's memo is compared against rather than an invalidation, and that snapshot is derived inside `flush` and nowhere else, so `n` mounts before one flush already derive one. `RA-158` through `RA-161`, recorded in ADR-077."},{"path":"docs/SESSION-STATUS.md","find":"- **Unmerged membership measurement:** phase 5 of [#362](https://github.com/chahyasantoso/motion5/issues/362) prices `mountAll` / `unmountAll` and refuses both, so it adds no verb, no tier and no runtime code at all. The saving the plan priced is zero rather than small: the membership revision is the key the publisher snapshot's memo is compared against rather than an invalidation, and the snapshot is derived inside `flush` and nowhere else, so `n` mounts before one flush already derive one snapshot. `unmount` has no per-id refusal for a collector to own, `mount`'s two are answered before the call by the phase 2 readers, and a loop that refuses partway leaves a prefix `mountedNodeIds()` names exactly. `RA-158` through `RA-161` are the tripwire under that decision and ADR-077 records it. Review, exact-head CI, and merge remain gates; no landed change is claimed.","replace":"- **Unmerged value-tier batch:** phase 6 of [#362](https://github.com/chahyasantoso/motion5/issues/362) is the one capability on that issue's list, in [#368](https://github.com/chahyasantoso/motion5/pull/368). `ProjectHandle.values(recipe)` is a second transaction tier beside `edit`, handed a frozen `ValueTransaction` from `contract/`, and it defers the publication rather than the write: `n` writes cost one `invalidate`, one notification per affected node and one sequence move, and the call answers the batch that publication produced while a staged verb answers a deferred one naming its node. `#invalidateOne` keeps its body and `#publishValue` goes in front of it, because four records and two shipped cases name it as the single-node value flush; the reentrancy rung splits into `#refuseValueReentrant` plus the existing one so the value verbs are not refused by the tier that exists for them; `invalidate` is refused inside a batch rather than joined. `apps/ik-playground/src/scroll-reach.ts` becomes one call and loses the comment that said it was not a multi-track transaction. `RA-162` through `RA-169`, recorded in ADR-078. Review, exact-head CI, and merge remain gates; no landed change is claimed."}]}