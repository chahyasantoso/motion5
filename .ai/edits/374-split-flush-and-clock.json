{
  "version": 1,
  "expected_head": "a431894d520fd4024a29e8730d8cd1fcd6c53fed",
  "expected_blobs": {
    "packages/core/src/runtime/graph-runtime.ts": "b0b82424a6ff27cf29237bb867929c23234a9426",
    "packages/core/src/runtime/project-runtime.md": "74747fd32941a15afec3a5cc42ab9f5f67726b9f",
    "packages/core/src/runtime/project-runtime.ts": "090b1cb6ba64342dfef6b44ea2e18a0bbe99d988",
    "packages/core/test/integration/demo-runtime-authoring.test.ts": "9387ab1607eb1e0a89e494da6d0aadd74fbc5c05",
    "packages/core/test/integration/ik-playground-scroll-rest.test.ts": "560592e65ffad592fb0fc70c29e58a9f1c0aa591",
    "packages/core/test/unit/runtime/clock-tick-identity.test.ts": "e253480b5cd26c05bb4811f17cca989058ad4cb2",
    "packages/core/test/unit/runtime/keyframe-property-edit.test.ts": "420e12d43a45ebe132ee440a1072a8f222f9efc8",
    "packages/core/test/unit/runtime/live-value-updates.test.ts": "cdeb88c76978c64a4c6c6fd0f150296e8b995072",
    "packages/core/test/unit/runtime/motion-consistency.test.ts": "ec5b471ea7076220ce58dd03ac771e24a59763ec",
    "packages/core/test/unit/runtime/publisher-reentrancy.test.ts": "ec756167aaa9a8cdc62ba254f11b648f106cd45d",
    "packages/core/test/unit/runtime/scheduler-reentrancy.test.ts": "335f16de774586c2a57cbec517db75aa7501b222"
  },
  "message": "fix(runtime): flush states its seeds, flushAtTick states the clock, invalidate retires",
  "edits": [
    {
      "path": "packages/core/src/runtime/graph-runtime.ts",
      "find": "  /**\n   * Publishes for `seeds`, unioned with whatever a deferred drain left pending, and answers it.\n   *\n   * `seeds` has no default. It defaulted to `[...this.#members]` until issue #371's follow-up, so\n   * an omitted argument asked for a full graph recompute: the most expensive answer this method\n   * has, selected by saying nothing. A default is what an unthinking call gets, so it may not be\n   * the worst case. Every caller states its list, and the one that wants every member says so.\n   *\n   * An empty list is not a cheap flush and is not treated as one. It still derives the snapshot,\n   * moves `#sequence` and notifies every batch subscriber, because `#scheduleDrain` calls\n   * `flush([])` precisely so a deferred drain publishes what `#pendingSeeds` carried. Emptiness is\n   * a project-tier question and `ProjectRuntime.#publishSeeds` owns it. See ADR-081 and ADR-080.\n   */\n  flush(seeds: readonly string[], tick?: number): PatchBatch {\n    this.#assertLive();\n    if (this.#flushing) {\n      for (const seed of seeds) this.#pendingSeeds.add(seed);\n      this.#scheduleDrain();\n      return deferredBatch(this.#sequence, seeds);\n    }\n    if (tick !== undefined) {\n      if (!Number.isFinite(tick)) throw new TypeError(\"Runtime ticks must be finite.\");\n      if (tick < this.#lastTick) throw new RangeError(\"Runtime ticks must be monotonic.\");\n      this.#lastTick = tick;\n    }\n    const carried = [...this.#pendingSeeds];\n",
      "replace": "  /**\n   * Publishes for `seeds`, unioned with whatever a deferred drain left pending, and answers it.\n   *\n   * The seed-list entry, and the whole of it: this verb cannot reach the clock's frame number.\n   * `flush(seeds, tick?)` carried both operations behind one name until issue #374, so which of the\n   * two a caller meant was stated by whether a second argument was present, and the one that\n   * consumes a frame number the clock will reuse was the one reachable by accident. `flushAtTick`\n   * owns that transition now and states it as a required parameter. See ADR-082.\n   *\n   * `seeds` has no default either, since issue #371's follow-up: an omitted argument used to ask\n   * for a full graph recompute, the most expensive answer this class has, selected by saying\n   * nothing. Every caller states its list, and the one that wants every member says so.\n   *\n   * An empty list is not a cheap flush and is not treated as one. It still derives the snapshot,\n   * moves `#sequence` and notifies every batch subscriber, because `#scheduleDrain` calls\n   * `flush([])` precisely so a deferred drain publishes what `#pendingSeeds` carried. Emptiness is\n   * a project-tier question and `ProjectRuntime.#publishSeeds` owns it. See ADR-080.\n   */\n  flush(seeds: readonly string[]): PatchBatch {\n    this.#assertLive();\n    return this.#deferIfFlushing(seeds) ?? this.#flushSeeds(seeds);\n  }\n  /**\n   * Publishes for `seeds` at `tick`, advancing the clock's frame number, and answers that batch.\n   *\n   * The clock's entry, and the only member that may move `#lastTick`. `#flushTick` is its only\n   * caller in `src`, which is what keeps a live edit structurally unable to consume a frame number\n   * the clock will reuse on its next frame: the verb the project tier holds has no such parameter,\n   * and `clock-tick-identity` reads that separation from the other side. The transition is required\n   * rather than optional, so it can never be selected by omission. See ADR-082.\n   *\n   * A reentrant call is answered before the tick is consumed, deliberately and in that order. A\n   * flush requested while subscribers are being notified publishes nothing now, so moving the frame\n   * number for it would record a frame that did not run. `publisher-reentrancy` reads that.\n   */\n  flushAtTick(seeds: readonly string[], tick: number): PatchBatch {\n    this.#assertLive();\n    const deferred = this.#deferIfFlushing(seeds);\n    if (deferred !== undefined) return deferred;\n    this.#advanceTick(tick);\n    return this.#flushSeeds(seeds);\n  }\n  /**\n   * Answers the deferred batch for a flush requested inside one, or `undefined` when none is open.\n   *\n   * The one owner of the reentrancy answer, for both public verbs. Queueing the seeds and scheduling\n   * the drain is the answer rather than a step before it, which is why this hands back the batch and\n   * not a boolean: two callers that had to ask and then act could act differently.\n   */\n  #deferIfFlushing(seeds: readonly string[]): PatchBatch | undefined {\n    if (!this.#flushing) return undefined;\n    for (const seed of seeds) this.#pendingSeeds.add(seed);\n    this.#scheduleDrain();\n    return deferredBatch(this.#sequence, seeds);\n  }\n  /**\n   * Validates `tick` and records it as the frame number this runtime has reached.\n   *\n   * The one owner of tick validity and the one write to `#lastTick`. A non-finite value is a\n   * `TypeError` and a value that does not advance is a `RangeError`, and both refuse before the\n   * field moves, so a rejected tick leaves the runtime on the frame it was already on.\n   */\n  #advanceTick(tick: number): void {\n    if (!Number.isFinite(tick)) throw new TypeError(\"Runtime ticks must be finite.\");\n    if (tick < this.#lastTick) throw new RangeError(\"Runtime ticks must be monotonic.\");\n    this.#lastTick = tick;\n  }\n  /**\n   * Publishes for `seeds` plus whatever a deferred drain left pending, and answers that batch.\n   *\n   * The publication mechanics both public verbs share, and nothing else: no liveness answer, no\n   * reentrancy answer and no clock transition, because each of those has an owner above. Its one\n   * precondition is stated rather than re-asked here, because a second copy of a decided question is\n   * a second owner of it: it must not be called while `#flushing`, which is what `#deferIfFlushing`\n   * answers for both callers. A publisher failure re-queues the seeds this call was carrying and\n   * rethrows, so the work is deferred rather than dropped.\n   */\n  #flushSeeds(seeds: readonly string[]): PatchBatch {\n    const carried = [...this.#pendingSeeds];\n"
    },
    {
      "path": "packages/core/src/runtime/graph-runtime.ts",
      "find": "  /**\n   * Publishes for `seeds` without reaching the clock's frame number.\n   *\n   * The project tier's entry, narrower than `flush` on purpose rather than by accident: it cannot\n   * be handed a `tick`, so a live edit can never consume a frame number the clock will reuse on\n   * its next frame. `clock-tick-identity` reads that separation from the other side. It is also\n   * why issue #371's follow-up proposal to delete this member and let `#invalidateSeeds` reach\n   * `flush` directly is refused: it would widen what the project tier can reach while claiming to\n   * narrow this class's surface. See ADR-081.\n   *\n   * It asserts no liveness of its own. `flush` asserts the same condition as its first statement,\n   * with nothing in between that could change the answer, so the second copy decided nothing.\n   */\n  invalidate(seeds: readonly string[]): PatchBatch {\n    return this.flush(seeds);\n  }\n  dispose(): void {\n",
      "replace": "  dispose(): void {\n"
    },
    {
      "path": "packages/core/src/runtime/graph-runtime.ts",
      "find": "      this.flush([...this.#members], event.tick);\n",
      "replace": "      this.flushAtTick([...this.#members], event.tick);\n"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #invalidateSeeds(nodeIds: readonly string[]): PatchBatch {\n    const batch = this.#graph.invalidate(nodeIds);\n",
      "replace": "  #invalidateSeeds(nodeIds: readonly string[]): PatchBatch {\n    const batch = this.#graph.flush(nodeIds);\n"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "The one place a seed list becomes a published, recorded batch: invalidate the graph, record the returned diagnostics, answer the batch.",
      "replace": "The one place a seed list becomes a published, recorded batch: flush the graph, record the returned diagnostics, answer the batch. It reaches GraphRuntime.flush rather than GraphRuntime.invalidate since issue #374, which deleted the second name: that member was flush with the clock's tick parameter removed, and flush no longer has one, so the two spellings had become one operation under two names."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "so a fold would make a public verb's return optional and hand its callers a second failure contract rather than remove a duplicate. See ADR-079 and ADR-080.",
      "replace": "so a fold would make a public verb's return optional and hand its callers a second failure contract rather than remove a duplicate. See ADR-079, ADR-080 and ADR-082."
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    const patch = runtime.graph.registry.get(ARM);\n    const invalidate = vi.spyOn(runtime.graph, \"invalidate\");\n",
      "replace": "    const patch = runtime.graph.registry.get(ARM);\n    const invalidate = vi.spyOn(runtime.graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    const failure = new Error(\"build refused\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const failure = new Error(\"build refused\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    const failure = new Error(\"displaced cleanup failed\");\n    const killed = vi.spyOn(timeline, \"kill\").mockImplementationOnce(() => {\n      kill();\n      throw failure;\n    });\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const failure = new Error(\"displaced cleanup failed\");\n    const killed = vi.spyOn(timeline, \"kill\").mockImplementationOnce(() => {\n      kill();\n      throw failure;\n    });\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    const invalidate = vi.spyOn(runtime.graph, \"invalidate\");\n    const arm = runtime.track(ARM);\n",
      "replace": "    const invalidate = vi.spyOn(runtime.graph, \"flush\");\n    const arm = runtime.track(ARM);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    const track = handle.track(ARM);\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const track = handle.track(ARM);\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    const published = values(handle, ARM);\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const published = values(handle, ARM);\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    const invalidate = vi.spyOn(rig.runtime.graph, \"invalidate\");\n",
      "replace": "    const invalidate = vi.spyOn(rig.runtime.graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    expect(write).not.toContain(\"this.#graph.invalidate(\");\n",
      "replace": "    expect(write).not.toContain(\"this.#graph.flush(\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    expect(source.split(\"this.#graph.invalidate(\")).toHaveLength(2);\n",
      "replace": "    expect(source.split(\"this.#graph.flush(\")).toHaveLength(2);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "    expect(owner).toContain(\"this.#graph.invalidate(nodeIds)\");\n",
      "replace": "    expect(owner).toContain(\"this.#graph.flush(nodeIds)\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/keyframe-property-edit.test.ts",
      "find": "    const killed = vi.spyOn(timeline, \"kill\").mockImplementationOnce(() => {\n      kill();\n      throw failure;\n    });\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const killed = vi.spyOn(timeline, \"kill\").mockImplementationOnce(() => {\n      kill();\n      throw failure;\n    });\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/keyframe-property-edit.test.ts",
      "find": "    const failure = new Error(\"new leaf refused to build\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const failure = new Error(\"new leaf refused to build\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/keyframe-property-edit.test.ts",
      "find": "    const arm = declaring(handle, ARM);\n    const replaceGraph = vi.spyOn(runtimeOf(handle).graph, \"replaceGraph\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const arm = declaring(handle, ARM);\n    const replaceGraph = vi.spyOn(runtimeOf(handle).graph, \"replaceGraph\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/keyframe-property-edit.test.ts",
      "find": "    const published = values(handle, ARM);\n    const replaceGraph = vi.spyOn(runtimeOf(handle).graph, \"replaceGraph\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const published = values(handle, ARM);\n    const replaceGraph = vi.spyOn(runtimeOf(handle).graph, \"replaceGraph\");\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/keyframe-property-edit.test.ts",
      "find": "    const before = arm.definition;\n    const published = values(handle, ARM);\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"invalidate\");\n",
      "replace": "    const before = arm.definition;\n    const published = values(handle, ARM);\n    const invalidate = vi.spyOn(runtimeOf(handle).graph, \"flush\");\n"
    },
    {
      "path": "packages/core/test/unit/runtime/motion-consistency.test.ts",
      "find": "    };\n    const invalidate = vi.spyOn(test.runtime.graph, \"invalidate\");\n    expect(caught(() => motion.setStagger(250))).toBe(failure);\n",
      "replace": "    };\n    const invalidate = vi.spyOn(test.runtime.graph, \"flush\");\n    expect(caught(() => motion.setStagger(250))).toBe(failure);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/motion-consistency.test.ts",
      "find": "    intercept = disposeDuringProgress;\n    const invalidate = vi.spyOn(test.runtime.graph, \"invalidate\");\n    const result = caught(() => motion.setStagger(250));\n",
      "replace": "    intercept = disposeDuringProgress;\n    const invalidate = vi.spyOn(test.runtime.graph, \"flush\");\n    const result = caught(() => motion.setStagger(250));\n"
    },
    {
      "path": "packages/core/test/integration/demo-runtime-authoring.test.ts",
      "find": "      const replace = vi.spyOn(runtime.graph, \"replaceGraph\");\n      const invalidate = vi.spyOn(runtime.graph, \"invalidate\");\n      for (let cycle = 0; cycle < 2; cycle++) {\n",
      "replace": "      const replace = vi.spyOn(runtime.graph, \"replaceGraph\");\n      const invalidate = vi.spyOn(runtime.graph, \"flush\");\n      for (let cycle = 0; cycle < 2; cycle++) {\n"
    },
    {
      "path": "packages/core/test/integration/demo-runtime-authoring.test.ts",
      "find": "      const replace = vi.spyOn(actual.runtime.graph, \"replaceGraph\");\n      const invalidate = vi.spyOn(actual.runtime.graph, \"invalidate\");\n      const track = actual.handle.track(nodeId(rig.goalTrack));\n",
      "replace": "      const replace = vi.spyOn(actual.runtime.graph, \"replaceGraph\");\n      const invalidate = vi.spyOn(actual.runtime.graph, \"flush\");\n      const track = actual.handle.track(nodeId(rig.goalTrack));\n"
    },
    {
      "path": "packages/core/test/integration/ik-playground-scroll-rest.test.ts",
      "find": "      expect(replace).not.toHaveBeenCalled();\n      const invalidate = vi.spyOn(runtime.graph, \"invalidate\");\n      controller.commit();\n",
      "replace": "      expect(replace).not.toHaveBeenCalled();\n      const invalidate = vi.spyOn(runtime.graph, \"flush\");\n      controller.commit();\n"
    },
    {
      "path": "packages/core/test/unit/runtime/scheduler-reentrancy.test.ts",
      "find": "      if (notifications === 1) runtime.invalidate([\"caption/label\"]);\n",
      "replace": "      if (notifications === 1) runtime.flush([\"caption/label\"]);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/scheduler-reentrancy.test.ts",
      "find": "      runtime.invalidate([\"caption/label\"]);\n      runtime.invalidate([\"caption/label\"]);\n      runtime.invalidate([\"hero/arm\"]);\n",
      "replace": "      runtime.flush([\"caption/label\"]);\n      runtime.flush([\"caption/label\"]);\n      runtime.flush([\"hero/arm\"]);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/scheduler-reentrancy.test.ts",
      "find": "    runtime.registry.subscribeNode(\"hero/arm\", () => {\n      runtime.invalidate([\"caption/label\"]);\n      runtime.invalidate([\"caption/label\"]);\n    });\n",
      "replace": "    runtime.registry.subscribeNode(\"hero/arm\", () => {\n      runtime.flush([\"caption/label\"]);\n      runtime.flush([\"caption/label\"]);\n    });\n"
    },
    {
      "path": "packages/core/test/unit/runtime/scheduler-reentrancy.test.ts",
      "find": "    runtime.flush([\"hero/arm\"], 1);\n",
      "replace": "    runtime.flushAtTick([\"hero/arm\"], 1);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/publisher-reentrancy.test.ts",
      "find": "      if (notifications === 1) deferred = runtime.invalidate([\"caption/label\"]);\n",
      "replace": "      if (notifications === 1) deferred = runtime.flush([\"caption/label\"]);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/publisher-reentrancy.test.ts",
      "find": "      if (notifications === 1) runtime.invalidate([\"caption/label\"]);\n",
      "replace": "      if (notifications === 1) runtime.flush([\"caption/label\"]);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/publisher-reentrancy.test.ts",
      "find": "      runtime.invalidate([\"caption/label\"]);\n      runtime.invalidate([\"caption/label\"]);\n      runtime.invalidate([\"hero/arm\"]);\n",
      "replace": "      runtime.flush([\"caption/label\"]);\n      runtime.flush([\"caption/label\"]);\n      runtime.flush([\"hero/arm\"]);\n"
    },
    {
      "path": "packages/core/test/unit/runtime/clock-tick-identity.test.ts",
      "find": "    runtime.invalidate([\"hero/arm\"]);\n",
      "replace": "    runtime.flush([\"hero/arm\"]);\n"
    }
  ]
}
