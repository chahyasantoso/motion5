{
  "version": 1,
  "expected_head": "fc67615f7fd488d82e9ebdfb1f1285dc75540aee",
  "expected_blobs": {
    "packages/core/src/graph/ir.ts": "dcb56422f23032af12df75309c176bfed508217f",
    "packages/core/src/domain/plugins.ts": "b2d4730794d601520290c6f805a0eb2b0165edf9",
    "packages/core/src/domain/keyframe-compiler.ts": "f9f00f1faf2a4cd7def36b2da398add304965761",
    "packages/core/test/contract/ids-ownership-at-the-producer.test.ts": "5e69154383466507eedb7bc4249a506b6005030e"
  },
  "message": "refactor(core): collapse three pure-forward diagnostic producers into aliases of the one constructor",
  "edits": [
    {
      "path": "packages/core/src/graph/ir.ts",
      "find": "import { diagnostic } from \"../contract/diagnostics\";",
      "replace": "import { diagnostic as diag } from \"../contract/diagnostics\";"
    },
    {
      "path": "packages/core/src/graph/ir.ts",
      "find": "import type { OwnedIds } from \"../contract/rule\";\n",
      "replace": ""
    },
    {
      "path": "packages/core/src/graph/ir.ts",
      "find": "import type { RuleId } from \"../contract/rule-id\";\n",
      "replace": ""
    },
    {
      "path": "packages/core/src/graph/ir.ts",
      "find": "/**\n * The graph layer's spelling of the one constructor, and no longer a second owner of the shape.\n *\n * It kept a body of its own for as long as `Diagnostic` was a union: the object it returned named a\n * severity for itself and chose between a payload and no member at all through a conditional spread,\n * which made this file the second place both answers lived. `contract/rule.ts` owns them now, so the\n * body is a forward and the only thing this declaration still owns is its name. Twenty seven call\n * sites read `diag`, and renaming them would be churn with no invariant behind it.\n *\n * No severity is declined here, because there is no parameter left to decline: the constructor reads\n * every rule's answer from `contract/rule.ts`, so no expression in this file names a severity and none\n * can. The ids list is forwarded rather than widened for the same reason: `OwnedIds` is the\n * constructor's own argument list, so the correlation this file used to re-state is the one its call\n * sites are already checked against.\n *\n * This declaration and the constructor now take the same arguments in the same order, which retires\n * the trap that made aliasing one to the other hand an array to a severity. The declaration is kept\n * rather than collapsed into an alias anyway, because `ids-ownership-at-the-producer.test.ts` reads it\n * as source text and an alias has neither the declaration nor the derived list it counts. See\n * ADR-097.\n */\nexport function diag<Rule extends RuleId>(\n  ruleId: Rule,\n  path: string,\n  message: string,\n  ...carried: OwnedIds<Rule>\n): Diagnostic {\n  return diagnostic(ruleId, path, message, ...carried);\n}",
      "replace": "// The graph layer's spelling of the one constructor, and now the alias it always was. Twenty seven\n// call sites here and four in the incremental builder read `diag`, so the name is kept and the\n// declaration is not: a forwarding body restated the constructor's whole signature to change nothing\n// about it, because severity was already the rule's own answer and `OwnedIds` was already the\n// constructor's own argument list referenced rather than redefined. `contract/validate-v5.ts` binds\n// the same constructor as `issue` exactly this way.\nexport { diag };"
    },
    {
      "path": "packages/core/src/domain/plugins.ts",
      "find": "import { diagnostic as buildDiagnostic } from \"../contract/diagnostics\";",
      "replace": "import { diagnostic } from \"../contract/diagnostics\";"
    },
    {
      "path": "packages/core/src/domain/plugins.ts",
      "find": "import type { OwnedIds } from \"../contract/rule\";\n",
      "replace": ""
    },
    {
      "path": "packages/core/src/domain/plugins.ts",
      "find": "import type { RuleId } from \"../contract/rule-id\";\n",
      "replace": ""
    },
    {
      "path": "packages/core/src/domain/plugins.ts",
      "find": "function diagnostic<Rule extends RuleId>(\n  ruleId: Rule,\n  path: string,\n  message: string,\n  ...carried: OwnedIds<Rule>\n): Diagnostic {\n  return buildDiagnostic(ruleId, path, message, ...carried);\n}\n",
      "replace": ""
    },
    {
      "path": "packages/core/src/domain/keyframe-compiler.ts",
      "find": "import { diagnostic as buildDiagnostic } from \"../contract/diagnostics\";",
      "replace": "import { diagnostic } from \"../contract/diagnostics\";"
    },
    {
      "path": "packages/core/src/domain/keyframe-compiler.ts",
      "find": "import type { OwnedIds } from \"../contract/rule\";\n",
      "replace": ""
    },
    {
      "path": "packages/core/src/domain/keyframe-compiler.ts",
      "find": "import type { RuleId } from \"../contract/rule-id\";\n",
      "replace": ""
    },
    {
      "path": "packages/core/src/domain/keyframe-compiler.ts",
      "find": "function diagnostic<Rule extends RuleId>(\n  ruleId: Rule,\n  path: string,\n  message: string,\n  ...carried: OwnedIds<Rule>\n): Diagnostic {\n  // This layer's spelling of the one constructor, and no longer a second owner of the shape. It kept a\n  // body while `Diagnostic` was a union: the object it returned named a severity for itself and chose\n  // between a payload and no member at all through a conditional spread. Both answers belong to the\n  // rule now, so the body is a forward and the only thing this declaration still owns is its name.\n  // No severity is named here and none can be: the constructor has no such parameter. Every rule this\n  // file reports is an error rule that always names ids, so nothing observable moves.\n  return buildDiagnostic(ruleId, path, message, ...carried);\n}\n",
      "replace": ""
    },
    {
      "path": "packages/core/test/contract/ids-ownership-at-the-producer.test.ts",
      "find": "const PRODUCERS: readonly (readonly [path: string, declaration: string])[] = [\n  [\"contract/diagnostics.ts\", \"export function diagnostic<Rule extends RuleId>(\"],\n  [\"graph/ir.ts\", \"export function diag<Rule extends RuleId>(\"],\n  [\"runtime/report.ts\", \"export function reportDiagnostic<Rule extends RuleId>(\"],\n  [\"domain/plugins.ts\", \"function diagnostic<Rule extends RuleId>(\"],\n  [\"domain/keyframe-compiler.ts\", \"function diagnostic<Rule extends RuleId>(\"],\n];",
      "replace": "const PRODUCERS: readonly (readonly [path: string, declaration: string])[] = [\n  [\"contract/diagnostics.ts\", \"export function diagnostic<Rule extends RuleId>(\"],\n  [\"runtime/report.ts\", \"export function reportDiagnostic<Rule extends RuleId>(\"],\n];"
    },
    {
      "path": "packages/core/test/contract/ids-ownership-at-the-producer.test.ts",
      "find": " * sites were always the half that mattered, and the five producers below take their ids as the\n * argument list `contract/rule.ts` derives from the rule they were handed. None of them names a\n * severity either, because the one constructor has no parameter for one.\n",
      "replace": " * sites were always the half that mattered, and the two producers below take their ids as the\n * argument list `contract/rule.ts` derives from the rule they were handed. Neither names a severity\n * either, because the one constructor has no parameter for one.\n *\n * Two rather than five, and the three that went were not weakened away. `graph/ir.ts`'s `diag` and\n * the private `diagnostic` in `domain/plugins.ts` and `domain/keyframe-compiler.ts` each declared the\n * constructor's whole signature and then forwarded to it unchanged, so what this suite read in those\n * three files was a restatement rather than a second mechanism. `OwnedIds<Rule>` is referenced from\n * `contract/rule.ts` and never redefined, so an alias of the constructor resolves to the same type\n * and stops compiling in the same commit the registry's shape moves in: the restatement bought reader\n * locality and no guarantee, and three duplicate declarations plus three cases is a steep price for\n * reader locality. All three are import aliases of the one constructor now, which is the form\n * `contract/validate-v5.ts` already binds it in as `issue`. What is left below is the constructor and\n * the one producer that is not a forward.\n"
    },
    {
      "path": "packages/core/test/contract/ids-ownership-at-the-producer.test.ts",
      "find": " * assertion rather than on a compile. It is spelled once per declaration rather than once across the\n * five, because a span has no bound that belongs to it and a rename in one file would otherwise be\n * reported as a claim about another.",
      "replace": " * assertion rather than on a compile. It is spelled once per declaration rather than once across the\n * two, because a span has no bound that belongs to it and a rename in one file would otherwise be\n * reported as a claim about another."
    },
    {
      "path": "packages/core/test/contract/ids-ownership-at-the-producer.test.ts",
      "find": " * every payload would satisfy the refusing half on its own. Only the three exported producers are\n * reachable from a test; the two private ones in `domain/` carry the same declaration and are covered\n * by the text cases above.",
      "replace": " * every payload would satisfy the refusing half on its own. All three spellings reached below are\n * exported, and `diag` is an alias of the constructor rather than a declaration of its own, so a\n * probe through it asks the compiler about the same signature the other two do rather than about a\n * second one that happens to agree with it."
    }
  ]
}
