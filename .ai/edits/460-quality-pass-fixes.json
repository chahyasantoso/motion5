{
  "version": 1,
  "expected_head": "d0ad561a9b754206102b385109d79eaa7ca67726",
  "expected_blobs": {
    "docs/SESSION-STATUS.md": "355259664355e843d834c8967ad1d4cea51aab19",
    "docs/guide/rendering-patches.md": "d2ed17b1a3b6654f7cee96b09270f1be57063f5d",
    "docs/guide/runtime-changes.md": "0635a9405adf2da14f4c654518da69acfd16692e",
    "docs/ADR-042-internal-key-enforcement.md": "3546500d9cb4e3ba5270f1cd8f0130b8b5e3ebd8"
  },
  "message": "docs(460): correct the eviction wording, the entry attribution and a stale ADR-042 sentence",
  "edits": [
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "Three React test fakes retained a destroyed patch their own `get` could answer and now drop the entry as the registry does, which was a green suite over a contract defect rather than a retyping. ",
      "replace": ""
    },
    {
      "path": "docs/guide/rendering-patches.md",
      "find": "`Patch`, `LivePatch`, `PatchStatus`, `PatchBatch` and `PatchListener` are the exported names, and `PatchStatus` is read off the union rather than spelled beside it, so it cannot name a status that no variant declares.",
      "replace": "`Patch`, `LivePatch`, `PatchStatus`, `PatchBatch` and `PatchListener` are the names `@motion5/core` exports, and `PatchStatus` is read off the union rather than spelled beside it, so it cannot name a status that no variant declares. `@motion5/react` re-exports `Patch`, `LivePatch` and `PatchListener` and not the other two, because a component types the wire it observes rather than the flush that produced it."
    },
    {
      "path": "docs/guide/runtime-changes.md",
      "find": "An evicted node publishes exactly one `destroyed` patch before its retained patch is dropped, so an already-attached subscriber cannot keep rendering a node the graph has removed.",
      "replace": "An evicted node that has a retained publication delivers exactly one `destroyed` patch to its subscribers, and its entry is dropped before that delivery rather than after, so an already-attached subscriber cannot keep rendering a node the graph has removed and `get` can never hand the terminal patch back. Evicting a node that never published is silent, because there is no state for a terminal patch to contradict."
    },
    {
      "path": "docs/ADR-042-internal-key-enforcement.md",
      "find": "`createDomPatchAdapter` would pass while React kept leaking.",
      "replace": "`createDomPatchAdapter` would pass while React kept leaking. **Refined by ADR-098, 2026-09-18.** The\nsentence saying `patch-store.ts` returns the whole `Patch`, values included, stopped being true when\n`Patch` became a union discriminated by `status`. The store answers `LivePatch | undefined` and\n`usePatch` returns that, so a terminal publication reaches a component as absence, and only a\n`ready` patch owns `values` for it to hand on. The argument this paragraph makes is untouched,\nbecause two renderers still receive the observation result and neither may reimplement internal-key\nfiltering. See [ADR-098](./ADR-098-a-patch-carries-only-the-payload-its-status-owns.md)."
    }
  ]
}
