{
  "version": 1,
  "operation": "apply",
  "expected_head": "23a19c52ccee8e7be436dfc003a16f5103ac3f9e",
  "expected_blobs": {
    "packages/core/src/runtime/project-runtime.ts": "abdf8591ddb981208e086b58e3dac47d04383af8",
    "packages/core/src/runtime/project-runtime.md": "78480c3e696766680c36ebc7ccbad7e666dd162a",
    "packages/core/src/runtime/value-batch.ts": "bfa24ef42cc21b10cf50b4c1cba7292a8ddb117a",
    "docs/SESSION-STATUS.md": "cf5531b6d977501343c46efdb15eb1d8658e9a6b",
    "docs/ADR-079-one-publication-owner.md": null
  },
  "message": "fix(runtime): one owner for a published seed list, and a return that means one thing",
  "edits": [
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #refuseValueReentrant(verb: string): void {\n    if (this.#open !== undefined) immediateInTransaction(verb);\n    if (this.#inFlight > 0) commitInFlight();\n  }\n\n  #refuseReentrant(verb: string): void {\n    this.#refuseValueReentrant(verb);",
      "replace": "  #refuseImmediateReentrant(verb: string): void {\n    if (this.#open !== undefined) immediateInTransaction(verb);\n    if (this.#inFlight > 0) commitInFlight();\n  }\n\n  #refuseReentrant(verb: string): void {\n    this.#refuseImmediateReentrant(verb);"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#refuseValueReentrant(rebase ? \"setValues\" : \"overrideValues\");",
      "replace": "    this.#refuseImmediateReentrant(rebase ? \"setValues\" : \"overrideValues\");"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#refuseValueReentrant(\"setKeyframe\");",
      "replace": "    this.#refuseImmediateReentrant(\"setKeyframe\");"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#refuseValueReentrant(\"removeKeyframe\");",
      "replace": "    this.#refuseImmediateReentrant(\"removeKeyframe\");"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#refuseValueReentrant(\"seek\");",
      "replace": "    this.#refuseImmediateReentrant(\"seek\");"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #flush(touched: readonly string[]): PatchBatch | undefined {\n    if (this.#disposed || touched.length === 0) return undefined;\n    const batch = this.#graph.invalidate(touched);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }",
      "replace": "  #flush(touched: readonly string[]): void {\n    if (this.#disposed) return;\n    this.#publishSeeds(touched);\n  }\n\n  #publishSeeds(seeds: readonly string[]): PatchBatch {\n    this.#assertLive();\n    if (seeds.length === 0) return emptyValueBatch(this.#graph.sequence);\n    return this.#invalidateSeeds(seeds);\n  }\n\n  #invalidateSeeds(nodeIds: readonly string[]): PatchBatch {\n    const batch = this.#graph.invalidate(nodeIds);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #invalidateOne(nodeId: string) {\n    this.#assertLive();\n    const batch = this.#graph.invalidate([nodeId]);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }",
      "replace": "  #invalidateOne(nodeId: string) {\n    this.#assertLive();\n    return this.#invalidateSeeds([nodeId]);\n  }"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    return this.#boundary(() => this.#flush(seeds) ?? emptyValueBatch(this.#graph.sequence));",
      "replace": "    return this.#boundary(() => this.#publishSeeds(seeds));"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#refuseReentrant(\"invalidate\");\n    const batch = this.#graph.invalidate(nodeIds);\n    this.#diagnostics.recordAll(batch.diagnostics);\n    return batch;\n  }",
      "replace": "    this.#refuseReentrant(\"invalidate\");\n    return this.#invalidateSeeds(nodeIds);\n  }"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "        this.#tracks.clear();\n        this.#motions.clear();\n        this.#open = undefined;\n      },",
      "replace": "        this.#tracks.clear();\n        this.#motions.clear();\n        this.#open = undefined;\n        this.#valueSeeds = undefined;\n      },"
    },
    {
      "path": "packages/core/src/runtime/value-batch.ts",
      "find": " * publication happened in between. See ADR-078.\n */",
      "replace": " * publication happened in between.\n *\n * Annotated rather than cast, here and in the empty factory below. Both used to end in\n * `as PatchBatch`, which opted the two functions whose entire job is producing a correctly shaped\n * batch out of the one check that they do it: a missing or misspelled member would have compiled.\n * `PatchBatch` is four readonly members and a frozen literal satisfies it unaided, so the answer to\n * the review's open question is that nothing needed fixing at the type level and the cast is simply\n * deleted. See ADR-078 and ADR-079.\n */"
    },
    {
      "path": "packages/core/src/runtime/value-batch.ts",
      "find": "  return Object.freeze({\n    tick: sequence,\n    seeds: ids,\n    patches: Object.freeze([]),\n    diagnostics: Object.freeze([diagnostic]),\n  }) as PatchBatch;",
      "replace": "  const batch: PatchBatch = {\n    tick: sequence,\n    seeds: ids,\n    patches: Object.freeze([]),\n    diagnostics: Object.freeze([diagnostic]),\n  };\n  return Object.freeze(batch);"
    },
    {
      "path": "packages/core/src/runtime/value-batch.ts",
      "find": "  return Object.freeze({\n    tick: sequence,\n    seeds: Object.freeze([]),\n    patches: Object.freeze([]),\n    diagnostics: Object.freeze([]),\n  }) as PatchBatch;",
      "replace": "  const batch: PatchBatch = {\n    tick: sequence,\n    seeds: Object.freeze([]),\n    patches: Object.freeze([]),\n    diagnostics: Object.freeze([]),\n  };\n  return Object.freeze(batch);"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "values clears it in finally before re-asking liveness and publishing, so a recipe that threw leaves no batch open and the next call is an ordinary one rather than a reentrancy refusal against a batch nothing closed.",
      "replace": "values clears it in finally before re-asking liveness and publishing, so a recipe that threw leaves no batch open and the next call is an ordinary one rather than a reentrancy refusal against a batch nothing closed. #teardown clears it beside #open, for the reason it clears the retained maps: both halves of open transaction state belong to the member that claims that concept, and leaving one of them to another code path made the claim half true."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "## #refuseValueReentrant\n\nThe lower rung of a two-step ladder, and the two conditions the immediate family has always asked, in the order it has always asked them: an open recipe as schema-transaction-immediate naming the verb, then an in-flight boundary as schema-commit-reentrant. The four value verbs and seek ask this one and stop here, because staging their publication is what a value batch is for.",
      "replace": "## #refuseImmediateReentrant\n\nThe lower rung of a two-step ladder, and the two conditions the immediate family has always asked, in the order it has always asked them: an open recipe as schema-transaction-immediate naming the verb, then an in-flight boundary as schema-commit-reentrant. The four value verbs and seek ask this one and stop here, because staging their publication is what a value batch is for.\n\nNamed for the family that asks it rather than for the tier that stops at it. Its first spelling read as a refusal of value-verb reentrancy, which is the opposite of what it does: these are the two conditions a value verb must pass in order to be allowed to join a batch, and both of them are about the immediate family every verb in this class belongs to, which is the vocabulary immediateInTransaction and the immediate-verb refusal cases already use. A rung whose name describes the caller it refuses rather than the callers it serves is a rung the next slice adds a condition to for the wrong reason. See ADR-079."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "and the upper rung of that ladder: it asks #refuseValueReentrant first, then refuses an open value batch with value-batch-immediate naming the verb.",
      "replace": "and the upper rung of that ladder: it asks #refuseImmediateReentrant first, then refuses an open value batch with value-batch-immediate naming the verb."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "Settlement mounts via #mountNode; structural and stagger publication use #flush; value publication uses #invalidateOne.",
      "replace": "Settlement mounts via #mountNode; structural and stagger publication use #flush; value publication uses #invalidateOne or #publishValue, and every one of those ends at #invalidateSeeds."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "One internal publication attempt for a supplied seed list. Skips a disposed runtime or empty list, otherwise invalidates the graph and records returned diagnostics. It has no error boundary: its owning completion collector preserves any synchronous exception with preceding failures. Unlike #invalidateOne it skips disposal instead of asserting. It answers the batch it published, or nothing when it published none, which is what lets values report an empty recipe from this one owner of the empty-seed decision rather than restating it; the three callers that own no answer ignore the return. Structural commits, accepted stagger changes and the value batch share this mechanism, not their topology semantics. See ADR-064, ADR-069, ADR-071 and ADR-078.",
      "replace": "One internal publication attempt for a supplied seed list, and the owner of exactly one decision: a disposed runtime is skipped rather than reported. Unlike #invalidateOne it skips disposal instead of asserting, which is the split ADR-071 decided and this member keeps. Everything else it used to decide now belongs to #publishSeeds, which it delegates to. It answers nothing, and that is the point: it used to answer undefined for a disposed runtime and for an empty seed list alike, so its one caller that read the answer collapsed two conditions into one empty batch and was correct only because liveness was asserted immediately before it at every call site. Safe by statement ordering is not safe by construction, and a sentinel that carries two meanings is deleted rather than documented. It has no error boundary: its owning completion collector preserves any synchronous exception with preceding failures. Structural commits, accepted stagger changes and the value batch share this mechanism, not their topology semantics. See ADR-064, ADR-069, ADR-071, ADR-078 and ADR-079.\n\n## #publishSeeds\n\nThe one owner of what an empty seed list means, and the member values publishes through. A list that names nothing is not a publication, because even an empty batch opens one, notifies every batch subscriber and advances sequence, so it answers the empty batch rather than making one. A list that names anything goes to #invalidateSeeds. Seeds are not deduplicated here; GraphRuntime.flush already does that and a second owner of it would be a second answer.\n\nIt asserts runtime liveness itself rather than trusting its caller to have asked, which is what makes its answer unambiguous by construction: every batch it hands back is one it published or one that had nothing to publish, and a disposed project reaches neither. values asks liveness too, after its recipe and for a different reason, and both calls are load-bearing rather than one of them redundant: that one is the re-ask a callback invalidated, this one is the precondition of the answer, and two side-effect-free field reads cannot disagree. #flush answers its own condition before delegating, so its skip never reaches this assertion. See ADR-079, ADR-064 and ADR-071.\n\n## #invalidateSeeds\n\nThe one place a seed list becomes a published, recorded batch: invalidate the graph, record the returned diagnostics, answer the batch. Three members carried that pair as three copies, #flush, #invalidateOne and public invalidate, and the third had been left standing by reasoning inherited from the second rather than by evidence of its own.\n\nEvery caller keeps its own preconditions and its own failure contract, which is the merge ADR-071 refused and is not this one: #flush skips a disposed runtime, #invalidateOne reports one, public invalidate additionally refuses reentrancy, and #publishSeeds decides emptiness. What none of them owns any more is the publication mechanics. Public invalidate is deliberately not folded into #flush: a caller may pass an empty list and still be answered the batch that publication produced, so folding it would change a public answer and make a public verb's return optional, which is a second failure contract rather than a deduplication. See ADR-079."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "Its body is unchanged by the value batch, deliberately. Four records and two shipped cases name this member as the published single-node value flush, so the batch decision goes in front of it in #publishValue rather than moving these two lines one member down, which would have turned two green cases red for no behavioural reason. It is still the only place a single-node value publication happens; it is no longer the only ending a value write can have. See ADR-064, ADR-069, ADR-070 and ADR-078.",
      "replace": "Still the only place a single-node value publication happens, still not the only ending a value write can have, and no longer a member that names the graph call: it asserts liveness and delegates the pair to #invalidateSeeds. ADR-078 kept the body on the measurement that two shipped cases read this declaration; remeasured, one does. PK-17 reads #writeValues, for its validation ordering rather than for any flush, so the cost of the move was one re-addressed case rather than two, and a third copy of the pair was found afterwards, which turns a tidy-up into an ownership defect. What the four records name is the assertion and the single-node answer, and both are unchanged. See ADR-064, ADR-069, ADR-070, ADR-078 and ADR-079."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "clear native instances and retained maps/open transaction state, then dispose graph and composition as separate steps.",
      "replace": "clear native instances, retained maps and both halves of open transaction state, a recipe's pending pair and a value batch's seed list, then dispose graph and composition as separate steps."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Captured:** 2026-09-13, Asia/Jakarta.",
      "replace": "- **Captured:** 2026-09-14, Asia/Jakarta."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Inspected main:** `8ea61a3f2a77639be174c3fb1eafdf3b2e437976`, after the membership measurement [#367](https://github.com/chahyasantoso/motion5/pull/367). Exact CI and operation evidence belong to their PRs.",
      "replace": "- **Inspected main:** `6674feb59ea8cc8a543ab6f35cf7cbf2f9b96c47`, after the value-tier batch [#368](https://github.com/chahyasantoso/motion5/pull/368). Exact CI and operation evidence belong to their PRs."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "## Now\n\n- **Runtime surface work landed, phases 2 through 5 of",
      "replace": "## Now\n\n- **The value tier has its transaction, and [#362](https://github.com/chahyasantoso/motion5/issues/362) is closed:** [#368](https://github.com/chahyasantoso/motion5/pull/368) is merged. `ProjectHandle.values(recipe)` is a second tier beside `edit`, handed a frozen `ValueTransaction` from `contract/`, and it defers the publication rather than the write: `n` writes cost one `invalidate`, one notification per affected node and one sequence move, a staged verb answers a deferred batch naming its own node, and `apps/ik-playground/src/scroll-reach.ts` is one call that no longer says it is not a multi-track transaction. `RA-162` through `RA-169`, recorded in ADR-078. All seven phases of that issue have an outcome on main, and its two remainders are owned elsewhere: the `CITATION_PENDING` ratchet, and [#369](https://github.com/chahyasantoso/motion5/issues/369).\n- **Runtime surface work landed, phases 2 through 5 of"
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Unmerged value-tier batch:** phase 6 of [#362](https://github.com/chahyasantoso/motion5/issues/362) is the one capability on that issue's list, in [#368](https://github.com/chahyasantoso/motion5/pull/368). `ProjectHandle.values(recipe)` is a second transaction tier beside `edit`, handed a frozen `ValueTransaction` from `contract/`, and it defers the publication rather than the write: `n` writes cost one `invalidate`, one notification per affected node and one sequence move, and the call answers the batch that publication produced while a staged verb answers a deferred one naming its node. `#invalidateOne` keeps its body and `#publishValue` goes in front of it, because four records and two shipped cases name it as the single-node value flush; the reentrancy rung splits into `#refuseValueReentrant` plus the existing one so the value verbs are not refused by the tier that exists for them; `invalidate` is refused inside a batch rather than joined. `apps/ik-playground/src/scroll-reach.ts` becomes one call and loses the comment that said it was not a multi-track transaction. `RA-162` through `RA-169`, recorded in ADR-078. Review, exact-head CI, and merge remain gates; no landed change is claimed.",
      "replace": "- **Unmerged batch follow-ups:** [#370](https://github.com/chahyasantoso/motion5/pull/370) implements the five findings [#369](https://github.com/chahyasantoso/motion5/issues/369) deferred from #368's review. `#invalidateSeeds` becomes the one place a seed list becomes a published, recorded batch where three copies stood, `#flush` answers nothing rather than an `undefined` that meant a disposed runtime and an empty list alike, `#publishSeeds` owns what an empty list means and asserts its own precondition, `#refuseValueReentrant` is renamed `#refuseImmediateReentrant`, `#teardown` clears the batch's seed list beside the recipe pair, both value-batch factories are compiler-checked rather than cast, and `RA-170` covers `setKeyframe`'s recompile ending inside a batch, which is the path the shipped consumer takes and the one no run had driven. Public `invalidate` is deliberately not folded into `#flush`. No public surface moves. ADR-079 records it and amends ADR-078's first decision. Review, exact-head CI, and merge remain gates; no landed change is claimed."
    },
    {
      "path": "docs/ADR-079-one-publication-owner.md",
      "create": "# ADR-079: One publication owner, and a return value that means one thing\n\n**Status:** Accepted, 2026-09-14\n\n**Context.** [Issue #369](https://github.com/chahyasantoso/motion5/issues/369) collected the findings deferred from the review of [#368](https://github.com/chahyasantoso/motion5/pull/368), the value-tier batch. None was incorrect at the reviewed head and all were commentary on a design the review otherwise endorsed, which is why they were left out of a pull request that was already fixing red CI. Recorded together because four of the six are one question asked from four directions, and the question is who owns a publication.\n\nThe reviewed head had three copies of the same two statements: invalidate a seed list on the graph, then record the diagnostics the returned batch carries. `#flush` had them, `#invalidateOne` had them, and public `invalidate` had them. ADR-078 examined the first two and decided to keep them apart. It did not examine the third, which was left standing by reasoning inherited from the second. The reviewed head also had one member answering a value that meant two different things, and a rung whose name described the opposite of what it did.\n\n**Decision.** Four things, and the first of them reverses a decision ADR-078 made three weeks of commits ago rather than quietly diverging from it.\n\n**One: `#invalidateSeeds(nodeIds)` is the one place a seed list becomes a published, recorded batch.** `#flush`, `#invalidateOne` and public `invalidate` all end there. Each keeps its own preconditions and its own failure contract, which is precisely what ADR-071 refused to merge and is not what this does: ADR-071's objection was that combining `#flush` and `#invalidateOne` would give one member a context-dependent contract, and here the contracts stay in the members that own them while only the mechanics move down.\n\nADR-078 refused this exact spelling, on the ground that `LV-5` and `PK-17` read `#invalidateOne`'s declaration and assert the two statements are inside it, so moving them would turn two green cases red for no behavioural reason. Remeasured for this record: `PK-17` reads `#writeValues(`, for `if (involved)` and `validateTrackDefinition(rewritten`, and its subject is a malformed stop list refused before anything mutates. One case reads `#invalidateOne`, not two. Three test files read regions of this source at all and none of them addresses public `invalidate`. So the price is one re-addressed case rather than two, and the thing being bought is larger than ADR-078 could see, because the third copy had not been found yet. Two copies of two statements is a tidy-up; three copies of one question is the defect this project cares about most.\n\nWhat the four records naming `#invalidateOne` name is unchanged. It is still the value tier's single-node flush, it still asserts runtime liveness so a disposed project is reported by the owner that decided rather than by the layer that noticed, and it still answers the actual batch. Its identity was never the two statements underneath those facts. `LV-5` is re-addressed rather than weakened and comes out strictly stronger: it now asserts that exactly one member in the whole file names the graph call at all, which is a claim its previous form could not make and which a second copy anywhere else would have satisfied.\n\n**Two: `#flush` answers nothing, and the empty-seed decision gets its own owner.** It returned `undefined` both for a disposed runtime and for an empty seed list, and `values` collapsed the two into an empty batch with `??`. Nothing was wrong at that head, because `#assertLive()` ran immediately before it at every call site. That is safety by statement ordering, and the next call site that does not happen to be written in that order is where it stops holding. So `#flush` keeps one decision, that a disposed runtime is skipped rather than reported, and returns `void` as it did before the batch; `#publishSeeds(seeds)` owns what an empty list means and is what `values` publishes through and what `#flush` delegates to, so the empty-seed rule still has exactly one owner. `#publishSeeds` asserts liveness itself, which is what makes its answer unambiguous by construction: every batch it hands back is one it published or one that had nothing to publish. `values` keeps its own re-ask after the recipe, because that one answers issue #288's question rather than this one, and two side-effect-free field reads cannot disagree.\n\nThe general form, and the reason this is a record rather than a diff: a sentinel that collapses two conditions is deleted rather than documented. Two owners cost one member here, and a comment saying which of the two an `undefined` meant would have cost a reader every time.\n\n**Three: public `invalidate` is not folded into `#flush`.** The follow-up asked whether it could be, since no test reads its declaration the way one reads `#invalidateOne`'s. The coupling is genuinely absent and the answer is still no, for a stronger reason than the one it was exempted by. `invalidate([])` publishes today and answers the batch that publication produced, while `#flush` skips an empty list, so folding would silently change a public answer. It would also make a public verb's return optional, which hands its callers a second failure contract to narrow rather than removing a duplicate. The duplication those two members really shared was the mechanics, and decision one takes it.\n\n**Four: `#refuseValueReentrant` becomes `#refuseImmediateReentrant`.** The old name read as a refusal of value-verb reentrancy, which is the opposite of its job: it is the pair of conditions a value verb must pass in order to be allowed to join a batch. Both conditions are about the immediate family that every verb in this class belongs to, which is the vocabulary `immediateInTransaction` and the immediate-verb refusal cases already use, and it leaves `#refuseReentrant` defensible as the total rung that adds the batch condition. Five call sites, no behaviour, no diagnostic and no rule id.\n\n**Also decided, and small.** `#teardown` clears `#valueSeeds` beside `#open`: both are open transaction state, that member claims that concept, and relying on `values`'s own `finally` to finish half of its stated job made the claim half true rather than harmless. Both value-batch factories drop `as PatchBatch` for an annotated local: the two functions whose entire job is producing a correctly shaped batch had opted out of the check that they do it, and the review's open question is answered by looking, since `PatchBatch` is four readonly members and a frozen literal satisfies it unaided. And `RA-170` drives `setKeyframe` through `#recompileKeyframes` inside a batch, which the review traced by hand and no run had ever executed, even though it is the call `apps/ik-playground/src/scroll-reach.ts` makes.\n\n**Consequences.** One member gains publication mechanics and three lose a copy of them. `#flush` loses a return type, which its three settlement callers already ignored. Seven private members own seven questions where five owned seven. No public surface moves: nothing is added, removed or renamed on an exported type or member, no refusal set changes, and every diagnostic and rule id is untouched, which is why this ships as a fix rather than as a tier.\n\nThis record amends ADR-078's first decision rather than editing it, and leaves ADR-069, ADR-070 and ADR-071 standing, because each of them names `#invalidateOne` for a property this change preserves. A reader arriving at ADR-078's `#invalidateSeeds` refusal should read it as accurate about the tree it measured and superseded by the measurement above.\n\n**Alternatives refused.** Renaming `#invalidateOne` to the shared owner and deleting the single-node member: it has a caller-facing report contract that `#flush` must not have, and four records name it. Giving `#flush` a discriminated result so its two conditions stayed distinguishable inside one member: that is the two-meanings defect with more syntax, and the caller would still have to branch on a condition it cannot cause. Keeping the casts and asserting the batch shape in a test instead: a compiler already answers that question, and a test that re-answers it is a second owner of a check.\n"
    }
  ]
}
