{
  "message": "docs(runtime): correct ADR-070's mount reading from the run that measured it",
  "target": 319,
  "edits": [
    {
      "path": "docs/ADR-070-one-reentrancy-rung.md",
      "find": "The same window had a second consequence with a different shape. `mount` and `unmount` from a hook attach or detach against the pre-commit graph, and a `mount` of the node the commit is adding leaves the settle step's own `#mountNode` to throw `Node \"...\" is already mounted.` inside the one phase that has no error boundary, which is issue #306's.",
      "replace": "The same window had a second consequence with a different shape. `mount` and `unmount` from a hook attach or detach against the pre-commit graph, so either one reaches a graph the commit has not replaced.\n\nThe issue text predicted that a `mount` of the node the commit is adding leaves the settle step's own `#mountNode` to throw `Node \"...\" is already mounted.` inside the one phase with no error boundary. The red run says otherwise, and the run is the finding. `RA-134` measured `Unknown graph node \"hero/hand\".` instead: the public verb reaches `#mountNode`, which reaches `GraphRuntime.attach`, and the candidate graph does not carry that node yet, so the graph layer refuses before an instance is ever registered and the settle step's own mount is never reached. The prediction was one layer too high and one phase too late.\n\nCorrecting it makes the case for this record rather than weakening it. What a caller actually read was `Unknown graph node` for a node it had just been handed a compiled Track for, which is the layer that noticed reporting in place of the owner that decided: the shape issue #288 corrected for a recipe, issue #303 for a commit and issue #305 for a direct write, arriving here for a fourth time. The rung is what makes the answer `schema-commit-reentrant` instead. Issue #306 keeps its whole job either way, because `#mountNode` still reaches ids `GraphRuntime.attach` refuses for reasons no reentrancy guard can see, and `addMotionTrack`, `disposeTrack` and `staged.commit()` are still caller code in a phase with no revert list."
    },
    {
      "path": "docs/ADR-070-one-reentrancy-rung.md",
      "find": "- `RA-134`, `structural-commit-path.test.ts`. `runtime.mount(addedNodeId)` from a `compileTrack` hook, which is the issue's fifth reproduction shape. The mount is refused, `instanceCount` is what the commit alone made it, and the settle phase runs clean. Cross-referenced to #306 rather than claiming to close it.",
      "replace": "- `RA-134`, `structural-commit-path.test.ts`. `runtime.mount(addedNodeId)` from a `compileTrack` hook, which is the issue's fifth reproduction shape. The mount is refused, `instanceCount` is what the commit alone made it, and the settle phase runs clean. Red on `Unknown graph node \"hero/hand\".` rather than on the `already mounted` the issue predicted, which is the correction in the Context above. Cross-referenced to #306 rather than claiming to close it."
    },
    {
      "path": "docs/ADR-070-one-reentrancy-rung.md",
      "find": "Four of five expected red. `RA-135` is green on both sides deliberately and the file says so, per the guardrail against padding a red count by asserting less in the case that could not fail. Which failure the pre-slice run reports for `RA-134` is read from the run rather than predicted in the case, because the hook's mount either attaches and leaves the settle step to throw `already mounted` or is refused by `GraphRuntime.attach` for a node the committed graph does not carry yet. Both are red on the journal's refusal line. Red and green run ids are in the pull request body.",
      "replace": "Four of five red, measured rather than predicted: run 33966081848 reports 4 failed and 960 passed, with `typecheck` and `format:check` green ahead of them, so every failure is an assertion that disagreed rather than a file that would not compile. Run 33966337062 is green on all seven required checks.\n\n`RA-131` and `RA-133` fail on `intrusion accepted` where the refusal belongs. `RA-132` fails on the extra journal line `progress hero/arm 0.5`, sitting exactly between `stage hero/arm` and `stage-commit hero/arm`, which is this issue's sharpest claim confirmed at the position the case predicted: the seek reached the Track the commit was displacing, before the staged one was committed. `RA-134` fails on `reentry Unknown graph node \"hero/hand\".`, which is the Context's correction.\n\n`RA-135` is green on both sides deliberately and the file says so, per the guardrail against padding a red count by asserting less in the case that could not fail."
    },
    {
      "path": "packages/core/test/unit/runtime/structural-commit-path.test.ts",
      "find": "    // Issue #310's second consequence, and the one that is not about publication. `mount` attaches\n    // against the pre-commit graph, so the same one condition refuses it, and the settle phase is left\n    // as the only owner of mounting this node. What the pre-slice run reports is read from the run\n    // rather than predicted here: the hook either attaches and leaves the settle step's own\n    // `#mountNode` to throw `already mounted` in the one phase with no error boundary, or it is refused\n    // by `GraphRuntime.attach` for a node the committed graph does not carry yet. Both are red on the\n    // `reentry` line, and only the first is also red on the thrown value.",
      "replace": "    // Issue #310's second consequence, and the one that is not about publication. `mount` reaches the\n    // pre-commit graph, so the same one condition refuses it, and the settle phase is left as the only\n    // owner of mounting this node. Which failure the pre-slice run reports was read from the run\n    // rather than predicted here, and it is not the one the issue named: the journal reads\n    // `reentry Unknown graph node \"hero/hand\".`, because the verb reaches `GraphRuntime.attach` and\n    // the candidate graph does not carry this node yet, so the settle step's `already mounted` is\n    // never reached. That is the layer that noticed answering in place of the owner that decided,\n    // which is the same defect #288, #303 and #305 each corrected one tier out. ADR-070's Context\n    // owns the correction."
    }
  ]
}
