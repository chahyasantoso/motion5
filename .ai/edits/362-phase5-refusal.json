{
  "version": 1,
  "expected_head": "4ee7c67ff2401174fa0c14d68c40698e9a7968db",
  "expected_blobs": {
    "docs/ADR-077-membership-stays-one-verb-per-node.md": null,
    "packages/core/test/unit/runtime/membership-batch-refused.test.ts": null,
    "docs/LIVE-EDIT-COST.md": "882e2c62187a7cd6e3b8bca337cd7e356e94ca1e",
    "docs/SESSION-STATUS.md": "7d0a67326ecadb2e155d0cd51e42f7074ba0d6de"
  },
  "message": "docs(runtime): refuse batched membership verbs and pin the measurement that refuses them",
  "edits": [
    {
      "path": "docs/ADR-077-membership-stays-one-verb-per-node.md",
      "create": "# ADR-077: Membership stays one verb per node\n\n**Status:** Accepted, 2026-09-13\n\n**Context.** Phase 5 of [issue #362](https://github.com/chahyasantoso/motion5/issues/362) was planned as a measurement rather than as an implementation, because `mount` publishes nothing under ADR-066, which makes the semantics of a loop and the semantics of a batch identical. The plan priced `mountAll` and `unmountAll` at two things: one membership revision bump and one `PublisherSnapshot` re-derivation instead of `n`, and all-or-nothing attempt semantics through the `collect` and `report` collector the teardown paths already use. It also said in advance what to do with the answer, which is why this record exists at all: if the saving is noise at realistic `n`, the verb is a facade over a `for` loop, the guardrails refuse facades, and the phase ends with a recorded measurement and no code. Both halves of that price were read against `runtime/graph-runtime.ts` and `runtime/project-runtime.ts` rather than assumed, and neither of them survives the reading.\n\n**The measurement, and it is not the number the plan expected.** The re-derivation a batch would collapse does not happen once per mount, so there is no `n` to reduce to one. `GraphRuntime.#membersRevision` is the key `#snapshotFor` compares its memo against, not an invalidation that eagerly rebuilds anything, and `#snapshotFor` is reached from `flush` and from nowhere else. `attach` therefore costs one `Set.add` and one integer increment, `mount` adds one `Map.set` on `#instances` to that, and between them they compose no publisher node, freeze no snapshot, publish no patch and schedule no flush. So `n` mounts followed by one flush already derive exactly one snapshot, which is the best a batch verb could do, and the loop both demos run is exactly that shape. The saving is not noise at realistic `n`: it is zero at every `n`. What is left for the verb to remove is `n - 1` increments of a number nothing reads until the flush, while the verb still owes the same `n` `Set.add` and `n` `Map.set` calls, because it still mounts `n` nodes. A verb whose body is the loop it replaces, buying one integer per node, is the facade this project refuses by name.\n\n**Decision.** No `mountAll` and no `unmountAll`. Membership stays one verb per node, and mounting a whole document stays a loop over the enumeration readers phase 2 landed. Phase 5 ends here, with the measurement recorded and no runtime code, which the plan named as a complete outcome rather than as a failure. The measurement is kept as four cases rather than as a number in prose, because a measurement in prose has no owner who can keep it true, and this project has already paid for that lesson in a file size that drifted 10,367 bytes with a green gate the whole way.\n\n**Alternatives rejected.**\n\n- A batch that mounts in a loop and moves the revision once. Rejected because nothing reads the revision between the mounts, so one bump and `n` bumps produce the same single derivation at the next flush. The optimal batch is observationally identical to the loop, which is the strongest form of this finding: there is no version of the verb that buys a publication, a derivation or a walk.\n- The collector, which is the all-or-nothing half. Rejected on the two refusal sets rather than on taste. `unmount` returns on a node it does not hold, and `detach` and `PatchRegistry.remove` are idempotent, so `unmountAll` has no partial failure to collect at all and the contract would be written for a state it cannot reach. `mount` has exactly two per-id refusals, `Node \"x\" is already mounted.` and `Unknown graph node \"x\".`, and both are answered before the call by readers that already exist: the first by `mountedNodeIds()`, the second by `motionIds()`, `freeTrackIds()` and `MotionHandle.trackIds`, none of which can name a node the graph does not carry. Its other three refusals are properties of the call site rather than of the id list, being `ProjectRuntime is disposed.`, `schema-transaction-immediate` inside a recipe and `schema-commit-reentrant` inside a commit's own hook, so they refuse the whole loop at its first element or none of it.\n- Rollback for the loop that does refuse partway. Rejected because the state it would undo is already readable: `mountedNodeIds()` is total and names exactly the prefix that mounted, so a caller resumes from the runtime's own answer. A collector reporting the same thing would be a second owner of a question a total reader answers, and its rollback would unmount work the caller asked for and can keep.\n- The ergonomic argument. Rejected because phase 2 already spent it. With `motionIds()`, `freeTrackIds()` and `MotionHandle.trackIds`, mounting a document is a loop over the runtime's own answer rather than over a copy of its ids kept beside the definition, which is what both demos now do, and `docs/guide/api-reference.md` already says so. The plan made this point itself, and it is why phase 5 was ordered after phase 2 rather than before it.\n- A dated revisit. Rejected for the reason ADR-076 refused a dated retirement: a date measures nothing, fails a build on a morning nobody picked, and is satisfied by editing the date. What reopens this decision is `RA-158` going red, which is precisely the condition under which the verb becomes worth pricing again.\n- One more that is worth naming because phase 3 just deleted three members over it: `mount` answers with the instance it mounted, and a batch would either drop that answer or invent a collection shape for it. One answer with two spellings is the defect this issue has been subtracting.\n\n**Consequences.** No public surface moves, no verb is added, no tier is added, and no failure contract is written, so there is nothing here for a consumer to migrate to or away from. No guide document changes either: `docs/guide/api-reference.md` already tells a consumer that mounting a whole document is a loop over the enumeration readers, and the absence of a member is not documented as a feature. `docs/LIVE-EDIT-COST.md` gains what a mount costs, because that document owns caller cost and the paragraph above is a caller-cost fact rather than a decision. The four cases below are green from the moment they land, and that is stated rather than dressed up as failing-first evidence: this slice changes no behaviour, so failing-first does not apply, and what the cases are is the tripwire under this decision rather than proof of a change. What each of them would fail without is named, because a green case is only evidence of what it would fail without.\n\n**Evidence.** `RA-158` through `RA-161` in `packages/core/test/unit/runtime/membership-batch-refused.test.ts`.\n\n- `RA-158` is the count the phase asked for, and it asserts both directions in one rig: four mounts before one flush hand the publisher one snapshot, and the same four mounts with a flush between them hand it four. Without the second half, a counter that can only ever answer one would be green against a publisher nobody called. It goes red if `mount` ever derives, publishes or schedules anything of its own, which is the condition that would make a batch verb worth pricing again.\n- `RA-159` asserts `unmount` is total, and it asserts the accepting direction first: the verb does move a mounted node out, and it then no-ops on that same node, on a node this project never mounted, and on an id the graph does not carry. It goes red if a per-id refusal is ever added to `unmount`, which is the only thing that would give `unmountAll` a collector to own.\n- `RA-160` mounts every id the three enumeration readers answer and asserts that none of them refuses, then asserts that both per-id refusals do fire on the ids those readers do not answer. It goes red if a reader ever answers an id the graph does not carry, which is the other way the per-id refusal could become reachable through the supported loop.\n- `RA-161` runs a loop that refuses partway and asserts that `mountedNodeIds()` names exactly the prefix that mounted and that the loop resumes from that answer. It is the case a rollback would have to delete rather than the case a rollback would pass.\n\n`packages/core/test/unit/runtime/publisher-snapshot-memo.test.ts` keeps owning the memo's keying, which is `RA-23` through `RA-26`, and this record does not restate it. That file owns whether the memo is keyed on everything its answer depends on; this one owns what a mount loop costs, and therefore what a batch over it would save.\n"
    },
    {
      "path": "packages/core/test/unit/runtime/membership-batch-refused.test.ts",
      "create": "import { afterEach, describe, expect, it, vi } from \"vitest\";\nimport type { ProjectDefinition } from \"../../../src/contract/v5\";\nimport { createManualClock } from \"../../../src/ports/clock\";\nimport { GraphPublisher } from \"../../../src/runtime/graph-publisher\";\nimport { GraphRuntime } from \"../../../src/runtime/graph-runtime\";\nimport { ProjectRuntime } from \"../../../src/runtime/project-runtime\";\n\n/**\n * Phase 5 of issue #362, and the phase ends in a refusal rather than in a verb. See ADR-077.\n *\n * The plan priced `mountAll` and `unmountAll` at one membership revision bump and one\n * `PublisherSnapshot` re-derivation instead of `n`, plus all-or-nothing attempt semantics through\n * the collector the teardown paths already use. Neither half is there to buy. The revision is the\n * key `#snapshotFor` compares its memo against rather than an invalidation, and `#snapshotFor` is\n * reached from `flush` and from nowhere else, so `n` mounts before one flush already derive one\n * snapshot. `unmount` refuses nothing per id, and `mount`'s two per-id refusals are both answered\n * before the call by the readers phase 2 landed.\n *\n * These four cases are green the day they land, and that is stated rather than dressed up: this\n * slice changes no behaviour, so failing-first does not apply, and what they are is the tripwire\n * under the decision. What each one would fail without is named at the case.\n *\n * `publisher-snapshot-memo.test.ts` owns the memo's keying, `RA-23` through `RA-26`. This file owns\n * the narrower claim a keying case cannot make: what a mount loop costs, and therefore what a batch\n * over it would save.\n */\nconst NODES = [\"hero/arm\", \"hero/hand\", \"hero/leg\", \"hero/foot\"] as const;\n\nconst PROJECT: ProjectDefinition = {\n  schemaVersion: 5,\n  motions: [\n    {\n      id: \"hero\",\n      trigger: { type: \"manual\" },\n      tracks: [{ id: \"arm\" }, { id: \"hand\" }, { id: \"leg\" }, { id: \"foot\" }],\n    },\n  ],\n  freeTracks: [{ id: \"cursor\" }],\n};\n\nconst compose = (node: { id: string }) => () => ({\n  values: { node: node.id },\n  sourceProgress: 0,\n  sourceRevisions: {},\n});\n\n// The prototype rather than the instance, because `GraphRuntime` owns its publisher and never hands\n// it out. `vi.spyOn` keeps the original implementation, so every flush below really runs.\nconst flushed = vi.spyOn(GraphPublisher.prototype, \"flush\");\n\nconst handed = () => flushed.mock.calls.map(([snapshot]) => snapshot);\nconst derivations = () => new Set(handed()).size;\n\nfunction loaded(): ProjectRuntime {\n  return new ProjectRuntime(PROJECT, { clock: createManualClock(), compose });\n}\n\nafterEach(() => {\n  flushed.mockClear();\n});\n\ndescribe(\"a mount loop is what mounting a document costs, so no batch verb is added\", () => {\n  it(\"RA-158 derives one publisher snapshot for a mount loop, against one per mount when a flush sits between them\", () => {\n    const clock = createManualClock();\n    const runtime = new GraphRuntime(PROJECT, clock, compose);\n\n    for (const nodeId of NODES) runtime.attach(nodeId);\n\n    // Mounting derives nothing and publishes nothing of its own, so there is no publication for a\n    // batch to collapse either: the publisher has not been asked anything yet.\n    expect(flushed).not.toHaveBeenCalled();\n    expect(runtime.memberCount).toBe(NODES.length);\n\n    clock.tick();\n\n    // The number the phase asked for. Four mounts, one derivation, because the revision is the\n    // memo's key rather than an invalidation and the derivation is pulled at the flush.\n    expect(derivations()).toBe(1);\n    expect([...(handed()[0]?.members ?? [])].sort()).toEqual([...NODES].sort());\n\n    runtime.dispose();\n    flushed.mockClear();\n\n    // The other direction, in the same rig, because a counter that can only ever answer one is green\n    // against a publisher nobody called. A flush between the mounts is what `n` derivations look\n    // like, and the loop above is not that shape.\n    const alternatingClock = createManualClock();\n    const alternating = new GraphRuntime(PROJECT, alternatingClock, compose);\n    for (const nodeId of NODES) {\n      alternating.attach(nodeId);\n      alternatingClock.tick();\n    }\n\n    expect(derivations()).toBe(NODES.length);\n\n    alternating.dispose();\n  });\n\n  it(\"RA-159 accepts an unmount of a node it does not hold, so a batch would have no partial failure to collect\", () => {\n    const runtime = loaded();\n    try {\n      // The accepting direction first, so the no-ops below are a claim about this verb rather than\n      // about a rig that never mounted anything.\n      runtime.mount(\"hero/arm\");\n      expect(runtime.mountedNodeIds()).toEqual([\"hero/arm\"]);\n      runtime.unmount(\"hero/arm\");\n      expect(runtime.mountedNodeIds()).toEqual([]);\n\n      // Total, and by the same line for all three: a node already unmounted, a node this project\n      // never mounted, and an id the graph does not carry at all.\n      expect(() => runtime.unmount(\"hero/arm\")).not.toThrow();\n      expect(() => runtime.unmount(\"hero/hand\")).not.toThrow();\n      expect(() => runtime.unmount(\"hero/nothing\")).not.toThrow();\n      expect(runtime.mountedNodeIds()).toEqual([]);\n      expect(runtime.instanceCount).toBe(0);\n    } finally {\n      runtime.dispose();\n    }\n  });\n\n  it(\"RA-160 mounts every id the enumeration readers answer, so a per-id refusal is unreachable through them\", () => {\n    const runtime = loaded();\n    try {\n      const ids = [\n        ...runtime.motionIds().flatMap((motionId) => [...runtime.motion(motionId).trackIds]),\n        ...runtime.freeTrackIds(),\n      ];\n      expect([...ids].sort()).toEqual([...NODES, \"~/cursor\"].sort());\n\n      for (const nodeId of ids) expect(() => runtime.mount(nodeId)).not.toThrow();\n      expect([...runtime.mountedNodeIds()].sort()).toEqual([...ids].sort());\n\n      // The two refusals a per-id loop can meet, and neither is reachable from the list above. This\n      // one is answered by `mountedNodeIds()`, which now names every id in it.\n      expect(() => runtime.mount(\"hero/arm\")).toThrow(/already mounted/);\n      // And this one by the readers themselves, which cannot name a node the graph does not carry.\n      expect(() => runtime.mount(\"hero/nothing\")).toThrow(/Unknown graph node/);\n    } finally {\n      runtime.dispose();\n    }\n  });\n\n  it(\"RA-161 leaves a refused loop's progress readable, so all-or-nothing would own nothing a total reader does not\", () => {\n    const runtime = loaded();\n    try {\n      const attempted = [\"hero/arm\", \"hero/hand\", \"hero/nothing\", \"hero/leg\"];\n      expect(() => {\n        for (const nodeId of attempted) runtime.mount(nodeId);\n      }).toThrow('Unknown graph node \"hero/nothing\"');\n\n      // Exactly the prefix, named by the runtime rather than reconstructed by the caller, which is\n      // the question a collector would be a second owner of.\n      expect(runtime.mountedNodeIds()).toEqual([\"hero/arm\", \"hero/hand\"]);\n\n      // And the loop resumes from that answer, which is the progress a rollback would have undone.\n      for (const nodeId of NODES)\n        if (!runtime.mountedNodeIds().includes(nodeId)) runtime.mount(nodeId);\n      expect([...runtime.mountedNodeIds()].sort()).toEqual([...NODES].sort());\n    } finally {\n      runtime.dispose();\n    }\n  });\n});\n"
    },
    {
      "path": "docs/LIVE-EDIT-COST.md",
      "find": "what refuses a removal is the token the handle captured, which is the mechanism ADR-056 already made total. See ADR-076.\n",
      "replace": "what refuses a removal is the token the handle captured, which is the mechanism ADR-056 already made total. See ADR-076.\n- Mounting a whole document is a loop, and the loop is what it costs. `mount(nodeId, instance?)` adds one entry to the instance map and one to the graph's member set, and moves the membership revision the publisher snapshot's memo is keyed on; it composes no publisher node, freezes no snapshot, publishes no patch and schedules no flush, which is the bullet above about ADR-066 read as a price rather than as a promise. Because that revision is a key rather than an invalidation, and because the snapshot is derived inside `flush` and nowhere else, `n` mounts followed by one flush derive exactly one snapshot rather than `n`. So there is no `mountAll` and no `unmountAll`, and that is a measured refusal rather than a gap: a batch could not derive fewer than one either, and all it would remove is `n - 1` increments of a number nothing reads until the flush. `unmount(nodeId)` is total on a node it does not hold, `mount` refuses only an id already mounted and an id the graph does not carry, and both of those are answerable from `mountedNodeIds()` and the three readers above before the call, so a partial loop is discoverable rather than something a collector has to report. See ADR-077.\n"
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Inspected main:** `c00f1cc8f47cf7cfa60629ada978d06609a1b8c8`, after the enumeration readers [#365](https://github.com/chahyasantoso/motion5/pull/365).",
      "replace": "- **Inspected main:** `4ee7c67ff2401174fa0c14d68c40698e9a7968db`, after the surface subtraction [#366](https://github.com/chahyasantoso/motion5/pull/366)."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Enumeration readers landed:** [#365](https://github.com/chahyasantoso/motion5/pull/365) is merged: `motionIds()`, `freeTrackIds()` and `mountedNodeIds()` on `ProjectHandle` answer frozen, total arrays in committed order, both demos mount from the runtime's own answer instead of a hand-written id list beside the document, and `#ownedBy` widened by one parameter so the free half and `MotionHandle.trackIds` read one ownership filter. `RA-145` through `RA-154`. Additive: no verb, no tier and no new failure contract.",
      "replace": "- **Runtime surface work landed, phases 2 through 4 of [#362](https://github.com/chahyasantoso/motion5/issues/362):** [#365](https://github.com/chahyasantoso/motion5/pull/365) and [#366](https://github.com/chahyasantoso/motion5/pull/366) are merged. `motionIds()`, `freeTrackIds()` and `mountedNodeIds()` on `ProjectHandle` answer frozen, total arrays in committed order, both demos mount from the runtime's own answer instead of a hand-written id list beside the document, and `#ownedBy` widened by one parameter so the free half and `MotionHandle.trackIds` read one ownership filter. `ProjectHandle.subscribe`, `adopt` and `destroyAdopted` are then deleted with no alias, which takes `TrackEntry.owner`, the `#schemaOwner` sentinel and `#addTrack`'s `owner` parameter out of `ProjectRuntime` with them, and the wrong `MotionHandle.signal` line is corrected in both guide documents that carried it. `RA-145` through `RA-157`. ADR-076 records the removal and ADR-020 is superseded rather than left contradicting it."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Unmerged surface subtraction:** phases 3 and 4 of [#362](https://github.com/chahyasantoso/motion5/issues/362) delete three public members and add none: `ProjectHandle.subscribe`, which was `subscribeNode` under an older name, and `adopt` / `destroyAdopted`, whose question `addTrack` and `TrackHandle.remove` already answer, which takes the `owner` concept out of `ProjectRuntime` with them. The guide's wrong `MotionHandle.signal` line is corrected in both documents that carried it. Breaking by subtraction, with no alias and no deprecation window on a private `0.0.0` package. `RA-155` through `RA-157` are its evidence, ADR-076 records the removal, and ADR-020 is superseded rather than left contradicting it. Review, exact-head CI, and merge remain gates; no landed change is claimed.",
      "replace": "- **Unmerged membership measurement:** phase 5 of [#362](https://github.com/chahyasantoso/motion5/issues/362) prices `mountAll` / `unmountAll` and refuses both, so it adds no verb, no tier and no runtime code at all. The saving the plan priced is zero rather than small: the membership revision is the key the publisher snapshot's memo is compared against rather than an invalidation, and the snapshot is derived inside `flush` and nowhere else, so `n` mounts before one flush already derive one snapshot. `unmount` has no per-id refusal for a collector to own, `mount`'s two are answered before the call by the phase 2 readers, and a loop that refuses partway leaves a prefix `mountedNodeIds()` names exactly. `RA-158` through `RA-161` are the tripwire under that decision and ADR-077 records it. Review, exact-head CI, and merge remain gates; no landed change is claimed."
    }
  ]
}
