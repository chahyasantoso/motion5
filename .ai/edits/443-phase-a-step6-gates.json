{
  "version": 1,
  "expected_head": "95bcc1c352d326b6ced413dd07f2894cc52f053a",
  "expected_blobs": {
    "packages/core/src/runtime/project-runtime.ts": "483230d008094fc2f10f36b932ce8d5bef486e3d",
    "packages/core/src/runtime/project-runtime.md": "a525dd27f7a1904925abc9f248b20db92478cc8a",
    "packages/core/test/unit/runtime/lifecycle-phase-wiring.test.ts": "a3a194bd27270fa21a9bed19dbd519ec1be0e76c",
    "packages/core/test/unit/runtime/commit-plan.test.ts": "bdf1b203f39db867d02a11827a5b3f1958bc8776"
  },
  "message": "fix(runtime): name the staged pair a structural verb commits and correct two source-shape gates",
  "edits": [
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "interface OpenTransaction {\n  tracks: Map<string, TrackEntry>;\n  motions: Map<string, MotionEntry>;\n}",
      "replace": "interface OpenTransaction {\n  tracks: Map<string, TrackEntry>;\n  motions: Map<string, MotionEntry>;\n}\n\ninterface StagedPair {\n  readonly tracks?: Map<string, TrackEntry>;\n  readonly motions?: Map<string, MotionEntry>;\n}"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #commit(plan: {\n    readonly tracks?: Map<string, TrackEntry>;\n    readonly motions?: Map<string, MotionEntry>;\n  }): void {",
      "replace": "  #commit(plan: StagedPair): void {"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "## #tracks\n\nThe retained pair; #motions follows the same rule.",
      "replace": "## StagedPair\n\nWhat one structural verb hands #commit: the half it staged, or both halves when a recipe staged both. An omitted half means untouched rather than empty, and #commit resolves it to the retained map, which is why this is two optional maps rather than a pair. Named rather than spelled inline at the one member that takes it, because a type written at its only call site is a type no sister document can describe and no gate can address, and because CommitPlan in commit-plan.ts is the derived effects and settlements: a second declaration called a plan would be two owners for one word. Step 6 retired the old SchemaPlan spelling along with the effect and settlement records it sat beside, and inlining the shape was the accident in that deletion rather than its point. See ADR-058 and ADR-064.\n\n## #tracks\n\nThe retained pair; #motions follows the same rule."
    },
    {
      "path": "packages/core/test/unit/runtime/lifecycle-phase-wiring.test.ts",
      "find": "[\"#commit(plan: SchemaPlan): void {\", 'this.#admit(COMMIT) === \"join\"'],",
      "replace": "[\"#commit(plan: StagedPair): void {\", 'this.#admit(COMMIT) === \"join\"'],"
    },
    {
      "path": "packages/core/test/unit/runtime/commit-plan.test.ts",
      "find": "expect(p).toContain(`case\"${k}\"`);",
      "replace": "expect(p).toContain(`case \"${k}\"`);"
    },
    {
      "path": "packages/core/test/unit/runtime/commit-plan.test.ts",
      "find": "expect(r).toContain(`case\"${k}\"`);",
      "replace": "expect(r).toContain(`case \"${k}\"`);"
    }
  ]
}
