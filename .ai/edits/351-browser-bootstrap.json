{
  "version": 1,
  "expected_head": "c28d6fd122c16c46fe5a29480b493faeddcc19a6",
  "expected_blobs": {
    "apps/ik-playground/src/App.tsx": "80df5fdedc1f229cfc78f2e6ab96b02e815b0ec1",
    "apps/ik-playground/src/main.tsx": "c6739c861ceb20de43cf241c1cbb2888afac2c33"
  },
  "message": "fix(ik): establish layout before restoration and release all owned resources",
  "edits": [
    {"path":"apps/ik-playground/src/App.tsx","find":"import React, { useEffect, useRef, useState } from \"react\";","replace":"import React, { useLayoutEffect, useRef, useState } from \"react\";"},
    {"path":"apps/ik-playground/src/App.tsx","find":"  useEffect(() => {","replace":"  useLayoutEffect(() => {"},
    {"path":"apps/ik-playground/src/App.tsx","find":"    gsap.registerPlugin(ScrollTrigger);","replace":"    let ownedProject: ProjectHandle | undefined;\n    let unsubscribeWeight = () => {};\n    const release = () => {\n      const failures: unknown[] = [];\n      for (const dispose of [() => unsubscribeWeight(), () => ownedProject?.dispose(), () => clock.dispose()]) {\n        try { dispose(); } catch (error) { failures.push(error); }\n      }\n      if (failures.length === 1) throw failures[0];\n      if (failures.length > 1) throw new AggregateError(failures, \"IK resource cleanup failed.\");\n    };\n    try {\n    gsap.registerPlugin(ScrollTrigger);"},
    {"path":"apps/ik-playground/src/App.tsx","find":"    let controller: ReturnType<typeof createScrollReach>;\n    let project: ProjectHandle;\n    try {\n      project = new Engine({","replace":"    let controller: ReturnType<typeof createScrollReach>;\n    const project = new Engine({"},
    {"path":"apps/ik-playground/src/App.tsx","find":"      }).load(ikPlaygroundProject);\n    } catch (error) {\n      clock.dispose();\n      throw error;\n    }","replace":"      }).load(ikPlaygroundProject);\n    ownedProject = project;"},
    {"path":"apps/ik-playground/src/App.tsx","find":"    const unsubscribeWeight = project.subscribe(nodeId(ARM.memberTracks[0]!), (patch) => {","replace":"    unsubscribeWeight = project.subscribe(nodeId(ARM.memberTracks[0]!), (patch) => {"},
    {"path":"apps/ik-playground/src/App.tsx","find":"      if (patch.status === \"ready\") setWeight(patch.sourceProgress);\n    });\n\n    return () => {\n      unsubscribeWeight();","replace":"      if (patch.status === \"ready\") setWeight(patch.sourceProgress);\n    });\n    } catch (error) {\n      controllerRef.current = undefined;\n      try { release(); } catch (cleanupError) {\n        throw new AggregateError([error, cleanupError], \"IK setup and cleanup failed.\");\n      }\n      throw error;\n    }\n\n    return () => {"},
    {"path":"apps/ik-playground/src/App.tsx","find":"      try {\n        project.dispose();\n      } finally {\n        clock.dispose();\n      }","replace":"      release();"},
    {"path":"apps/ik-playground/src/main.tsx","find":"import { createRoot } from \"react-dom/client\";","replace":"import { createRoot } from \"react-dom/client\";\nimport { flushSync } from \"react-dom\";"},
    {"path":"apps/ik-playground/src/main.tsx","find":"createRoot(container).render(<App />);","replace":"const root = createRoot(container);\n// Establish the authored scrollable layout before load/pageshow and native restoration.\n// Source initialization remains deferred; this is not a per-event Motion flush.\nflushSync(() => root.render(<React.StrictMode><App /></React.StrictMode>));"}
  ]
}
