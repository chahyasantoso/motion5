{
  "message": "docs(runtime): reflow the removal sentence the seed edit left ragged",
  "target": 265,
  "edits": [
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "   * after that, with nothing to revert, so a\n   * Motion whose last track the same commit removed is destroyed after that track deregistered from\n   * it.",
      "replace": "   * after that, with nothing to revert, so a Motion whose last track the same commit removed is\n   * destroyed after that track deregistered from it."
    }
  ]
}
