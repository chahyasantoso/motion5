{
  "version": 1,
  "expected_head": "ac6a771c62e7b253369abcf55a96fd958783e1cf",
  "expected_blobs": {
    "packages/core/src/runtime/patch-registry.ts": "62d812b09ff6f8c03f1235075dd6f7cc73cf983e",
    "packages/core/test/unit/runtime/patch-last-ready.test.ts": "dd43243a90677d09ff4f4802f6ea5cb4939db9ee",
    "docs/ADR-098-a-patch-carries-only-the-payload-its-status-owns.md": "89e0c8014b3269073430d46995b74a9bc2db2e70",
    "docs/SESSION-STATUS.md": "cca3739bb5befb574eb2c50b1f7419b2e7cec788"
  },
  "message": "feat(runtime): the registry retains the last ready patch, so a blocked node keeps no pose of its own",
  "edits": [
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "  readonly #patches = new Map<string, Patch>();\n  readonly #nodeListeners = new Map<string, Set<PatchListener>>();",
      "replace": "  readonly #patches = new Map<string, Patch>();\n  readonly #lastReady = new Map<string, Patch>();\n  readonly #nodeListeners = new Map<string, Set<PatchListener>>();"
    },
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "  get(nodeId: string): Patch | undefined {\n    return this.#patches.get(nodeId);\n  }\n  get notifying(): boolean {",
      "replace": "  get(nodeId: string): Patch | undefined {\n    return this.#patches.get(nodeId);\n  }\n  /**\n   * The last patch this node published as `ready`, or nothing if it never published one.\n   *\n   * Retention is this registry's question rather than a field on a patch that is about something\n   * else, and that is the decision slice 1 of #450 owed. A blocked or errored node has no pose of\n   * its own; the pose a consumer still wants to render is the last good one, which belongs to an\n   * earlier `ready` publication together with the progress and the source revisions measured with\n   * it. Carrying those forward onto a blocked patch, which is what `publish` below still does, makes\n   * that patch say `blocked` while holding values no blocked evaluation produced. ADR-098 deletes\n   * that field, so this member is where the answer moves rather than where it is lost, and a\n   * consumer rendering the last good pose keeps reading a real one.\n   *\n   * What it answers is the frozen patch that was published rather than a copy of it, so identity\n   * still tells a reader whether anything moved. A republication the registry suppressed is not a\n   * publication and cannot move it, and a status that owns no pose cannot replace one. Unmount,\n   * eviction and disposal all drop it, because a node with no retained patch has no last pose\n   * either: a remount reading a pose from before it was detached would be reading exactly the\n   * staleness `remove` exists to clear.\n   */\n  lastReady(nodeId: string): Patch | undefined {\n    return this.#lastReady.get(nodeId);\n  }\n  get notifying(): boolean {"
    },
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "  remove(nodeId: string): void {\n    this.#patches.delete(nodeId);\n  }",
      "replace": "  remove(nodeId: string): void {\n    this.#patches.delete(nodeId);\n    this.#lastReady.delete(nodeId);\n  }"
    },
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "    const previous = this.#patches.get(nodeId);\n    this.#patches.delete(nodeId);",
      "replace": "    const previous = this.#patches.get(nodeId);\n    this.#patches.delete(nodeId);\n    this.#lastReady.delete(nodeId);"
    },
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "    this.#patches.clear();\n    this.#nodeListeners.clear();",
      "replace": "    this.#patches.clear();\n    this.#lastReady.clear();\n    this.#nodeListeners.clear();"
    },
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "    const patch = deepFreeze(candidate);\n    this.#patches.set(input.nodeId, patch);",
      "replace": "    const patch = deepFreeze(candidate);\n    this.#patches.set(input.nodeId, patch);\n    // Read off the accepted publication rather than off the input, so a republication this member\n    // suppressed above cannot move what is retained, and a status owning no pose cannot replace one.\n    if (patch.status === \"ready\") this.#lastReady.set(input.nodeId, patch);"
    },
    {
      "path": "packages/core/test/unit/runtime/patch-last-ready.test.ts",
      "find": " * The member is reached through one intersection rather than off the class, so this round fails on\n * assertions rather than on `tsc`: `docs/GUARDRAILS.md` does not accept a failed compile as\n * failing-first evidence. The request that lands `lastReady` deletes this type and its cast.\n */\ntype RegistryUnderTest = PatchRegistry & {\n  lastReady(nodeId: string): Patch | undefined;\n};\n\nfunction open(): RegistryUnderTest {\n  return new PatchRegistry() as RegistryUnderTest;\n}",
      "replace": " * The round that landed these cases reached the member through an intersection rather than off the\n * class, so it failed on assertions rather than on `tsc`, which is the failing-first evidence\n * `docs/GUARDRAILS.md` asks for and a failed compile is not. The member exists now, so the cast is\n * deleted and every case below reads it off the class.\n */\nfunction open(): PatchRegistry {\n  return new PatchRegistry();\n}"
    },
    {
      "path": "docs/ADR-098-a-patch-carries-only-the-payload-its-status-owns.md",
      "find": "- Status: accepted as a decision, implementation not landed. This record is intent until the slices below merge, on documentation rule 2 of [README.md](./README.md).",
      "replace": "- Status: accepted as a decision, with slice 1 landed and the rest still intent. The retention question this record deliberately left open is answered in the Decision below; every other slice is intent until it merges, on documentation rule 2 of [README.md](./README.md)."
    },
    {
      "path": "docs/ADR-098-a-patch-carries-only-the-payload-its-status-owns.md",
      "find": "Which of the two that resolves to, a retained ready patch beside the current one or an accepted loss of the behaviour, is the one decision the first slice owes and the one this record deliberately leaves open, because it is a product question about what a blocked node renders as and it needs a failing-first case either way.",
      "replace": "That resolves to a retained ready patch rather than an accepted loss, and slice 1 is where it landed: `PatchRegistry` keeps the last accepted `ready` patch per node beside the current one and answers it through `lastReady(nodeId)`, so a consumer that renders the last good pose while a node is blocked keeps reading a real answer once the blocked variant stops owning `values`. Accepting the loss was refused because the behaviour is load-bearing rather than incidental: the carry-forward in `publish` has republished the last pose, the last progress and the last source revisions on every blocked patch for as long as those fields existed, and a consumer cannot be asked to rebuild from a wire that stopped carrying it. Retention belongs to the registry for the same reason a payload belongs to a status, which is one owner per question. What `lastReady` answers is the frozen patch that was published rather than a copy, so identity still tells a reader whether anything moved; a republication the registry suppressed is not a publication and does not move it; and unmount, eviction and disposal drop it, because a node with no retained patch has no last pose either. Slice 1 moves nothing on the observation wire, and `patch-last-ready.test.ts` carries one case that says so and is green on both sides deliberately, so the later slice that deletes the carry-forward re-reads that case rather than adjusting it to stay green."
    },
    {
      "path": "docs/ADR-098-a-patch-carries-only-the-payload-its-status-owns.md",
      "find": "The work is cut so no slice exceeds the sizing rule in [PR-WORKFLOW.md](./PR-WORKFLOW.md), and the first slice is the one that decides the retention question with a failing-first case, because every later slice is mechanical once it is answered.",
      "replace": "The work is cut so no slice exceeds the sizing rule in [PR-WORKFLOW.md](./PR-WORKFLOW.md). The first slice decided the retention question with a failing-first case and has landed, which is what makes every later slice mechanical, and the next one owes the complete reader census before any type moves."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Captured:** 2026-09-19, Asia/Jakarta.",
      "replace": "- **Captured:** 2026-09-20, Asia/Jakarta."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "`8bdfe5554ad623016564a6a68d9da9f13cbfb9cd`",
      "replace": "`ac6a771c62e7b253369abcf55a96fd958783e1cf`"
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **A rule owns its severity and its payload, and closing that is the live area:** [#449](https://github.com/chahyasantoso/motion5/issues/449) is open in review on [#456](https://github.com/chahyasantoso/motion5/pull/456), which supersedes slice 4 of [#452](https://github.com/chahyasantoso/motion5/pull/452) and depends on its slice 1. `contract/rule-id.ts` owns every id `Diagnostic.ruleId` can carry, derives the `plugin-contribution-` family through the one alias map rather than a second hand-maintained list, and `contract/rule-id.test.ts` scans `packages/core/src` and asserts the enumeration covers every id constructed there, including the module constants a literal argument scan cannot see. The six generic constructors take `RuleId` where they took `string`, `validateKeyframes` takes one `scope` where it took a prefix, an alias map and a group flag, and `Diagnostic.ruleId` is `RuleId`, which is the public surface change: a consumer constructing a `Diagnostic` with a rule id of its own invention stops compiling. `contract/rule.ts` owns what each rule is, checked total against that enumeration by `satisfies`, and `Diagnostic` is flat again: `severity` derives from the rule and `ids` is required and empty for a rule that names none, so `asDiagnostic`, `DiagnosticFields` and `contract/diagnostic-ids.ts` are deleted rather than re-justified. One constructor builds every diagnostic, every producer and every former raw literal forwards to it, and the `severity` parameter is deleted, so no call site can name one. Review and the records are what remain owed rather than code. Green at all seven jobs on the head named above; it is not merged, and the runs that measured it belong to that pull request.",
      "replace": "- **A patch carries only the payload its status owns, and the retention question is answered:** [#450](https://github.com/chahyasantoso/motion5/issues/450) is the live area on [#453](https://github.com/chahyasantoso/motion5/pull/453), and [#449](https://github.com/chahyasantoso/motion5/issues/449) is merged with [#452](https://github.com/chahyasantoso/motion5/pull/452), so `contract/rule-id.ts` owns every id `Diagnostic.ruleId` can carry, `contract/rule.ts` owns each rule's severity and whether it names ids, `Diagnostic` is one flat frozen interface whose `ids` is required and empty for a rule naming none, and one constructor builds every diagnostic with no argument position in which a call site could name a severity. On #450, slice 1 of ADR-098 has landed and it is the retention decision rather than a type change: `PatchRegistry` retains the last accepted `ready` patch per node and answers it through `lastReady(nodeId)`, so the last good pose is the registry's retained answer rather than a payload on a patch whose status says the node is not ready, and unmount, eviction and disposal drop it while a suppressed republication cannot move it. Nothing on the observation wire moved: `Patch`, `PublishInput`, `samePatch`, `#notifyTerminal` and the asymmetric carry-forward in `publish` are all unchanged, and one case is green on both sides deliberately to pin that. The next slice owes the complete reader census before any type moves. Red then green on the branch, with the exact commits and runs in that pull request rather than here."
    }
  ]
}
