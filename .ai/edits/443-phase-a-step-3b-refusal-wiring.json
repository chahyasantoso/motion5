{
  "version": 1,
  "expected_head": "b95aa63f7263bfa64132219ebc6040795e026df3",
  "expected_blobs": {
    "packages/core/src/runtime/project-runtime.ts": "d414362971d2fcbe680b0108afc42dcca79d0187",
    "packages/core/src/runtime/project-runtime.md": "1775ee9f78c1bb291276726df5dfc6892ad79351",
    "packages/core/test/unit/runtime/refusal-wiring.test.ts": null,
    "docs/SESSION-STATUS.md": "fff34594b42e20ca6d3f33c58b8147ebfdfc5b1e"
  },
  "message": "refactor(runtime): decode results and refuse through the closed unions in the project runtime",
  "edits": [
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "  commitInFlight,\n  describeDiagnostics,\n  describeError,", "replace": "  commitInFlight,\n  describeError,"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "import { collect, report, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";", "replace": "import { collect, report, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";\nimport { refuse } from \"./refusal\";\nimport {\n  expectLive,\n  expectValid,\n  isLive,\n  resolveToken,\n  stale,\n  validated,\n  type Resolved,\n} from \"./results\";"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "import { StaleMotionHandleError, type MotionHandle } from \"../contract/motion-handle\";", "replace": "import type { MotionHandle } from \"../contract/motion-handle\";"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "import {\n  StaleTrackHandleError,\n  type AuthoredValues,\n  type LiveValues,\n  type RequireView,\n  type TrackHandle,\n} from \"../contract/track-handle\";", "replace": "import type {\n  AuthoredValues,\n  LiveValues,\n  RequireView,\n  TrackHandle,\n} from \"../contract/track-handle\";"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    if (this.#disposed) throw new Error(\"ProjectRuntime is disposed.\");", "replace": "    if (this.#disposed) refuse({ kind: \"disposed\" });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "      throw new TypeError(describeDiagnostics(triggerDiagnostics));", "replace": "      refuse({ kind: \"invalid-definition\", diagnostics: triggerDiagnostics });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    if (!this.#readMotions().has(motionId)) throw new TypeError(`Unknown motion \"${motionId}\".`);", "replace": "    if (!this.#readMotions().has(motionId)) refuse({ kind: \"unknown-motion\", motionId });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    if (!entry) throw new TypeError(`Unknown motion \"${motionId}\".`);", "replace": "    if (!entry) refuse({ kind: \"unknown-motion\", motionId });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    if (motionId !== undefined && !this.#readMotions().has(motionId))\n      throw new TypeError(`Unknown motion \"${motionId}\".`);", "replace": "    if (motionId !== undefined && !this.#readMotions().has(motionId))\n      refuse({ kind: \"unknown-motion\", motionId });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    const validation = validateTrackDefinition(track, `addTrack(${track.id})`);\n    if (!validation.valid || !validation.value)\n      throw new TypeError(describeDiagnostics(validation.diagnostics));\n    const accepted = validation.value;", "replace": "    const accepted = expectValid(\n      validated(validateTrackDefinition(track, `addTrack(${track.id})`)),\n    );"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    if (!entry) throw new TypeError(`Unknown graph node \"${nodeId}\".`);", "replace": "    if (!entry) refuse({ kind: \"unknown-node\", nodeId });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "  ): E | undefined {\n    if (this.#disposed) return undefined;\n    const entry = entries.get(id);\n    return entry !== undefined && entry.token === token ? entry : undefined;\n  }", "replace": "  ): Resolved<E> {\n    if (this.#disposed) return stale();\n    return resolveToken(entries, id, token);\n  }"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "  #entryIfLive(id: string, token: number): TrackEntry | undefined {", "replace": "  #entryIfLive(id: string, token: number): Resolved<TrackEntry> {"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "  #liveEntry(id: string, token: number): TrackEntry {\n    const entry = this.#entryIfLive(id, token);\n    if (entry === undefined) throw new StaleTrackHandleError(id);\n    return entry;\n  }", "replace": "  #liveEntry(id: string, token: number): TrackEntry {\n    return expectLive(this.#entryIfLive(id, token), { kind: \"track\", id });\n  }"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "  #motionIfLive(id: string, token: number): MotionEntry | undefined {", "replace": "  #motionIfLive(id: string, token: number): Resolved<MotionEntry> {"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "  #liveMotion(id: string, token: number): MotionEntry {\n    const entry = this.#motionIfLive(id, token);\n    if (entry === undefined) throw new StaleMotionHandleError(id);\n    return entry;\n  }", "replace": "  #liveMotion(id: string, token: number): MotionEntry {\n    return expectLive(this.#motionIfLive(id, token), { kind: \"motion\", id });\n  }"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "        return runtime.#entryIfLive(id, token) !== undefined;", "replace": "        return isLive(runtime.#entryIfLive(id, token));"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "        return runtime.#motionIfLive(id, token) !== undefined;", "replace": "        return isLive(runtime.#motionIfLive(id, token));"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "      if (diagnostics.some(({ severity }) => severity === \"error\"))\n        throw new TypeError(describeDiagnostics(diagnostics));", "replace": "      if (diagnostics.some(({ severity }) => severity === \"error\"))\n        refuse({ kind: \"invalid-definition\", diagnostics });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    if (resolved.diagnostics.some(({ severity }) => severity === \"error\"))\n      throw new TypeError(describeDiagnostics(resolved.diagnostics));", "replace": "    if (resolved.diagnostics.some(({ severity }) => severity === \"error\"))\n      refuse({ kind: \"invalid-definition\", diagnostics: resolved.diagnostics });"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "    const validation = validateTrackDefinition(next, `replaceTrack(${id})`);\n    if (!validation.valid || !validation.value)\n      throw new TypeError(describeDiagnostics(validation.diagnostics));", "replace": "    const accepted = expectValid(validated(validateTrackDefinition(next, `replaceTrack(${id})`)));"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "      track: validation.value,", "replace": "      track: accepted,"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "      if (involved) {\n        const validation = validateTrackDefinition(rewritten, `writeValues(${nodeId})`);\n        if (!validation.valid) throw new TypeError(describeDiagnostics(validation.diagnostics));\n      }", "replace": "      // Validated and then discarded: this write stages no definition, so the refusal is the\n      // whole of what validating the candidate is for here.\n      if (involved)\n        expectValid(validated(validateTrackDefinition(rewritten, `writeValues(${nodeId})`)));"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "      const validation = validateTrackDefinition(next, `${verb}(${nodeId})`);\n      if (!validation.valid || !validation.value)\n        throw new TypeError(describeDiagnostics(validation.diagnostics));\n      const accepted = validation.value;", "replace": "      const accepted = expectValid(validated(validateTrackDefinition(next, `${verb}(${nodeId})`)));"},
    {"path": "packages/core/src/runtime/project-runtime.ts", "find": "      if (resolved?.diagnostics.some(({ severity }) => severity === \"error\"))\n        throw new TypeError(describeDiagnostics(resolved.diagnostics));", "replace": "      if (resolved?.diagnostics.some(({ severity }) => severity === \"error\"))\n        refuse({ kind: \"invalid-definition\", diagnostics: resolved.diagnostics });"},
    {"path": "packages/core/src/runtime/project-runtime.md", "find": "Resolves an id this project must have, or reports an unknown graph node.", "replace": "Resolves an id this project must have, or refuses it as the unknown-node of `refusal.ts`, so the one sentence a missing id renders has one owner."},
    {"path": "packages/core/src/runtime/project-runtime.md", "find": "The generic comparison of a handle's captured token against the readable entry. A disposed runtime answers absent immediately, even while deferred teardown has not cleared maps. This is a nonthrowing probe so live getters remain safe.", "replace": "Runtime liveness and nothing else. A disposed runtime answers a stale resolution immediately, even while deferred teardown has not cleared maps, and the token comparison itself belongs to `resolveToken` in `results.ts`, which is why this member answers one `Resolved<E>` rather than an entry or its absence: a reader states which of the two it wants instead of restating the comparison. It still throws nothing, so live getters remain safe."},
    {"path": "packages/core/src/runtime/project-runtime.md", "find": "The track probe through #liveOf.", "replace": "The track resolution through #liveOf, read as a discriminant by `isLive` and as an entry by `expectLive`."},
    {"path": "packages/core/src/runtime/project-runtime.md", "find": "The throwing track-read resolver. Absence or a changed token produces StaleTrackHandleError, including on a disposed runtime.", "replace": "The throwing track-read resolver, which is `expectLive` over that resolution rather than a second comparison of its own. Absence or a changed token still produces StaleTrackHandleError, including on a disposed runtime, because the refusal names the target and the contract layer keeps the sentence."},
    {"path": "docs/SESSION-STATUS.md", "find": "Steps 3b through 8 are ordered in that pull request body and none of them is on `main`.", "replace": "Step 3b makes both modules load-bearing rather than adopted at the edges: `project-runtime.ts` decodes its four validation results through `expectValid` and its handle resolutions through `resolveToken` and `expectLive`, and fifteen inline throws become one of those or `refuse`, so eight retyped diagnostic templates and both stale-handle constructions have one owner each. Steps 4 through 8 are ordered in that pull request body and none of them is on `main`."},
    {"path": "packages/core/test/unit/runtime/refusal-wiring.test.ts", "create": "import { describe, expect, it } from \"vitest\";\nimport type { ProjectDefinition, TrackDefinition } from \"../../../src/contract/v5\";\nimport { StaleMotionHandleError } from \"../../../src/contract/motion-handle\";\nimport { StaleTrackHandleError } from \"../../../src/contract/track-handle\";\nimport { createManualClock } from \"../../../src/ports/clock\";\nimport {\n  DisposedProjectError,\n  RefusalError,\n  describeDiagnostics,\n  type Refusal,\n} from \"../../../src/runtime/refusal\";\nimport { ProjectRuntime } from \"../../../src/runtime/project-runtime\";\n\n/**\n * Issue #443, phase A step 3b: the refusals `ProjectRuntime` spells are the union, not prose.\n *\n * `refusal-data.test.ts` pins what each refusal says and what class it is thrown as, in isolation.\n * What this file pins is the wiring: every site in the runtime that refused inline now refuses\n * through `refuse`, `expectValid` or `expectLive`, so a caller can branch on `error.refusal.kind`\n * rather than parse a message, while the sentence and the class each site threw stay exactly what\n * they were.\n *\n * It fails without that wiring, because today those sites throw a bare `TypeError` or a bare\n * `Error` carrying no payload at all.\n */\n\nconst NODE_ID = \"hero/arm\";\n\nconst PROJECT: ProjectDefinition = {\n  schemaVersion: 5,\n  motions: [{ id: \"hero\", trigger: { type: \"manual\" }, tracks: [{ id: \"arm\" }] }],\n};\n\nconst compose = (node: { id: string }) => () => ({\n  values: { node: node.id },\n  sourceProgress: 0,\n  sourceRevisions: {},\n});\n\nfunction loaded(): ProjectRuntime {\n  return new ProjectRuntime(PROJECT, { clock: createManualClock(), compose });\n}\n\n/** Returns the thrown value, because each case asserts on the class and on the payload. */\nfunction thrownBy(operation: () => unknown): unknown {\n  try {\n    operation();\n  } catch (error) {\n    return error;\n  }\n  throw new Error(\"Expected the operation to refuse.\");\n}\n\n/** The payload one refusal carries, from either of the two classes that carry one. */\nfunction refusalIn(thrown: unknown): Refusal {\n  if (thrown instanceof RefusalError || thrown instanceof DisposedProjectError)\n    return thrown.refusal;\n  throw new Error(`Expected a refusal carrying its payload, received ${String(thrown)}.`);\n}\n\ndescribe(\"the project runtime refuses through the union rather than inline\", () => {\n  it(\"names the node an unknown id asked for\", () => {\n    const project = loaded();\n\n    const thrown = thrownBy(() => project.track(\"free:nope\"));\n    const refusal = refusalIn(thrown);\n\n    expect(thrown).toBeInstanceOf(TypeError);\n    expect((thrown as Error).message).toBe('Unknown graph node \"free:nope\".');\n    expect(refusal.kind).toBe(\"unknown-node\");\n    expect(refusal.kind === \"unknown-node\" ? refusal.nodeId : undefined).toBe(\"free:nope\");\n\n    project.dispose();\n  });\n\n  it(\"names the motion at all three verbs that refuse an unknown one\", () => {\n    const project = loaded();\n\n    const read = refusalIn(thrownBy(() => project.motion(\"nope\")));\n    const destroyed = refusalIn(thrownBy(() => project.destroyMotion(\"nope\")));\n    const added = refusalIn(thrownBy(() => project.addTrack({ id: \"a\" }, { motionId: \"nope\" })));\n\n    for (const refusal of [read, destroyed, added]) {\n      expect(refusal.kind).toBe(\"unknown-motion\");\n      expect(refusal.kind === \"unknown-motion\" ? refusal.motionId : undefined).toBe(\"nope\");\n    }\n\n    project.dispose();\n  });\n\n  it(\"carries the diagnostics an invalid definition was refused for\", () => {\n    const project = loaded();\n    const invalid = { id: \"a\", keyframes: 5 } as unknown as TrackDefinition;\n\n    const thrown = thrownBy(() => project.addTrack(invalid));\n    const refusal = refusalIn(thrown);\n    const diagnostics = refusal.kind === \"invalid-definition\" ? refusal.diagnostics : [];\n\n    expect(thrown).toBeInstanceOf(TypeError);\n    expect(refusal.kind).toBe(\"invalid-definition\");\n    expect(diagnostics.length).toBeGreaterThan(0);\n    // The message is still the rendering of those diagnostics, and now it has one owner.\n    expect((thrown as Error).message).toBe(describeDiagnostics(diagnostics));\n\n    project.dispose();\n  });\n\n  it(\"carries the disposal refusal, still an Error and still not a TypeError\", () => {\n    const project = loaded();\n    project.dispose();\n\n    const thrown = thrownBy(() => project.mount(NODE_ID));\n\n    expect(thrown).toBeInstanceOf(DisposedProjectError);\n    expect(thrown).not.toBeInstanceOf(TypeError);\n    expect((thrown as Error).message).toBe(\"ProjectRuntime is disposed.\");\n    expect(refusalIn(thrown).kind).toBe(\"disposed\");\n  });\n\n  it(\"keeps both stale-handle classes and both sentences the contract layer owns\", () => {\n    const project = loaded();\n    const track = project.track(NODE_ID);\n    const motion = project.motion(\"hero\");\n\n    project.track(NODE_ID).remove();\n    project.destroyMotion(\"hero\");\n\n    const staleTrack = thrownBy(() => track.definition);\n    const staleMotion = thrownBy(() => motion.definition);\n\n    expect(staleTrack).toBeInstanceOf(StaleTrackHandleError);\n    expect((staleTrack as Error).message).toBe('Track \"hero/arm\" is no longer live.');\n    expect(staleMotion).toBeInstanceOf(StaleMotionHandleError);\n    expect((staleMotion as Error).message).toBe('Motion \"hero\" is no longer live.');\n    expect(track.live).toBe(false);\n    expect(motion.live).toBe(false);\n\n    project.dispose();\n  });\n});\n"}
  ]
}
