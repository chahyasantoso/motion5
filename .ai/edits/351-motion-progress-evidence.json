{
"version":1,
"expected_head":"afbfd81ff38bc669f30f6d2ef76f25b11e9d8052",
"expected_blobs":{
"packages/core/test/integration/ik-playground-motion-progress.test.ts":"99572067123690f614cb9995eaa5553f39361ebc",
"apps/ik-playground/src/components/SolverPanel.tsx":"30769da632767db68deea8ec8c6f90b9f07908aa"
},
"message":"test(ik-playground): exercise native scroll and intent with real interpolation",
"edits":[
{"path":"packages/core/test/integration/ik-playground-motion-progress.test.ts","find":"import { describe, expect, it, vi } from \"vitest\";","replace":"import { describe, expect, it, vi } from \"vitest\";\nimport { gsap } from \"gsap\";\nimport { bindScrollReach, createScrollReach } from \"../../../../apps/ik-playground/src/scroll-reach\";\nimport { createGsapInterpolator } from \"../../src/adapters/interpolator/gsap\";"},
{"path":"packages/core/test/integration/ik-playground-motion-progress.test.ts","find":"import { createFakeInterpolator, createFakeScheduler } from \"../../src/testing/fakes\";","replace":"import { createFakeScheduler } from \"../../src/testing/fakes\";"},
{"path":"packages/core/test/integration/ik-playground-motion-progress.test.ts","find":"    const clock = createManualClock();\n    const handle = new Engine({","replace":"    const clock = createManualClock();\n    let controller: ReturnType<typeof createScrollReach>;\n    const handle = new Engine({"},
{"path":"packages/core/test/integration/ik-playground-motion-progress.test.ts","find":"      interpolator: createFakeInterpolator(),\n      triggerFactory: createTriggerFactory({ scroll: () => scroll.source }),\n    }).load(ikPlaygroundProject);","replace":"      // Only the browser producer and scheduler are controlled; interpolation is real GSAP.\n      interpolator: createGsapInterpolator(gsap),\n      triggerFactory: createTriggerFactory({ scroll: () => bindScrollReach(scroll.source, () => controller.commit()) }),\n    }).load(ikPlaygroundProject);\n    controller = createScrollReach(handle);"},
{"path":"packages/core/test/integration/ik-playground-motion-progress.test.ts","find":"      const held = handle.get(id);\n      clock.tick(1000);\n      scheduler.flush();\n      expect(handle.get(id)).toBe(held);","replace":"      const held = handle.get(id);\n      const appliedGoal = handle.get(nodeId(ARM.goalTrack));\n      controller.moveGoal(ARM.goalTrack, 290, 360);\n      controller.flip(ARM.solverTrack, true);\n      clock.tick(1000);\n      scheduler.flush();\n      scroll.refresh(0.25, 500);\n      scroll.update(0.25, 500);\n      scheduler.flush();\n      expect(handle.get(id)).toBe(held);\n      expect(handle.get(nodeId(ARM.goalTrack))).toBe(appliedGoal);\n      scroll.update(1, 1000);\n      scheduler.flush();\n      expect(handle.get(id)?.sourceProgress).toBe(1);\n      expect(handle.get(nodeId(ARM.goalTrack))?.values).toMatchObject({ x: 290, y: 360 });\n      expect(handle.get(nodeId(ARM.solverTrack))?.values.flip).toBe(true);\n      const full = handle.get(id);\n      controller.moveGoal(ARM.goalTrack, 310, 380);\n      controller.flip(ARM.solverTrack, false);\n      clock.tick(2000);\n      scheduler.flush();\n      expect(handle.get(id)).toBe(full);\n      scroll.update(0.75, 750);\n      scheduler.flush();\n      expect(handle.get(nodeId(ARM.goalTrack))?.values).toMatchObject({ x: 310, y: 380 });\n      expect(handle.get(nodeId(ARM.solverTrack))?.values.flip).toBe(false);"},
{"path":"packages/core/test/integration/ik-playground-motion-progress.test.ts","find":"    expect(scroll.kill).toHaveBeenCalledTimes(1);\n  });\n});","replace":"    expect(scroll.kill).toHaveBeenCalledTimes(1);\n    expect(() => { scroll.update(0.5, 500); scheduler.flush(); }).not.toThrow();\n  });\n});"},
{"path":"apps/ik-playground/src/components/SolverPanel.tsx","find":"      The scroll adapter applies pending targets and flips; Motion progress drives authored\n      <code>fk.weight</code>","replace":"      The scroll adapter applies pending targets and flips; Motion progress drives authored{\" \"}\n      <code>fk.weight</code>"}
]
}
