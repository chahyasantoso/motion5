{
  "message": "test(scripts): pin that the gate reaches the AE- series",
  "target": 283,
  "edits": [
    {
      "path": "packages/core/test/unit/scripts/evidence-case-ids.test.ts",
      "find": "const SELF = \"unit/scripts/evidence-case-ids.test.ts\";",
      "replace": "const SELF = \"unit/scripts/evidence-case-ids.test.ts\";\nconst AI_EDIT = \"unit/scripts/apply-ai-edit.test.ts\";"
    },
    {
      "path": "packages/core/test/unit/scripts/evidence-case-ids.test.ts",
      "find": "    expect(total).toBeGreaterThan(15);\n  });\n});",
      "replace": "    expect(total).toBeGreaterThan(15);\n  });\n\n  it(\"reaches the AE- series, which was declared and cited and never scanned\", () => {\n    const ids = declaredCaseIds(AI_EDIT);\n    expect(ids.length).toBeGreaterThan(0);\n    expect(ids.filter((id) => !/^AE-\\d+$/.test(id))).toEqual([]);\n  });\n});"
    }
  ]
}
