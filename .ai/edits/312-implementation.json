{
  "message": "fix(runtime): complete teardown and preserve caller outcomes",
  "target": 324,
  "edits": [
    {
      "path": "packages/core/src/runtime/rollback.ts",
      "find": "function collect(steps: readonly (() => void)[]): readonly unknown[] {",
      "replace": "export function collect(steps: readonly (() => void)[]): readonly unknown[] {"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  describeDiagnostics,\n  immediateInTransaction,",
      "replace": "  describeDiagnostics,\n  describeError,\n  immediateInTransaction,"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "import { rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";",
      "replace": "import { collect, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "      if (this.#inFlight === 0 && this.#pendingTeardown) this.#teardown();",
      "replace": "      if (this.#inFlight === 0 && this.#pendingTeardown)\n        this.#recordRelease(this.#teardown());"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    this.#teardown();\n  }\n\n  #teardown(): void {\n    this.#pendingTeardown = false;\n    for (const nodeId of [...this.#instances.keys()]) this.#graph.detach(nodeId);\n    this.#instances.clear();\n    this.#tracks.clear();\n    this.#motions.clear();\n    this.#open = undefined;\n    this.#graph.dispose();\n    this.#disposeComposition();\n  }\n  #assertLive(): void {",
      "replace": "    const failures = this.#teardown();\n    this.#recordRelease(failures);\n    if (failures.length === 1) throw failures[0];\n    if (failures.length > 1) throw new AggregateError(failures, \"ProjectRuntime release failed.\");\n  }\n\n  #teardown(): readonly unknown[] {\n    this.#pendingTeardown = false;\n    const release: (() => void)[] = [];\n    for (const nodeId of [...this.#instances.keys()])\n      release.push(() => this.#graph.detach(nodeId));\n    release.push(() => {\n      this.#instances.clear();\n      this.#tracks.clear();\n      this.#motions.clear();\n      this.#open = undefined;\n    });\n    release.push(() => this.#graph.dispose());\n    release.push(() => this.#disposeComposition());\n    return collect(release);\n  }\n\n  #recordRelease(failures: readonly unknown[]): void {\n    for (const failure of failures)\n      this.#diagnostics.record({\n        ruleId: \"project-release-failed\",\n        path: \"dispose()\",\n        message: describeError(failure),\n        severity: \"error\",\n      });\n  }\n  #assertLive(): void {"
    }
  ]
}
