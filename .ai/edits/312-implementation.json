{"message":"fix(runtime): complete teardown without replacing caller outcomes","target":325,"edits":[{"path":"packages/core/src/runtime/rollback.ts","find":"function collect(steps: readonly (() => void)[]): readonly unknown[] {","replace":"export function collect(steps: readonly (() => void)[]): readonly unknown[] {"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  describeDiagnostics,\n  immediateInTransaction,","replace":"  describeDiagnostics,\n  describeError,\n  immediateInTransaction,"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"import { rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";","replace":"import { collect, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  #teardown(): void {\n    this.#pendingTeardown = false;\n    for (const nodeId of [...this.#instances.keys()]) this.#graph.detach(nodeId);\n    this.#instances.clear();\n    this.#tracks.clear();\n    this.#motions.clear();\n    this.#open = undefined;\n    this.#graph.dispose();\n    this.#disposeComposition();\n  }","replace":"  #teardown(): void {\n    this.#pendingTeardown = false;\n    const failures = collect([\n      ...[...this.#instances.keys()].map((nodeId) => () => this.#graph.detach(nodeId)),\n      () => {\n        this.#instances.clear();\n        this.#tracks.clear();\n        this.#motions.clear();\n        this.#open = undefined;\n      },\n      () => this.#graph.dispose(),\n      () => this.#disposeComposition(),\n    ]);\n    // Reporting is inspection-only and happens after every release attempt. Neither a direct\n    // dispose nor a boundary drain may replace the outcome of the caller it is releasing for.\n    const describe = (failure: unknown, seen = new Set<unknown>()): string => {\n      try {\n        if (seen.has(failure)) return \"[circular release failure]\";\n        seen.add(failure);\n        const message = describeError(failure);\n        if (!(failure instanceof AggregateError)) return message;\n        return [message, ...failure.errors.map((cause: unknown) => describe(cause, seen))].join(\"; \");\n      } catch {\n        return \"Release failed with an unprintable thrown value.\";\n      }\n    };\n    for (const failure of failures) {\n      this.#diagnostics.record(Object.freeze({\n        ruleId: \"project-release-failed\",\n        path: \"dispose\",\n        message: describe(failure),\n        severity: \"error\",\n      }));\n    }\n  }"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"   * refusal and the release are two things rather than one. See ADR-067.\n   */\n  dispose(): void {","replace":"   * refusal and the release are two things rather than one. See ADR-067.\n   * Release failures are recorded in diagnostics, never thrown; every release step is attempted.\n   */\n  dispose(): void {"},{"path":"packages/core/src/runtime/project-runtime.md","find":"A failure in that separate release can still replace the selected outcome; issue #312 owns that known remaining boundary rather than this slice silently claiming to fix it.","replace":"That release records its failures without throwing, so the selected settlement outcome survives unchanged. Issue #312's completion and reporting policy is owned entirely by `#teardown`, not by this member or the boundary."},{"path":"packages/core/src/runtime/project-runtime.md","find":"Nothing about what it does changed when it was split out: the body is `dispose()`'s previous body, moved rather than rewritten.","replace":"Issue #312 extends that owner with one failure policy for direct disposal and deferred drains: attempt every independent release step, then record failures in the bounded diagnostics without throwing. No call-site flag or second error boundary decides whether an operation's outcome survives."},{"path":"packages/core/src/runtime/project-runtime.md","find":"It still detaches before it disposes the graph, and it still empties both retained maps, which is what `edit` cites when it drops a staged pair rather than committing it.","replace":"Each mounted node's detach is an independent collected step. Native map clearing and the open-transaction reset follow those attempts, then graph disposal and composition disposal each get their own step. The collector in `rollback.ts` is reused without changing rollback or settlement reporting. A failed detach cannot prevent a later detach, retained-map clearing, or either disposal. The guarantee is exactly-once attempts, not successful recovery inside a host that threw partway through its own cleanup; retrying such a host could double-release it. Both retained maps are cleared, which is what `edit` cites when it drops a staged pair rather than committing it."},{"path":"packages/core/src/runtime/project-runtime.md","find":"`disposeComposition` is therefore called once, after the unwind rather than in the middle of it, which is what `RA-114` and `RA-117` both count.","replace":"`disposeComposition` is therefore attempted once, after the unwind rather than in the middle of it, which is what `RA-114` and `RA-117` both count. `RA-140` through `RA-144` extend that evidence to cleanup completion, effect and rollback precedence, successful handle returns, thrown undefined, and the healthy direction. Release failures are recorded in occurrence order as error-severity `project-release-failed` diagnostics at `dispose`; nested aggregate causes are included in their messages, and hostile formatting cannot escape this owner. Diagnostics remain bounded by the existing capacity and dropped-count contract. The original operation's error is neither wrapped nor mutated. Even a direct `dispose()` returns void rather than introducing a second policy; callers inspect diagnostics after disposal."}]}