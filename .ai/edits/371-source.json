{
  "version": 1,
  "expected_head": "f30a6966ff2910f67e2693b5ef92430f521f7fcb",
  "expected_blobs": {
    "packages/core/src/runtime/project-runtime.ts": "c99889d1f0926b1e841d349f9756f3afa02668cb",
    "packages/core/src/runtime/project-runtime.md": "3b9d23a1fcd7a8d013b9fe764fefe3f8fda07480",
    "docs/ADR-080-empty-invalidation-answers-rather-than-publishes.md": null,
    "docs/LIVE-EDIT-COST.md": "b9fdf688b6ddce351647707c30bcb151706924ab",
    "docs/SESSION-STATUS.md": "45640873642092f96d1293d113d2e2d72d8c09c1"
  },
  "message": "fix(runtime): an empty invalidation is answered rather than published, issue #371",
  "edits": [
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  invalidate(nodeIds: readonly string[]) {\n    this.#assertLive();\n    this.#refuseReentrant(\"invalidate\");\n    return this.#invalidateSeeds(nodeIds);\n  }",
      "replace": "  /**\n   * Publishes for `nodeIds` and answers the batch that publication produced.\n   *\n   * A list that names nothing is answered rather than published, through the one owner of what an\n   * empty seed list means, which is the member `values(recipe)` publishes through too. An empty\n   * list still opens a batch, notifies every batch subscriber and moves the sequence, and a caller\n   * that asked for nothing to be invalidated is owed none of that. What a caller can read is\n   * unchanged: this verb always answers a real `PatchBatch`, and for an empty list that batch\n   * carries the sequence the graph is already on. See ADR-080 and issue #371.\n   */\n  invalidate(nodeIds: readonly string[]) {\n    this.#assertLive();\n    this.#refuseReentrant(\"invalidate\");\n    return this.#publishSeeds(nodeIds);\n  }"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "The one owner of what an empty seed list means, and the member values publishes through. A list that names nothing is not a publication, because even an empty batch opens one, notifies every batch subscriber and advances sequence, so it answers the empty batch rather than making one. A list that names anything goes to #invalidateSeeds.",
      "replace": "The one owner of what an empty seed list means, and the member every seed list a caller supplies is published through. A list that names nothing is not a publication, because even an empty batch opens one, notifies every batch subscriber and advances sequence, so it answers the empty batch rather than making one. A list that names anything goes to #invalidateSeeds. Three callers and one rule: values publishes through it for a recipe that staged nothing, public invalidate publishes through it for a caller that named nothing, and #flush delegates to it after answering its own condition. Public invalidate reached #invalidateSeeds directly until issue #371, which is how the one verb a caller can hand an empty list paid a whole publication for one while the rule refusing that cost lived a single call away."
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "Public invalidate is deliberately not folded into #flush: a caller may pass an empty list and still be answered the batch that publication produced, so folding it would change a public answer and make a public verb's return optional, which is a second failure contract rather than a deduplication. See ADR-079.",
      "replace": "Public invalidate reaches this member through #publishSeeds rather than directly, so the empty list it may be handed costs the construction of a batch rather than the publication of one. ADR-079 read that cost as the reason the verb could not move; issue #371 read the same cost as the defect, and both readings agree on the half that survives: folding it into #flush is still refused, because #flush answers nothing and skips an empty list, so a fold would make a public verb's return optional and hand its callers a second failure contract rather than remove a duplicate. See ADR-079 and ADR-080."
    },
    {
      "path": "docs/LIVE-EDIT-COST.md",
      "find": "so a commit with nothing to publish does not call `invalidate` at all.",
      "replace": "so a commit with nothing to publish does not call `invalidate` at all. Public `invalidate(nodeIds)` reads that same rule from the caller's side: a list that names nothing is answered with an empty batch carrying the sequence the graph is already on, rather than paying a publication, a notification for every batch subscriber and a sequence move for an invalidation that touches nothing, and a list that names anything is unchanged in every respect. See ADR-080."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Inspected main:** `6674feb59ea8cc8a543ab6f35cf7cbf2f9b96c47`, after the value-tier batch [#368](https://github.com/chahyasantoso/motion5/pull/368). Exact CI and operation evidence belong to their PRs.",
      "replace": "- **Inspected main:** `b49aec6908e753927303aad5fadd8782fec7600e`, after the publication-owner fix [#370](https://github.com/chahyasantoso/motion5/pull/370). Exact CI and operation evidence belong to their PRs."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "Its remainders are the `CITATION_PENDING` ratchet and [#369](https://github.com/chahyasantoso/motion5/issues/369).",
      "replace": "Its remainders are the `CITATION_PENDING` ratchet and [#371](https://github.com/chahyasantoso/motion5/issues/371); the six findings [#369](https://github.com/chahyasantoso/motion5/issues/369) deferred landed whole in [#370](https://github.com/chahyasantoso/motion5/pull/370) under ADR-079, so `#invalidateSeeds` is the one place a seed list becomes a published, recorded batch, `#publishSeeds` owns what an empty list means, `#flush` answers nothing, and that issue is closed."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Unmerged batch follow-ups:** [#370](https://github.com/chahyasantoso/motion5/pull/370) implements the five findings [#369](https://github.com/chahyasantoso/motion5/issues/369) deferred from #368's review. `#invalidateSeeds` becomes the one place a seed list becomes a published, recorded batch where three copies stood, `#flush` answers nothing rather than an `undefined` that meant a disposed runtime and an empty list alike, `#publishSeeds` owns what an empty list means and asserts its own precondition, `#refuseValueReentrant` is renamed `#refuseImmediateReentrant`, `#teardown` clears the batch's seed list beside the recipe pair, both value-batch factories are compiler-checked rather than cast, and `RA-170` covers `setKeyframe`'s recompile ending inside a batch, which is the path the shipped consumer takes and the one no run had driven. Public `invalidate` is deliberately not folded into `#flush`. No public surface moves. ADR-079 records it and amends ADR-078's first decision. Review, exact-head CI, and merge remain gates; no landed change is claimed.",
      "replace": "- **Unmerged empty-invalidation cost:** [#372](https://github.com/chahyasantoso/motion5/pull/372) implements [#371](https://github.com/chahyasantoso/motion5/issues/371): public `invalidate` publishes through `#publishSeeds` rather than through `#invalidateSeeds`, so a list that names nothing is answered with the empty batch at the sequence the graph is already on instead of paying a publication, a notification for every batch subscriber and a sequence move for an invalidation that touches nothing. One line of source and one public docblock, no new member and no public surface: the verb still always answers a real `PatchBatch`, and it has no caller in either application or in `packages/react`, so this is cost hygiene on unused public API rather than a behaviour fix. `RA-171` is red on the evidence commit. ADR-080 records it and retires the premise ADR-079's third decision reasoned from without reversing that decision, because `#flush` answers nothing and folding a public verb into it stays refused. Review, exact-head CI, and merge remain gates; no landed change is claimed."
    },
    {
      "path": "docs/ADR-080-empty-invalidation-answers-rather-than-publishes.md",
      "create": "# ADR-080: An empty invalidation is answered rather than published\n\n**Status:** Accepted, 2026-09-14\n\n**Context.** [Issue #371](https://github.com/chahyasantoso/motion5/issues/371) traced `ProjectRuntime.invalidate([])` at `b49aec6908e753927303aad5fadd8782fec7600e` and found it paying for a real publication. `#invalidateSeeds` calls `this.#graph.invalidate(nodeIds)`, `GraphRuntime.invalidate` delegates to `flush`, and `flush` moves `#sequence`, raises `#flushing`, derives the snapshot and calls the publisher, with no empty short-circuit anywhere in the chain, so an invalidation that touches nothing opens a batch, notifies every batch subscriber and moves the sequence. This project had already decided that question twice: ADR-064 for a commit with no seeds, and ADR-078 for a recipe that staged nothing. [#370](https://github.com/chahyasantoso/motion5/pull/370) gave the decision one owner, `#publishSeeds`, which answers `emptyValueBatch(this.#graph.sequence)` for an empty list and hands anything else to the mechanics. Public `invalidate` is the only verb that takes a caller's seed list and does not reach that owner.\n\n**Decision.** Public `invalidate(nodeIds)` publishes through `#publishSeeds` rather than through `#invalidateSeeds`. It keeps its liveness assertion, keeps the reentrancy refusal that makes it `value-batch-immediate` inside a batch, and keeps answering a real `PatchBatch`. What changes is that the empty case is constructed rather than published, decided by the member that already owns what an empty seed list means.\n\n**Why not the spelling the issue asked for.** The finding proposes that the verb \"check for an empty list up front and return the equivalent\", and separately excludes folding it into `#flush`/`#publishSeeds` as already refused. Those two members answer differently and the exclusion holds for only one of them. `#flush` returns `void` and skips an empty list, so a fold into it would change a public answer and make a public verb's return optional, which is ADR-079's third decision and is not disturbed here. `#publishSeeds` returns a `PatchBatch` unconditionally and answers the empty one for an empty list, which is exactly the contract the issue asks to preserve: always a real batch, cheap to construct rather than cheap to skip. So the route the issue excluded is the route that satisfies the requirement it states, and the up-front check it proposed is what gets refused: it would put a second copy of an existing rule into the file whose previous slice existed to delete the third copy of a two-statement pair. Two owners for one question is the defect this project cares about most, and one of them being three lines long does not make it a smaller one.\n\n**Why this is not fixed in `GraphRuntime`.** Short-circuiting `GraphRuntime.flush([])` or `GraphRuntime.invalidate([])` looks like the more general form of the same fix and would delete a working mechanism. `#scheduleDrain` calls `this.flush([])` precisely so that a deferred drain publishes what `#pendingSeeds` carried, and `flush` computes `effectiveSeeds` as the union of its argument and those carried seeds. An empty argument at that layer does not mean nothing to publish; it means publish whatever was queued. The emptiness of a caller's list is a project-tier question, and the project tier is where it already has an owner.\n\n**What ADR-079 said, and what survives.** ADR-079's third decision refused the fold and reasoned from a premise: `invalidate([])` publishes today and answers the batch that publication produced. The premise was an accurate reading of the tree it measured and was never itself a decision. This record retires it rather than reversing the decision it supported: the fold into `#flush` stays refused, for the part of the reasoning that does not depend on what the empty case costs, which is that a public verb whose return became optional would hand its callers a second failure contract. A reader arriving at ADR-079's third decision should read the sentence about what `invalidate([])` costs as true of that head and superseded here.\n\n**Consequences.** One observable change, and it is the point of the issue: `invalidate([])` no longer calls the publisher, no longer notifies subscribers and no longer advances `sequence`, and the batch it answers carries the sequence the graph is already on rather than the one a publication would have moved to. Nothing else moves. No exported type, member, refusal, diagnostic or rule id changes, no member is added or deleted, and `#invalidateSeeds` remains the only place in the file that names `this.#graph.invalidate(`, which is what `LV-5` asserts. The verb has no caller under `apps/` or in `packages/react`, and no test drove the empty case before this slice, so nothing shipped changes behaviour and the correction is priced as hygiene on unused public API.\n\n`RA-171` is the evidence, red at the source head on an assertion rather than on a missing member: it drives `invalidate([])` and asserts the publisher was not called, the sequence did not move, and the batch is empty at the current sequence, then drives `invalidate([\"rig/a\"])` in the same rig so that a verb answering an empty batch for every list fails it too.\n\n**Alternatives refused.** An up-front empty check inside `invalidate`, refused above as a second owner. A short-circuit inside `GraphRuntime`, refused above as the deletion of the deferred drain. Leaving it alone as the issue's own scope paragraph suggests, on the ground that no caller exists: a public verb with no callers is where a cost rule goes unnoticed, and the fix is one call rather than a member, so the cheaper thing to do is the correct one. Adding a standing rule to `docs/GUARDRAILS.md`: the rule is already there and in ADR-064, and what this slice found is a verb that never reached it, so a new bullet would restate a rule rather than earn one.\n"
    }
  ]
}
