{
  "version": 1,
  "expected_head": "a5dbbd6c8d02ea15ee8ef5f1f2f2bf8d78d4a9fd",
  "expected_blobs": {
    "packages/core/src/contract/migrate-v4-to-v5.ts": "c4f6d11e6360cd2256f00f85ab89cd9de78a2922",
    "packages/core/test/contract/rule-id.test.ts": "5680b52b4e4d6585dda05122747f73b8f54da10d"
  },
  "message": "Hold the migration rule in a constant, and read the aliased constructor spelling as a construction position",
  "edits": [
    {
      "path": "packages/core/src/contract/migrate-v4-to-v5.ts",
      "find": "import { diagnostic as buildDiagnostic } from \"./diagnostics\";\nimport type { MigrationDiagnostic } from \"./v5\";",
      "replace": "import { diagnostic as buildDiagnostic } from \"./diagnostics\";\nimport type { RuleId } from \"./rule-id\";\nimport type { MigrationDiagnostic } from \"./v5\";"
    },
    {
      "path": "packages/core/src/contract/migrate-v4-to-v5.ts",
      "find": "// The rule id, held once, and nothing else. This built a frozen object of its own and spelled its own",
      "replace": "// Held rather than spelled at the call, the shape `graph/order.ts`, `graph/references.ts`\n// and `runtime/report.ts` already use. `satisfies` is why: it refuses an unenumerated id\n// where the rule is named, which is a compile-time proof beside the coverage scan in\n// `test/contract/rule-id.test.ts` rather than a replacement for it. That scan cannot read\n// a first argument in this file, because the wrapper below is path-first and shares the\n// constructor's name, so holding the id is what keeps this module's one rule inside the\n// measurement. Routing the wrapper through the constructor deleted the `ruleId:` property\n// the scan used to read here, and the gate caught it rather than reporting coverage it had\n// stopped testing.\nconst MIGRATION_RULE = \"schema-v4-migration\" satisfies RuleId;\n\n// The rule id, held once, and nothing else. This built a frozen object of its own and spelled its own"
    },
    {
      "path": "packages/core/src/contract/migrate-v4-to-v5.ts",
      "find": "  return buildDiagnostic(\"schema-v4-migration\", path, message);",
      "replace": "  return buildDiagnostic(MIGRATION_RULE, path, message);"
    },
    {
      "path": "packages/core/test/contract/rule-id.test.ts",
      "find": "// construction site. `contract/migrate-v4-to-v5.ts` is excluded from the first-argument form on\n// purpose: its private `diagnostic(path, message, ids)` puts the path first, and its one id is\n// caught by the assigned form instead.",
      "replace": "// construction site. `contract/migrate-v4-to-v5.ts` is excluded from the first-argument form on\n// purpose: its private `diagnostic(path, message)` puts the path first, and its one id is caught\n// by the held form instead, which is why that module holds its rule in a constant.\n//\n// `buildDiagnostic` is a constructor position too. It is not a second constructor: it is what a\n// module that already binds `diagnostic` to a producer of its own calls the one in\n// `contract/diagnostics.ts`, because an import cannot shadow a local declaration. A scan that\n// knew only the exported spelling went blind the moment a raw literal was routed through the\n// alias, and went blind silently for every id but the one asserted below by name."
    },
    {
      "path": "packages/core/test/contract/rule-id.test.ts",
      "find": "// constant and assign the constant, so nine ids reached `Diagnostic.ruleId` without ever appearing\n// as a literal argument. Those declarations now also state `satisfies RuleId`, so the compiler",
      "replace": "// constant and assign the constant, so nine ids reached `Diagnostic.ruleId` without ever appearing\n// as a literal argument, and `contract/migrate-v4-to-v5.ts` now joins them for a reason of its\n// own, stated above. Those declarations all state `satisfies RuleId`, so the compiler"
    },
    {
      "path": "packages/core/test/contract/rule-id.test.ts",
      "find": "const CONSTRUCTED = /\\b(?:issue|diag|diagnostic|frozenDiagnostic)\\(\\s*\"([a-z][a-z0-9-]*)\"/g;",
      "replace": "const CONSTRUCTED =\n  /\\b(?:issue|diag|diagnostic|buildDiagnostic|frozenDiagnostic)\\(\\s*\"([a-z][a-z0-9-]*)\"/g;"
    },
    {
      "path": "packages/core/test/contract/rule-id.test.ts",
      "find": "      \"clock-tick-regression\",\n      \"diagnostic-sink-failure\",\n    ])",
      "replace": "      \"clock-tick-regression\",\n      \"diagnostic-sink-failure\",\n      \"schema-v4-migration\",\n    ])"
    }
  ]
}
