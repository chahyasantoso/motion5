{
  "message": "docs(adr): record the reverse-topology owner and what a commit costs in front of the graph",
  "target": 267,
  "edits": [
    {
      "path": "docs/ADR-051-derived-solver-membership.md",
      "find": "- **Blend weight (`WT-1`..`WT-16`)**: ADR-055 and the three files it names, which own the seam between an authored rest pose and the solve this record derives.",
      "replace": "- **Blend weight (`WT-1`..`WT-16`)**: ADR-055 and the three files it names, which own the seam between an authored rest pose and the solve this record derives.\n\n---\n\n## Amendment, 2026-09-03: which nodes read one node has one owner, and a removal seeds them\n\nRecorded here as the promotion half of the read-budget audit in [issue #267](https://github.com/chahyasantoso/motion5/issues/267). The reasoning below lived in two docblocks inside `packages/core/src/runtime/project-runtime.ts` and nowhere else, and the audit that shrinks that file would otherwise have deleted the only record of it along with the comments that held it. Nothing about the behaviour changes here.\n\n**One owner, and it is the reverse topology `finalizeGraph` already derived.** `ProjectRuntime.#readersOf` reads `GraphIR.dependents`, which is frozen onto the `GraphIR` once per graph by the walk that already had the solver fan-in in hand, and it is never rederived. The walk it replaced filtered `graph.nodes` on `edges`, one property access away from the frozen answer, and the duplicate was not the defect: it was the weaker of the two derivations. `deriveDependents` names both kinds of reader because only one of them is an edge. A solver reads every member `resolveSolvers` derived onto its `solves` and no edge points that way, so an edge walk missed every member of every chain. Over a live IK chain the query answered nothing for the leaf and named no solver for any member, which is a preflight telling an editor that deleting a chain member breaks nothing.\n\n**Two consumers, one question.** What a caller is told before it removes a node, which is `dependantsOf`, and which nodes have to publish once it did, which is the flush seed a structural commit derives. Those two coincide by construction only while one mechanism answers both, and this file had already paid once for two derivations of exactly this fact, so the second consumer reads the first's owner rather than the graph. Enforcement does not move: a candidate that would orphan a live dependant is still refused by the candidate graph, where ADR-050 put it, and a preflight that is wrong in the permissive direction is worse than none, because it is read.\n\n`#readersOf` stays private, and the commit path deliberately does not call `dependantsOf` instead. That member owns a public contract, which is about a caller from outside, so binding a commit's own invariant to it would make the seed depend on a preflight's promises and point the dependency at the class's own surface rather than away from it. It is not passed in as a parameter either, because the derivation already reads the retained pair off the same object under the same ordering rule, and a parameter for one of two identically ordered reads would claim the two are different.\n\nDeduplication and freezing belong to the consumers rather than to the derivation. `deriveDependents` keeps a reader once per dependency deliberately, because a consumer walking it guards on its own visited set; both consumers here want a set, so the collapse happens where they both are. First occurrence wins, which is the committed node order the old filter produced, so no existing expectation about ordering moves.\n\n**A removal seeds the nodes that were reading it,** not the removed node, which is gone. The premise this replaces said a removal needs no seed at all, because `finalizeGraph` would have refused a candidate anything still depended on, and that premise is half right. It holds for every reader that is an edge: `finalizeGraph` walks every edge of every node rather than only the observation ones, so a removal whose node is still bound by an ordinary `requires` slot is refused before anything commits, which `RA-98` pins. It fails for the reader that is not an edge. A solver reads its chain members through `solves`, and under the bare `target` slot a chain that loses its leaf still has exactly one leaf, so every rule in `resolveSolvers` passes, the commit lands, the solver's member set moves and nothing publishes. On a manual clock with no tick the subscribers hold the old pose forever, which is the failure single-pass publication exists to end, one node downstream.\n\n**The seed is read before the graph is replaced, and that ordering is the whole correctness of it.** The commit path derives before it asks, so this reads the graph the removal is replacing, which is the only one that still knows who read a node it no longer contains. `RA-99` fails if it moves after `replaceGraph`, because the answer becomes empty.\n\nTwo seeds are deliberately not filtered. A reader the same commit also removed is seeded and harmless, because the publisher walks the committed order and a seed the graph does not contain publishes nothing, so a membership test here would be a second opinion about what the committed pair holds. A reader a replacement in the same commit already seeded is seeded twice and harmless, because `flush` collapses its seeds.\n\nEvidence `RA-86` through `RA-88` for the unified derivation, and `RA-98` and `RA-99` for the seed. The flush this seed reaches, and why an empty seed set is not a cheap flush, are [ADR-064](./ADR-064-schema-transaction-derived-hooks.md)'s."
    },
    {
      "path": "docs/ADR-064-schema-transaction-derived-hooks.md",
      "find": "The behaviour is unchanged and has been pinned by `RA-10` since A1. What this amendment adds is the why, so the docblock above `#apply` collapses to a pointer like every other one in that file and loses nothing.",
      "replace": "The behaviour is unchanged and has been pinned by `RA-10` since A1. What this amendment adds is the why, so the docblock above `#apply` collapses to a pointer like every other one in that file and loses nothing.\n\n## Amendment, 2026-09-03: what a commit costs in front of the graph\n\nThe second promotion of the read-budget audit in #267, on the same rule the amendment above was written under: the reasoning lived only in docblocks inside `packages/core/src/runtime/project-runtime.ts`, so the audit that shrinks that file would otherwise have removed the only record of it. The behaviour is unchanged and is pinned by `RA-89` through `RA-97`.\n\nNothing a structural commit costs is priced per motion, per op or per access. Three properties say what that means, and each of them is an ownership statement rather than a measurement.\n\n**The committed pair is walked once.** The snapshot the graph is handed is built from one walk, bucketed by the owner each entry already names, with the free tracks falling out of the same pass rather than out of a second one. A bucket fills in map order, so each motion's list is the list a per-motion filter produced, and a motion that owns nothing answers with an empty list rather than with no key, because that is the document a loader accepts and what a runtime-added Motion starts as. What it replaced asked `#ownedBy` once per motion, and that member materialises the whole track map, so placing V definitions cost `O(M x V)` in time and M arrays of length V in allocation. Every structural commit paid it, in front of a candidate build the incremental builder cache-hits for every untouched node, which is what kept it invisible: the measured wins that came before it were all about the tick, and this is the authoring side.\n\nThat is not a second derivation of the fact the reverse-topology unification collapsed, which is the objection worth answering rather than waving at. Which motion owns a track is a stored field on the entry rather than a derivation, so neither spelling derives anything and both read that field in the same map order. `#ownedBy` stays the owner of the single-motion question, asked by the destroy refusal's count and by both `MotionHandle` members, and `RA-91` asserts the two agree against all three of its remaining readers.\n\n**The pair is adopted by pointer.** Adoption used to read both retained maps out, clear them, and `set` every entry back, so a commit that moved one entry paid `O(V)` map writes and paid them for the half of the schema that did not move at all: a one-track edit on a rig of 600 tracks rewrote 600 track entries and every motion beside them. The read-out existed for a real hazard, because a plan builder hands its own live map straight through for an untouched half and clearing the source mid-iteration would have emptied it. Adopting the object answers that hazard by construction instead: an untouched half is the map the class is already holding, so its assignment is a no-op, and a half that moved is a map one of the two staging accessors made for this commit and nothing else holds.\n\nWhat that costs is the ownership change, and it is why the retained pair is no longer `readonly`: a plan builder may not keep what it handed over, and `#stageTracks` and `#stageMotions` are the only two things allowed to make one. Each half of a `SchemaPlan` is therefore optional, and a half a commit did not move is absent rather than handed back, so an absent half resolves to the map the class already holds and the adoption assigns it back to itself. `RA-92` and `RA-97` pin it, and every untouched entry's definition is handed through by identity, which is ADR-058's cache-hit rule enforced rather than amended.\n\n**A recipe copies each half once, after the last refusal.** A half starts as the retained map by identity and is replaced by a copy on the first op that stages into it, so n ops cost one copy rather than n, and a recipe that staged nothing is still answered by identity on the pair rather than by a dirty flag, which is the property the decision above already relies on. The copy is therefore taken after an entry point's last refusal and never before it: a copy made for an op that then refused would leave the recipe holding a pair that differs from the retained one while holding exactly the same entries, and `edit` would spend a candidate build and a flush committing it. `RA-95` is that case, in both directions.\n\nOutside a recipe the copy is the floor rather than an expense to remove, because the derivation compares the committed pair against the retained one, so the pair the graph is asked about cannot be the pair the class is still holding."
    }
  ]
}
