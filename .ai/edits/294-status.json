{
"message": "docs(status): the alternation ordering correction has landed",
"target": 294,
"edits": [
{"path": "docs/SESSION-STATUS.md", "find": "`23e6afa` on `main`, which is [#286](https://github.com/chahyasantoso/motion5/pull/286) merged", "replace": "`06388bd` on `main`, which is [#293](https://github.com/chahyasantoso/motion5/pull/293) merged"},
{"path": "docs/SESSION-STATUS.md", "find": "- **Just landed.** The evidence-id gate refuses a series its pattern cannot see,", "replace": "- **Just landed.** The alternation's ordering has one owner and a true reason, [issue #294](https://github.com/chahyasantoso/motion5/issues/294). Fourteen series paragraphs in `evidence-case-ids.test.ts` each claimed its own place in the pattern was forced, because an alternation offering the single letter first would match it and then fail, and `AE-` inherited the claim as the one widening with no ordering hazard to avoid. Every alternative is followed by the same hyphen-and-digits suffix, so the engine retries the next alternative at the same position and `CF-3` matches whichever way round `C` and `CF` are written: all sixteen two-letter-and-initial pairs the pattern carries are free. The fifteen claims are deleted and one paragraph beside the pattern owns the ordering, so a series paragraph now says what its series owns and nothing about where it sorts. Comment and status only, no behaviour, and it earns no ADR: the gate's own header owns the pattern.\n- **Landed before it.** The evidence-id gate refuses a series its pattern cannot see,"},
{"path": "docs/SESSION-STATUS.md", "find": "\n- **Landed before it.** The `AE-` series is gated for uniqueness, [issue #283](https://github.com/chahyasantoso/motion5/issues/283). Nineteen ids were declared in `apply-ai-edit.test.ts`, cited by `docs/AI-EDIT-WORKFLOW.md` and by the header of the script they cover, and absent from the pattern `evidence-case-ids.test.ts` scans, so nothing would have caught a second `AE-7`. It widened the alternation by hand and left the alternation itself ungated, which is the half the slice above finishes, and the guardrail it earned is the last bullet of [GUARDRAILS.md](./GUARDRAILS.md): an allowlist is the thing that needs a gate.\n\n## Next in line", "replace": "\n\n## Next in line"},
{"path": "docs/SESSION-STATUS.md", "find": "two invariants with two cases.\n- **Fifteen series paragraphs give a false reason for the order of the alternation.** Each two-letter series in `evidence-case-ids.test.ts` states that it sorts before its own initial because an alternation offering `C` first would match `C` and then fail on the `F`. A backtracking engine retries the next alternative, so `/(?:C|CF)-\\d+/` matches `CF-3` and every one of the fifteen orderings is free. Measured against all fifteen pairs while the gate above was written, and deliberately not in it: the ordering the paragraphs ask for is harmless and the reason they give for it is wrong, so the correction is prose in fifteen places and no behaviour.\n", "replace": "two invariants with two cases.\n"}
]
}
