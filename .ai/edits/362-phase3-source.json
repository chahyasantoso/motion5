{
  "version": 1,
  "expected_head": "3f492cd76c906effe2e275afbd9b8ddf018963e4",
  "expected_blobs": {
    "packages/core/src/runtime/project-runtime.ts": "45e48040d54309d84574b25828d6c13c7181fa1f",
    "packages/core/src/runtime/project-runtime.md": "fba7a6174be59a435ccd85e9f3771a1f164f432b",
    "packages/core/src/engine.ts": "18da6f3439a3244be1264240066a1c4eca381b22",
    "packages/core/src/contract/schema-transaction.ts": "27336a29ca4a1d0e55fd7eb2a5ac1c737cfc9a4e",
    "packages/core/test/integration/project-handle-surface.test.ts": "07e15e2a4d757464107722f71f556203cf536c57"
  },
  "message": "feat(runtime): delete the duplicate subscribe, adopt and destroyAdopted members",
  "edits": [
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "type TrackEntry = {\n  track: TrackDefinition;\n  owner: object;\n  motionId?: string;",
      "replace": "type TrackEntry = {\n  track: TrackDefinition;\n  motionId?: string;"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #tracks = new Map<string, TrackEntry>();\n  #motions = new Map<string, MotionEntry>();\n  readonly #schemaOwner = {};\n  #nextToken = 1;",
      "replace": "  #tracks = new Map<string, TrackEntry>();\n  #motions = new Map<string, MotionEntry>();\n  #nextToken = 1;"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "        this.#tracks.set(qualifyMotionTrack(motion.id, track.id).value, {\n          track,\n          owner: this.#schemaOwner,\n          motionId: motion.id,",
      "replace": "        this.#tracks.set(qualifyMotionTrack(motion.id, track.id).value, {\n          track,\n          motionId: motion.id,"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "      this.#tracks.set(qualifyFreeTrack(track.id).value, {\n        track,\n        owner: this.#schemaOwner,\n        token: this.#nextToken++,",
      "replace": "      this.#tracks.set(qualifyFreeTrack(track.id).value, {\n        track,\n        token: this.#nextToken++,"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  addTrack(track: TrackDefinition, options?: { motionId?: string }): TrackHandle {\n    return this.#addTrack(track, this.#schemaOwner, options);\n  }",
      "replace": "  addTrack(track: TrackDefinition, options?: { motionId?: string }): TrackHandle {\n    return this.#addTrack(track, options);\n  }"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  adopt(\n    track: TrackDefinition,\n    owner: object,\n    options?: { motionId?: string },\n  ): { readonly id: string; readonly track: TrackDefinition } {\n    const handle = this.#addTrack(track, owner, options);\n    return Object.freeze({ id: handle.id, track: handle.definition });\n  }\n  destroyAdopted(nodeId: string, owner: object): void {\n    this.#assertLive();\n    const entry = this.#readTracks().get(nodeId);\n    if (!entry || entry.owner !== owner)\n      throw new TypeError(\n        !entry\n          ? `Node \"${nodeId}\" is not adopted.`\n          : `Only the adopting owner can destroy \"${nodeId}\".`,\n      );\n    this.#removeTrack(nodeId, entry.token);\n  }\n  /**\n   * Every node that reads this one",
      "replace": "  /**\n   * Every node that reads this one"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "  #addTrack(track: TrackDefinition, owner: object, options?: { motionId?: string }): TrackHandle {",
      "replace": "  #addTrack(track: TrackDefinition, options?: { motionId?: string }): TrackHandle {"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "    tracks.set(id, {\n      track: accepted,\n      owner,\n      motionId,",
      "replace": "    tracks.set(id, {\n      track: accepted,\n      motionId,"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.ts",
      "find": "      addTrack: (track: TrackDefinition) =>\n        runtime.#addTrack(track, runtime.#schemaOwner, {\n          motionId: runtime.#writableId(id, token),\n        }),",
      "replace": "      addTrack: (track: TrackDefinition) =>\n        runtime.#addTrack(track, { motionId: runtime.#writableId(id, token) }),"
    },
    {
      "path": "packages/core/src/runtime/project-runtime.md",
      "find": "The retained track, adopting owner, optional motion owner,",
      "replace": "The retained track, optional motion owner,"
    },
    {
      "path": "packages/core/src/engine.ts",
      "find": "  dependantsOf(nodeId: string): readonly string[];\n  subscribe(nodeId: string, listener: PatchListener): () => void;\n  get(nodeId: string): Patch | undefined;",
      "replace": "  dependantsOf(nodeId: string): readonly string[];\n  get(nodeId: string): Patch | undefined;"
    },
    {
      "path": "packages/core/src/engine.ts",
      "find": "  renderMetadata(nodeId: string): RenderMetadata | undefined;\n  adopt(\n    track: TrackDefinition,\n    owner: object,\n    options?: { motionId?: string },\n  ): { readonly id: string; readonly track: TrackDefinition };\n  destroyAdopted(nodeId: string, owner: object): void;\n  dispose(): void;",
      "replace": "  renderMetadata(nodeId: string): RenderMetadata | undefined;\n  dispose(): void;"
    },
    {
      "path": "packages/core/src/engine.ts",
      "find": "    subscribe: (nodeId, listener) => runtime.graph.registry.subscribeNode(nodeId, listener),\n    get: (nodeId) => runtime.graph.registry.get(nodeId),",
      "replace": "    get: (nodeId) => runtime.graph.registry.get(nodeId),"
    },
    {
      "path": "packages/core/src/engine.ts",
      "find": "    renderMetadata,\n    adopt: (track, owner, options) => runtime.adopt(track, owner, options),\n    destroyAdopted: (nodeId, owner) => runtime.destroyAdopted(nodeId, owner),\n    dispose: () => runtime.dispose(),",
      "replace": "    renderMetadata,\n    dispose: () => runtime.dispose(),"
    },
    {
      "path": "packages/core/src/contract/schema-transaction.ts",
      "find": " * slice is that there is one. What the interface buys is the narrowing, and only that: `mount`,\n * `seek`, `subscribe` and `dispose` are not members of it. A recipe still closes over the\n * `ProjectHandle` it was called on, so what it may not do while one is open is answered by the verbs\n * themselves: every verb that applies immediately refuses by name with\n * `schema-transaction-immediate`, and the two structural verbs absent from here, `adopt` and\n * `destroyAdopted`, compose into the one commit exactly as `addTrack` does.",
      "replace": " * slice is that there is one. What the interface buys is the narrowing, and only that: `mount`,\n * `seek`, `subscribeNode` and `dispose` are not members of it. A recipe still closes over the\n * `ProjectHandle` it was called on, so what it may not do while one is open is answered by the verbs\n * themselves: every verb that applies immediately refuses by name with\n * `schema-transaction-immediate`, while the structural verbs absent from here are\n * `ProjectHandle.addTrack` and `TrackHandle.remove`, which compose into the one commit exactly as\n * this surface's own `addTrack` does."
    },
    {
      "path": "packages/core/test/integration/project-handle-surface.test.ts",
      "find": "    // Enumerated rather than sampled, so a member added to `ProjectHandle` lands here first. The\n    // three resolvers below arrived with the handle base in issue #223, `edit` arrived with slice D\n    // of the same issue, and the three enumeration readers arrived with phase 2 of issue #362;\n    // nothing else moved.\n    expect(Object.keys(runtime).sort()).toEqual([\n      \"addMotion\",\n      \"addTrack\",\n      \"adopt\",\n      \"dependantsOf\",\n      \"destroyAdopted\",\n      \"destroyMotion\",",
      "replace": "    // Enumerated rather than sampled, so a member added to `ProjectHandle` lands here first. The\n    // three resolvers below arrived with the handle base in issue #223, `edit` arrived with slice D\n    // of the same issue, and the three enumeration readers arrived with phase 2 of issue #362.\n    // Phase 3 subtracted three: `adopt` and `destroyAdopted`, whose question `addTrack` and\n    // `TrackHandle.remove` already answered, and `subscribe`, which was `subscribeNode` under an\n    // older name. Nothing else moved.\n    expect(Object.keys(runtime).sort()).toEqual([\n      \"addMotion\",\n      \"addTrack\",\n      \"dependantsOf\",\n      \"destroyMotion\","
    },
    {
      "path": "packages/core/test/integration/project-handle-surface.test.ts",
      "find": "      \"signal\",\n      \"subscribe\",\n      \"subscribeNode\",",
      "replace": "      \"signal\",\n      \"subscribeNode\","
    }
  ]
}
