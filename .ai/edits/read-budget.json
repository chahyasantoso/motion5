{
  "message": "chore: record the read budget rule, its ADR-008 amendment and its case series",
  "target": 267,
  "edits": [
    {
      "path": "docs/AI-EDIT-WORKFLOW.md",
      "find": "## What it will not do",
      "replace": "## Reading the region, and the asymmetry this contract has\n\nThe rule above says read the region before you write the anchor, and it does not say how a region is read, because there is no way to read one. A contents read is whole-file or prefix: no offset, no line window, and no second call that returns the part the first one dropped. A file past the response cap arrives truncated, and its tail is not late, it is unreachable.\n\nThat is the asymmetry, and it is the whole of why this section exists. The write side is size-independent by construction, because you never write the file back. The read side is size-bound with no lever except the size of the file itself, which is the one variable in the loop that anybody controls.\n\nCode search is a locator rather than a reader, and it does not close the gap. It will tell you a symbol exists and print a few lines around it, and it will not hand you the region: it caps fragments per file and answers against the indexed default branch, so on the working branch you are editing it is stale or blind.\n\nThe consequence for the anchor rule is exact. An anchor must match exactly once **in the file**, and a truncated read cannot establish that. It can only establish uniqueness across the prefix that arrived. An anchor unique in the prefix and repeated in the invisible tail comes back as the two-match refusal, which fails safe and costs one round trip. An anchor that genuinely is unique applies cleanly, all-or-nothing, exactly as designed, while the reasoning behind the edit was checked against a prefix. The write pass validates the anchor against the whole file, and nothing validates the edit against the part you never read, so every safety property here stays intact while checking a different thing.\n\n**A file that does not survive one read may not be edited by anchor. Split it first.** `npm run test:read-budget` is how you find out which side of that line a file is on, and `scripts/read-budget-scan.mjs` carries the measured numbers, the budget they justify, and every file still over it.\n\n## What it will not do"
    },
    {
      "path": "docs/DECISIONS.md",
      "find": "**Consequences.** Review must explain architecture, while tests prove behavior. A banned symbol scan may enforce a boundary, but it cannot replace an integration test.",
      "replace": "**Consequences.** Review must explain architecture, while tests prove behavior. A banned symbol scan may enforce a boundary, but it cannot replace an integration test.\n\n**Amended, 2026-09-03 (issue #267).** A byte budget on a source file belongs to the allowed family above rather than being an exception to it, and the amendment is recorded here because the context above names a non-shrinking file allowlist as one of the things this record refuses. Neither hazard that sentence describes reaches a read budget. It cannot be green while the runtime is wrong, because it makes no claim about the runtime at all, and it cannot block a legitimate deletion, because every deletion moves a file the way the budget wants. What it measures is not a proxy for correctness: it is the toolchain precondition an anchor-based edit already depends on, which puts it beside import boundaries and mechanical formatting rather than beside a comment ratio. The measured evidence is that `packages/core/src/runtime/project-runtime.ts` truncates on a single contents read, so an implementor with no local checkout can edit a method by anchor without ever seeing an invariant written earlier in the same file.\n\n**The warning about allowlists is kept rather than argued with.** A waiver in `scripts/read-budget-scan.mjs` carries the file's exact current size as a ceiling it may not exceed, so the list can only shrink, and an expiry date the scan fails on by itself, so a waiver is deleted by a check rather than renewed by silence. A waiver with neither property is the gate this record was right to refuse. A comment ratio stays refused outright and is not what this measures: 43 percent of that file being comment is a symptom, and a percentage adopted as a target is met by deleting whichever comments are easiest rather than whichever are redundant."
    },
    {
      "path": "packages/core/test/unit/scripts/evidence-case-ids.test.ts",
      "find": "// and then fail on the `A`.",
      "replace": "// and then fail on the `A`.\n//\n// `RB-` belongs to the read budget on a source file, issue #267: that a file over the measured\n// budget is refused, that a file carrying a recorded waiver is held to its own ceiling instead,\n// that the waiver expires on a date rather than by review, and that a scan which found nothing\n// says so rather than answering clean. Every series above owns something the runtime does or\n// something a caller is told; `RB-` owns whether the file that states an invariant can be read at\n// all by the implementor about to edit it, which is a question none of them ask. It is the\n// eighteenth two-letter series, under the policy the `P-` paragraph sets, and `R-` alone is the\n// progress range fix. It sorts before `R` in the pattern below, because an alternation that\n// offered `R` first would match `R` and then fail on the `B`."
    },
    {
      "path": "packages/core/test/unit/scripts/evidence-case-ids.test.ts",
      "find": "PK|PV|RA|RS|SH|WT",
      "replace": "PK|PV|RA|RB|RS|SH|WT"
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Still open, and not part of any slice above.**",
      "replace": "- **Just opened.** The read budget on a source file, [issue #267](https://github.com/chahyasantoso/motion5/issues/267). `packages/core/src/runtime/project-runtime.ts` is 103,657 bytes and does not survive one contents read: the tail is dropped, and what the dropped part holds includes `#assertLive`, which every public verb in the class calls first. The read primitive has no offset and no line window, so a lost tail is unreachable rather than late, and code search answers against the indexed default branch rather than the branch being edited, which is why it locates a symbol and cannot hand over a region. `scripts/read-budget-scan.mjs` budgets 60,000 bytes, bounded below the largest file measured to arrive whole and above the largest file here that is not the outlier, and it carries one waiver whose ceiling may only shrink and whose expiry is 2026-10-01. `RB-1` through `RB-8` are its evidence, and the amendment in `docs/DECISIONS.md` records why ADR-008 allows a gate of this shape at all.\n- **What that scan deliberately does not do yet.** It reads `packages/core/src` and no documentation, because a doc read that loses its tail costs orientation while a source read that loses its tail is how an anchored edit lands against an invariant nobody saw. The finding it leaves on the table is this file: 77,978 bytes, the largest text file in the repository, inside the band the response cap was measured to sit in, and describing itself as deliberately small. It is the next thing to shrink rather than the next thing to waive.\n- **Still open, and not part of any slice above.**"
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- Formatting never shares a commit with behavior.",
      "replace": "- Formatting never shares a commit with behavior.\n- A source file under `packages/core/src` may not exceed 60,000 bytes, and `RB-7` fails when one does. That is a read budget rather than a style rule: a file past the contents-API response cap loses its tail to every implementor working without a checkout, and `docs/AI-EDIT-WORKFLOW.md` owns the rule that follows from it, which is that a file which does not survive one read may not be edited by anchor."
    }
  ]
}
