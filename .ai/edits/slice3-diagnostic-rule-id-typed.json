{
  "version": 1,
  "operation": "apply",
  "expected_head": "6198832ad59dfc0c9dc804e9e0427b211fbe1143",
  "expected_blobs": {
    "packages/core/src/contract/v5.ts": "5dd034219f147440ed52ca892a98991adc79ce94",
    "packages/core/src/contract/rule-id.ts": "a06a718edd9b057cb786fefaa2e5e017ed7bb8e6",
    "packages/core/test/integration/p2-runtime-smells.test.ts": "7aa42bfe069b05b9d9dbe271095eb4343d0baa84",
    "packages/core/test/unit/runtime/live-value-updates.test.ts": "28fe250e16d1449ce9b2c971802c691979dfaaf5",
    "packages/core/test/unit/runtime/registry-phase.test.ts": "8796f40e930f1a5b008b2c35be6f537e17ad3edd",
    "packages/core/test/unit/runtime/diagnostics-ring.test.ts": "af8ee86f785e61ca00e012cb8129b724723ef601",
    "packages/core/test/unit/runtime/report.test.ts": "74b9d2d03461d3ca104963a90a51e3fa74a78922",
    "docs/ADR-097-a-rule-id-is-a-closed-union.md": "549b1d9ab96b5e11f96eaf8103f102885582aa80",
    "docs/SESSION-STATUS.md": "07945de73a226715ed83677bbf2cf0ca1809deda"
  },
  "message": "feat(contract): type Diagnostic.ruleId as the closed RuleId union",
  "edits": [
    {
      "path": "packages/core/src/contract/v5.ts",
      "find": "export const AUTHORED_SCHEMA_VERSION = 5 as const;",
      "replace": "import type { RuleId } from \"./rule-id\";\n\nexport const AUTHORED_SCHEMA_VERSION = 5 as const;"
    },
    {
      "path": "packages/core/src/contract/v5.ts",
      "find": "export interface Diagnostic {\n  readonly ruleId: string;",
      "replace": "/**\n * One refusal, named by the rule it refuses under.\n *\n * `ruleId` is `RuleId` and not `string`, so this set is closed on both sides: nothing can mint a\n * rule the enumeration does not carry, and a reader can switch on one. Adding a rule breaks the\n * build in `contract/rule-id.ts`, at the one place the rule is named, rather than being inherited\n * silently by every consumer that pattern-matches a string. That is the acceptance criterion issue\n * #449 asks for, and it is a public surface change: `Diagnostic` is exported from the package\n * entry, so a consumer constructing one with a rule id of its own invention stops compiling.\n * See ADR-097.\n */\nexport interface Diagnostic {\n  readonly ruleId: RuleId;"
    },
    {
      "path": "packages/core/src/contract/v5.ts",
      "find": "export interface MigrationDiagnostic extends Diagnostic {",
      "replace": "/**\n * A migration diagnostic, pinned to the one rule the v4 reader refuses under.\n *\n * The literal narrows `Diagnostic.ruleId` rather than sitting beside it, so `schema-v4-migration`\n * is provably a member of `RuleId`: were the enumeration to stop carrying it, this declaration is\n * the build failure rather than a string nobody checks. See ADR-097.\n */\nexport interface MigrationDiagnostic extends Diagnostic {"
    },
    {
      "path": "packages/core/src/contract/rule-id.ts",
      "find": "import type { Diagnostic } from \"./v5\";\n\n/**\n * Every rule id `Diagnostic.ruleId` can carry, as one closed union.",
      "replace": "/**\n * Every rule id `Diagnostic.ruleId` can carry, as one closed union."
    },
    {
      "path": "packages/core/src/contract/rule-id.ts",
      "find": " * This module is not re-exported from the package entry, so it adds no entry to the export map or\n * to the boundary allow-list. `Diagnostic.ruleId` still declares `string` at this slice: the\n * enumeration is proved complete by `packages/core/test/contract/rule-id.test.ts` before anything\n * depends on it.",
      "replace": " * This module is not re-exported from the package entry, so it adds no entry to the export map or\n * to the boundary allow-list. `Diagnostic.ruleId` declares `RuleId`, so the compiler answers for\n * every id named at a construction site, and this module owns no import from `./v5`: a narrowed\n * alias of a type that already carries the narrowing was the one thing it held, and it is gone.\n * The coverage case in `packages/core/test/contract/rule-id.test.ts` answers for the shape a type\n * cannot see, which is an id assembled at run time, and it asserts coverage rather than a count."
    },
    {
      "path": "packages/core/src/contract/rule-id.ts",
      "find": " * Every other rule id, sorted, from all seventeen construction modules.",
      "replace": " * Every other rule id, sorted.\n *\n * No module count is stated. Three inventories of this union disagreed about that number, in both\n * directions, and a figure in a comment is a claim no gate reads: the coverage case owns it."
    },
    {
      "path": "packages/core/src/contract/rule-id.ts",
      "find": "\n/** `Diagnostic` with its rule id narrowed, for a reader that has already proved the id. */\nexport type IdentifiedDiagnostic = Diagnostic & { readonly ruleId: RuleId };",
      "replace": ""
    },
    {
      "path": "packages/core/test/integration/p2-runtime-smells.test.ts",
      "find": "      diagnostics: [{ ruleId: \"test\", path: \"source\", message: \"boom\", severity: \"error\" }],",
      "replace": "      diagnostics: [\n        {\n          ruleId: \"composition-failure\",\n          path: \"source\",\n          message: \"boom\",\n          severity: \"error\",\n          ids: [\"source\"],\n        },\n      ],"
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "      ruleId: \"lv5-probe\",",
      "replace": "      ruleId: \"value-batch-deferred\","
    },
    {
      "path": "packages/core/test/unit/runtime/live-value-updates.test.ts",
      "find": "(entry) => entry.ruleId === \"lv5-probe\"",
      "replace": "(entry) => entry.ruleId === \"value-batch-deferred\""
    },
    {
      "path": "packages/core/test/unit/runtime/registry-phase.test.ts",
      "find": "    ruleId: \"registry-phase\",",
      "replace": "    ruleId: \"composition-failure\","
    },
    {
      "path": "packages/core/test/unit/runtime/diagnostics-ring.test.ts",
      "find": "    ruleId: \"diagnostics-ring\",",
      "replace": "    ruleId: \"flush-failure\","
    },
    {
      "path": "packages/core/test/unit/runtime/report.test.ts",
      "find": "import type { Diagnostic } from \"../../../src/contract/v5\";",
      "replace": "import type { RuleId } from \"../../../src/contract/rule-id\";\nimport type { Diagnostic } from \"../../../src/contract/v5\";"
    },
    {
      "path": "packages/core/test/unit/runtime/report.test.ts",
      "find": "function diagnostic(ruleId: string): Diagnostic {",
      "replace": "function diagnostic(ruleId: RuleId): Diagnostic {"
    },
    {
      "path": "packages/core/test/unit/runtime/report.test.ts",
      "find": "    const second = diagnostic(\"diagnostic-sink-failure-2\");",
      "replace": "    const second = diagnostic(\"clock-tick-regression\");"
    },
    {
      "path": "packages/core/test/unit/runtime/report.test.ts",
      "find": "    const nested = diagnostic(\"nested-sink-failure\");\n    const outer = diagnostic(\"outer-sink-failure\");",
      "replace": "    const nested = diagnostic(\"scheduler-failure\");\n    const outer = diagnostic(\"clock-tick-regression\");"
    },
    {
      "path": "docs/ADR-097-a-rule-id-is-a-closed-union.md",
      "find": "- Status: accepted as a decision, enumeration implemented and not merged. This record is intent until the slices below merge, on documentation rule 2 of [README.md](./README.md). The inventory paragraphs below were corrected in place against a second measurement, after the first one was found wrong in five places; the corrections replace the claims rather than being appended beside them.",
      "replace": "- Status: accepted, and implemented on the branch that carries this record. The enumeration exists with a coverage gate, the six generic constructors take `RuleId`, and `Diagnostic.ruleId` is typed by it; the record stays intent as to merge, on documentation rule 2 of [README.md](./README.md). The inventory paragraphs below were corrected in place more than once, and the corrections replace the claims rather than being appended beside them. The totals they used to state are gone, because the gate is the authority a count kept pretending to be."
    },
    {
      "path": "docs/ADR-097-a-rule-id-is-a-closed-union.md",
      "find": "There are 116 distinct ids actually assigned to `Diagnostic.ruleId`, not the handful the issue sketches, and they are constructed in fifteen modules rather than the twelve the first inventory named.",
      "replace": "Many more ids are assigned to `Diagnostic.ruleId` than the handful the issue sketches, and they are constructed across more modules than the twelve the first inventory named. No total is stated, here or anywhere else in this record. Three inventories of this union disagreed with each other, in both directions, so a number written down here would pin the error rather than catch it, and the coverage case in `packages/core/test/contract/rule-id.test.ts` is the authority instead; it asserts coverage and never a total, for the same reason. The modules named below are where a reader looks, not the proof."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Captured:** 2026-09-18, Asia/Jakarta.\n- **Read against:** `6cc4e1fd57722889380e0bbded3b4c4eaa568dd7`, which is the head this rewrite was verified at rather than the head it publishes on; that one belongs to its own pull request, with the exact CI and operation evidence. See ADR-095.",
      "replace": "- **Captured:** 2026-09-19, Asia/Jakarta.\n- **Read against:** `6198832ad59dfc0c9dc804e9e0427b211fbe1143`, which is the head this rewrite was verified at rather than the head it publishes on; that one belongs to its own pull request, with the exact CI and operation evidence. See ADR-095."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Both halves of the runtime redesign are on `main`, and the live area is the quality pass over them:** [#443](https://github.com/chahyasantoso/motion5/issues/443) landed in two halves, so the whole `runtime` folder now states its refusals, results, lifecycle, live writes, authored edits, commit effects, host seams, capability handles, publisher outcomes, registry phase, ordered steps and publication requests as branded unions read by total switches, ADR-094 records it, and no export of `packages/core` moved. Open in review on top of that is one pass closing three findings against it, each of which was one question with two answers. `report.ts` declares `describeWithCauses` beside the imported `describeError` its leaf case delegates to, so the folder no longer holds two functions of one name and no diagnostic message moves. `undeliverable` keeps the handover failure of the newest report the trace retains, so a nested sink failure is no longer overwritten by the outer one unwinding behind it. `deferredScheduler` answers one identity for one condition: a port that runs the callback inline and then throws its own error is answered with the refusal, carrying that error as its `cause`. The status gate reads one normalised subject in both the entry count and the label rule, so a label hidden behind an HTML comment or a character reference is refused rather than rendered. `adr-integrity.test.ts` gives ADR-093's one-file-per-number invariant a mechanism and resolves every relative record link in `docs/`. ADR-095 and ADR-096 record the decisions, with markers on ADR-085, ADR-089, ADR-091, ADR-093 and ADR-094, and the record corrections [#441](https://github.com/chahyasantoso/motion5/issues/441) asked for are in `docs/README.md` and ADR-092. It is not merged, and the runs that measured it belong to that pull request.",
      "replace": "- **A rule id is a closed union, and closing the last side of it is the live area:** [#449](https://github.com/chahyasantoso/motion5/issues/449) is open in review on [#452](https://github.com/chahyasantoso/motion5/pull/452). `contract/rule-id.ts` owns every id `Diagnostic.ruleId` can carry, derives the `plugin-contribution-` family through the one alias map rather than a second hand-maintained list, and `contract/rule-id.test.ts` scans `packages/core/src` and asserts the enumeration covers every id constructed there, including the module constants a literal argument scan cannot see. The six generic constructors take `RuleId` where they took `string`, `validateKeyframes` takes one `scope` where it took a prefix, an alias map and a group flag, and `Diagnostic.ruleId` is `RuleId`, which is the public surface change: a consumer constructing a `Diagnostic` with a rule id of its own invention stops compiling. It is not merged, and the runs that measured it belong to that pull request."
    }
  ]
}
