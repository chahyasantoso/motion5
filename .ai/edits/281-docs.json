{
  "message": "docs(ai-edit): state the edit ceiling and the two path lists",
  "target": 282,
  "edits": [
    {
      "path": "docs/AI-EDIT-WORKFLOW.md",
      "find": "Each entry in `edits` names a `path` and exactly one of three modes: `find` with `replace` to rewrite an anchor, `create` with the full content of a new file, or `delete` set to `true`. Naming two modes is refused rather than guessed at.\n",
      "replace": "Each entry in `edits` names a `path` and exactly one of three modes: `find` with `replace` to rewrite an anchor, `create` with the full content of a new file, or `delete` set to `true`. Naming two modes is refused rather than guessed at.\n\nOne request carries at most **50 edits**, and a request holding more is refused whole, with the count it held. The ceiling is on one request rather than on one slice, so a batch larger than that splits into two pushes and the batch-first rule below is untouched.\n"
    },
    {
      "path": "docs/AI-EDIT-WORKFLOW.md",
      "find": "Before committing, it compares the staged file list against the paths your request named, plus the request file itself, and fails if anything else appears. Least privilege applies to the file list, not only to the token.\n",
      "replace": "Before committing, it compares the staged file list against the paths your request named, plus the request file itself, and fails if anything else appears. Least privilege applies to the file list, not only to the token.\n\nIt formats the paths that still exist and no others. A path a `delete` edit removed stays in the commit allow-list, because `git add -A` would otherwise stage a removal nothing authorised, and it is kept out of the formatter's input, because Prettier exits non-zero on a path that is not there. Those are two questions about one set of paths, so the apply step answers them as two lists rather than one: a request whose every changed path it deleted formats nothing at all and does not install the formatter either. See issue #281.\n"
    },
    {
      "path": "docs/AI-EDIT-WORKFLOW.md",
      "find": "A refusal fails the run, so nothing is committed and your request file stays exactly where you put it. Correct that same path and push it again. An applied request is removed by the commit that consumed it, which is how you tell the two outcomes apart from the tree alone. A dry run is consumed the same way, so the tree does not distinguish it from an apply, and the report and the commit subject are what do.",
      "replace": "A refusal fails the run, so nothing is committed and your request file stays exactly where you put it. Correct that same path and push it again. An applied request is removed by the commit that consumed it, which is how you tell the two outcomes apart from the tree alone. A dry run is consumed the same way, so the tree does not distinguish it from an apply, and the report and the commit subject are what do.\n\nA step failing after the apply step is a third outcome and it reads differently again. The report describes what that step wrote to the runner's working tree, so a failure between there and the commit means none of it landed, and the comment says exactly that above the report: nothing was committed, and your request is still pending at the same path. Read `Applied ... edits` as attempted rather than landed whenever that prefix is there."
    },
    {
      "path": "docs/AI-EDIT-WORKFLOW.md",
      "find": "that a refusal leaves every file it named untouched, and that a dry run gets the fixed `[skip ci]` subject while a real run keeps your `message` verbatim.",
      "replace": "that a refusal leaves every file it named untouched, that a dry run gets the fixed `[skip ci]` subject while a real run keeps your `message` verbatim, and that the formatter is handed only the paths that still exist while the commit allow-list keeps the ones a `delete` edit removed."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "The sentence is there now, beside the mode rules, and it states what the ceiling is on: one request rather than one slice, so a larger batch splits in two and the batch-first rule is untouched.",
      "replace": "This file then claimed the sentence had landed beside the mode rules while the document never carried it, which is the debt-list rule in the guardrails below wearing a different hat: a status entry describing a documentation edit is re-read against the document rather than against itself. [Issue #281](https://github.com/chahyasantoso/motion5/issues/281) lands it, and it states what the ceiling is on: one request rather than one slice, so a larger batch splits in two and the batch-first rule is untouched."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "## Where the work is\n\n- **Just landed.** One spelling for reverse topology,",
      "replace": "## Where the work is\n\n- **Just landed.** The formatter no longer receives a path a request deleted, [issue #281](https://github.com/chahyasantoso/motion5/issues/281). `scripts/apply-ai-edit.mjs` wrote one list of changed paths and `.github/workflows/ai-edit.yml` handed it to both the commit allow-list and Prettier, and those two are asking different questions of it: the allow-list needs every path the request changed, a deletion included, or `git add -A` stages a removal nothing authorised, while Prettier exits non-zero on a path that is not there. So a mixed or delete-only request applied its edits, failed formatting, skipped the commit with the tree already written, and commented `Applied ... edits` with nothing behind it. Run [33778347410](https://github.com/chahyasantoso/motion5/actions/runs/33778347410) is that failure, on the second commit of issue #262's rename. The write pass emits a second list holding only the paths that still exist, the format step reads that one and installs no formatter when it is empty, and the comment now says that nothing was committed whenever a step after the apply step failed. `AE-17` through `AE-19` are its evidence and it earns no ADR: `docs/AI-EDIT-WORKFLOW.md` owns the contract and gained the 50-edit ceiling in the same pull request. The workflow refuses every path under `.github/workflows/`, so the two code files are a hand-pushed pull request and only the documentation half went through `.ai/edits`.\n- **The decision worth carrying forward from it.** Two consumers of one list are two owners of a question neither of them asked. The list was named after what the script did rather than after what a reader needs, so a path that had to be on it for the commit was on it for the formatter too, and the formatter is the one consumer that cannot survive a path that is gone. So a list is named for the question it answers, and a second question gets a second list rather than a filter at the point of use: the filter would have lived in YAML, re-derived from the filesystem what the write pass already knew, and been invisible to the only test that can see any of this.\n- **Just landed.** One spelling for reverse topology,"
    }
  ]
}
