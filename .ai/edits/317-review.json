{"message":"test(source-evidence): preserve named property calls and unique evidence ids","target":327,"edits":[{"path":"packages/core/test/helpers/source-region.ts","find":"/** Direct identifier calls only, not imports, declarations, property calls, or prose. */","replace":"/** Named calls, including namespace and literal-property access, never declarations or prose. */"},{"path":"packages/core/test/helpers/source-region.ts","find":"    if (\n      ts.isCallExpression(node) &&\n      ts.isIdentifier(node.expression) &&\n      node.expression.text === name\n    )\n      found.push(node.getStart(tree));","replace":"    if (ts.isCallExpression(node)) {\n      const callee = node.expression;\n      const calledName = ts.isIdentifier(callee) ? callee.text\n        : ts.isPropertyAccessExpression(callee) ? callee.name.text\n        : ts.isElementAccessExpression(callee) && ts.isStringLiteralLike(callee.argumentExpression)\n          ? callee.argumentExpression.text : undefined;\n      if (calledName === name) found.push(node.getStart(tree));\n    }"},{"path":"packages/core/test/helpers/source-region.ts","find":" * The source of one declaration whose own syntax carries its terminator: `}` for an interface, `;`\n * for a type alias.\n *\n * `PK-18`'s two regions were already sound by the rule above, because both of their bounds belong to\n * the declaration they name. They are re-expressed through this function so the brace and the\n * semicolon are matched by one owner rather than assumed at two call sites, which is what makes a\n * later change to the rule reach them.","replace":" * Exactly one top-level interface or type alias, ending at its parsed terminator.\n * Nested member braces and semicolons cannot truncate the subject. Prose cannot impersonate its\n * opening, and missing, duplicate, wrong-kind, and unterminated declarations fail explicitly.\n * The signature remains shared with PK-18; issue #317 strengthens the one owner."},{"path":"packages/core/test/unit/adapters/trigger-factory-no-fallback.test.ts","find":"T-11 counts executable calls rather than comments or literal spellings","replace":"T-13 counts executable calls rather than comments or literal spellings"},{"path":"packages/core/test/unit/adapters/trigger-factory-no-fallback.test.ts","find":"      \"function createManualTriggerPort() {}\",\n      \"object.createManualTriggerPort();\",","replace":"      \"function createManualTriggerPort() {}\","},{"path":"packages/core/test/unit/adapters/trigger-factory-no-fallback.test.ts","find":"    expect(manualPortCalls(prose)).toBe(0);","replace":"    expect(manualPortCalls(prose)).toBe(0);\n    // The old scan also caught namespace calls. The parser must not weaken that half.\n    expect(manualPortCalls(\"ports.createManualTriggerPort();\")).toBe(1);\n    expect(manualPortCalls('ports[\"createManualTriggerPort\"]();')).toBe(1);\n    expect(manualPortCalls(\"ports.other(); unrelated();\")).toBe(0);"},{"path":"packages/core/test/unit/scripts/source-region-anchors.test.ts","find":"import { readFileSync, readdirSync } from \"node:fs\";","replace":"import { readFileSync, readdirSync, mkdtempSync, writeFileSync, rmSync } from \"node:fs\";\nimport { tmpdir } from \"node:os\";\nimport { join } from \"node:path\";"},{"path":"packages/core/test/unit/scripts/source-region-anchors.test.ts","find":"import { codeOnly, declaration, parseSource } from \"../../helpers/source-region\";","replace":"import { code, codeOnly, declaration, parseSource } from \"../../helpers/source-region\";"},{"path":"packages/core/test/unit/scripts/source-region-anchors.test.ts","find":"  it(\"preserves offsets and executable template substitutions while erasing literal text\", () => {","replace":"  it(\"reads statement syntax without comments while preserving quoted tokens\", () => {\n    const directory = mkdtempSync(join(tmpdir(), \"motion5-source-projection-\"));\n    const path = join(directory, \"fixture.ts\");\n    const source = [\n      '/** removed-doc */ const url = \"https://example.test/a\"; // removed-tail',\n      'const literal = \"/* literal */\"; const pattern = /word/;',\n      'const text = `literal ${actual()}`;',\n      'const result = left/* removed-inline */+right;',\n    ].join(\"\\n\");\n    try {\n      writeFileSync(path, source);\n      const projected = code(path);\n      expect(projected.length).toBe(source.length);\n      expect(projected).not.toMatch(/removed-doc|removed-tail|removed-inline/);\n      expect(projected).toContain('\"https://example.test/a\"');\n      expect(projected).toContain('\"/* literal */\"');\n      expect(projected).toContain('/word/');\n      expect(projected).toContain('`literal ${actual()}`');\n      expect(projected.indexOf(\"actual\")).toBe(source.indexOf(\"actual\"));\n    } finally {\n      rmSync(directory, { recursive: true, force: true });\n    }\n  });\n\n  it(\"preserves offsets and executable template substitutions while erasing literal text\", () => {"},{"path":"docs/TESTING-STRATEGY.md","find":"It counts direct identifier calls, including executable template substitutions, and excludes comments, strings, regex bodies, imports, declarations, and property calls. This is syntactic evidence, not semantic alias resolution or proof about an indirect call.","replace":"It counts calls whose callee names the symbol directly, through a namespace/property access, or through a literal property key, including executable template substitutions. It excludes comments, standalone strings, regex bodies, imports, and declarations. Namespace calls stay covered because the previous occurrence scan covered them too; narrowing to bare identifiers would weaken the claim. This is syntactic evidence, not semantic alias resolution or proof about a dynamically computed callee."},{"path":"docs/SESSION-STATUS.md","find":"Every decision ADR-028 through ADR-070 records is shipped.","replace":"Every decision ADR-028 through ADR-071 records is shipped."},{"path":"docs/SESSION-STATUS.md","find":"**Source-evidence correctness is being implemented on its own branch, not merged.**","replace":"**Source-evidence correctness is implemented on its own branch, not merged.**"}]}
