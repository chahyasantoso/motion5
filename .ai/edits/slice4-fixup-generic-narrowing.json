{
  "version": 1,
  "operation": "apply",
  "expected_head": "b2225fa0c7aa260b0f6d5a14d43e0778479e52c9",
  "expected_blobs": {
    "packages/core/src/contract/diagnostic-ids.ts": "a858e62e7f36b6b9798705d0b38563d5536cc75a",
    "packages/core/src/contract/diagnostics.ts": "10c7e3fa1764442ba7b8f9a9060af264850df6ed",
    "packages/core/src/runtime/publisher-outcome.ts": "0e5a9736944fd29bd3302658e28e09a25bae049c"
  },
  "message": "fix(contract): narrow the failure rule to the group that always names ids",
  "edits": [
    {
      "path": "packages/core/src/contract/diagnostic-ids.ts",
      "find": " * The assertion is the type guard a `filter` predicate cannot state: the comparison proves the\n * ownership at run time, and the compiler cannot carry that proof into the element type. It is\n * confined here, and the partition case in `rule-id.test.ts` reads the result rather than trusting\n * it.",
      "replace": " * The assertion is the type guard a `filter` predicate cannot state: the comparison proves the\n * ownership at run time, and the compiler cannot carry that proof into the element type. It goes\n * through `unknown` because `Ownership` is still a parameter here, so no member of the element union\n * is comparable to `BaseRuleIdOwning<Ownership>` until it is instantiated, which is the same reason\n * `mint` in `runtime/publisher-outcome.ts` widens before it narrows. It is confined to this one\n * function, and the partition case in `rule-id.test.ts` reads the result rather than trusting it."
    },
    {
      "path": "packages/core/src/contract/diagnostic-ids.ts",
      "find": "  ) as readonly BaseRuleIdOwning<Ownership>[];",
      "replace": "  ) as unknown as readonly BaseRuleIdOwning<Ownership>[];"
    },
    {
      "path": "packages/core/src/contract/diagnostics.ts",
      "find": " * the partition case in `rule-id.test.ts` refuses a rule the groups do not cover. A diagnostic built\n * from a literal rule id needs none of this and is checked directly against its variant, which is\n * why `graph/order.ts` and `runtime/report.ts` construct theirs without reaching for it.\n * See ADR-097.",
      "replace": " * the partition case in `rule-id.test.ts` refuses a rule the groups do not cover. A producer whose\n * rule id is already known to belong to one group needs none of this and is checked directly against\n * that variant, which is why `graph/order.ts` constructs its diagnostic from a literal and\n * `runtime/graph-publisher.ts` constructs one from an `IdentifiedRuleId` without reaching for this.\n * See ADR-097."
    },
    {
      "path": "packages/core/src/runtime/publisher-outcome.ts",
      "find": "import type { RuleId } from \"../contract/rule-id\";",
      "replace": "import type { IdentifiedRuleId } from \"../contract/diagnostic-ids\";"
    },
    {
      "path": "packages/core/src/runtime/publisher-outcome.ts",
      "find": " * The return type is `RuleId` rather than `string`, which is the other half of that. This function\n * is a producer of rule ids, so it is held to the closed set like every other producer, and the\n * three arms answering `composition-failure` are provably members rather than strings that happen\n * to match one. It was the last producer left open, and the publisher forwarding its answer into a\n * `Diagnostic` is where the build said so: a union closed at the field and open at the function\n * that computes the field is closed nowhere. See ADR-097.\n */\nexport function publishFailureRule(failure: PublishFailure): RuleId {",
      "replace": " * The return type is `IdentifiedRuleId` rather than `string`, which is the other half of that. This\n * function is a producer of rule ids, so it is held to the closed set like every other producer, and\n * the three arms answering `composition-failure` are provably members rather than strings that\n * happen to match one. It was the last producer left open, and the publisher forwarding its answer\n * into a `Diagnostic` is where the build said so: a union closed at the field and open at the\n * function that computes the field is closed nowhere.\n *\n * It says more than `RuleId` would, and the extra is true of all seven: every failure a publication\n * reports names the node it happened at, so every one of these rules always carries ids. That is\n * what lets `graph-publisher.ts` construct its diagnostic directly rather than asserting the\n * correlation, because the group the rule id belongs to is already decided here. See ADR-097.\n */\nexport function publishFailureRule(failure: PublishFailure): IdentifiedRuleId {"
    }
  ]
}
