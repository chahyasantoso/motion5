{
  "version": 1,
  "expected_head": "002e5e3c8682c2820355d4a8ba8041e788d104aa",
  "expected_blobs": {
    "packages/core/test/integration/membership-gating.test.ts": "29ee0f7db78eb9b641afff4a1ebe1c6575bb50ca",
    "docs/SESSION-STATUS.md": "0f87477a74c2f81a7f4746f3f5f05eda8d3905bc",
    "docs/ADR-082-a-flush-states-its-seeds-and-the-clock-is-its-own-verb.md": null
  },
  "message": "docs(runtime): ADR-082, the status rewrite, and the compile-time half of the split",
  "edits": [
    {
      "path": "packages/core/test/integration/membership-gating.test.ts",
      "find": "    // Read through a cast rather than through the declarations, so the red this commit is pushed\n    // for is a failing assertion in a real run rather than a compile error that never gets to run.\n    // `Function.length` counts the parameters before the first one carrying a default, so it reads\n    // 2 for a flush that also accepts the clock's frame number and 1 once it cannot.\n    const api = GraphRuntime.prototype as unknown as Record<string, (...args: never[]) => unknown>;\n    expect(api.flush.length).toBe(1);\n    expect(typeof api.flushAtTick).toBe(\"function\");\n    expect(api.flushAtTick.length).toBe(2);\n",
      "replace": "    // `Function.length` counts the parameters before the first one carrying a default, so this is\n    // the runtime-observable form of a compile-time contract: it read 2 for a flush that also\n    // accepted the clock's frame number, and reads 1 now that it cannot. Read through a cast whose\n    // new members are optional, so the red the evidence commit was pushed for is a failing\n    // assertion in a real run rather than a `tsc` diagnostic that stops the file from executing.\n    const api = GraphRuntime.prototype as unknown as {\n      flush: (...args: never[]) => unknown;\n      flushAtTick?: (...args: never[]) => unknown;\n      invalidate?: unknown;\n    };\n    expect(api.flush.length).toBe(1);\n    expect(typeof api.flushAtTick).toBe(\"function\");\n    expect(api.flushAtTick?.length).toBe(2);\n"
    },
    {
      "path": "packages/core/test/integration/membership-gating.test.ts",
      "find": "    // What a caller states is what the batch reports, and the seed-list entry still cannot move the\n    // clock's frame number. Both halves are unchanged by the split, which is the point of it.\n    expect(runtime.flush([\"hero/arm\"]).seeds).toEqual([\"hero/arm\"]);\n    expect(runtime.tick).toBe(0);\n\n    // And it refuses a disposed runtime through its own first statement.\n    runtime.dispose();\n    expect(() => runtime.flush([\"hero/arm\"])).toThrow(\"GraphRuntime is disposed.\");\n",
      "replace": "    // The compile-time half of the same claim: a tick handed to `flush` is an error rather than an\n    // argument it quietly ignores. The arrow is deliberately never called.\n    // @ts-expect-error `flush` states its seeds; the clock transition belongs to `flushAtTick`.\n    const withTick = () => runtime.flush([\"hero/arm\"], 1);\n    expect(typeof withTick).toBe(\"function\");\n\n    // What a caller states is what the batch reports, and the seed-list entry still cannot move the\n    // clock's frame number. Both halves are unchanged by the split, which is the point of it.\n    expect(runtime.flush([\"hero/arm\"]).seeds).toEqual([\"hero/arm\"]);\n    expect(runtime.tick).toBe(0);\n\n    // The clock's verb is the only member that moves that number, and it refuses a value that does\n    // not advance and one that is not a number at all, both before `#lastTick` moves.\n    expect(runtime.flushAtTick([\"hero/arm\"], 1).seeds).toEqual([\"hero/arm\"]);\n    expect(runtime.tick).toBe(1);\n    expect(() => runtime.flushAtTick([\"hero/arm\"], 0)).toThrow(\"Runtime ticks must be monotonic.\");\n    expect(() => runtime.flushAtTick([\"hero/arm\"], Number.NaN)).toThrow(\n      \"Runtime ticks must be finite.\",\n    );\n    expect(runtime.tick).toBe(1);\n\n    // And both refuse a disposed runtime, each through its own first statement.\n    runtime.dispose();\n    expect(() => runtime.flush([\"hero/arm\"])).toThrow(\"GraphRuntime is disposed.\");\n    expect(() => runtime.flushAtTick([\"hero/arm\"], 2)).toThrow(\"GraphRuntime is disposed.\");\n"
    },
    {
      "path": "docs/ADR-082-a-flush-states-its-seeds-and-the-clock-is-its-own-verb.md",
      "create": "# ADR-082: A flush states its seeds, and the clock transition is its own verb\n\n**Status:** Accepted, 2026-09-16\n\n**Context.** [Issue #374](https://github.com/chahyasantoso/motion5/issues/374) reads `GraphRuntime` at `746f1bb05ddd1e2343e5e34680f50abb3d0fe7ef`, which is main after [#372](https://github.com/chahyasantoso/motion5/pull/372) and [#373](https://github.com/chahyasantoso/motion5/pull/373), and finds one member carrying two operations. `flush(seeds: readonly string[], tick?: number)` publishes for a seed list, and additionally validates finiteness and monotonicity and moves `#lastTick` when a second argument is present. Which of the two a caller performed was stated by argument count, which is the weakest statement available, and the half that was reachable by omission was the half that consumes a frame number the clock will reuse on its next frame. `invalidate(seeds)` was `return this.flush(seeds);` after #373 deleted its duplicate liveness assertion, so the class also carried two names for the no-tick operation. Both halves of that are the same defect, counted once per axis.\n\n**Decision one. `flush(seeds)` loses the tick, and `flushAtTick(seeds, tick)` gains it as a required parameter.** This is the issue's proposal, accepted as written. A caller of `flush` cannot reach the clock's frame number, and a caller of `flushAtTick` cannot avoid stating the transition. The segregation stops being a convention a reader has to hold and becomes a signature: `#flushTick` is the only caller of `flushAtTick` in `src`, so a live edit remains structurally unable to consume a frame number the clock will reuse, which is the property `clock-tick-identity` reads from the other side and which #373 protected by keeping a second member instead.\n\n**Decision two. `invalidate` is deleted, and ADR-081 loses its premise rather than its reasoning.** ADR-081's decision two refused this deletion, and the argument was not sentiment: `invalidate` was `flush` with the clock's parameter removed, so deleting it and letting `ProjectRuntime.#invalidateSeeds` call `flush` directly would have **widened** what the project tier could reach. That argument was correct at that head and decision one spends it. After decision one, `flush(seeds)` is exactly as narrow as `invalidate(seeds)` was, so keeping both is a second name for one operation, which is the defect ADR-081 objects to one axis over. A reader arriving at ADR-081's decision two should read it as true of the tree it measured and superseded here; nothing about its decision one moves, and `seeds` is still required.\n\n**What the blast radius says, measured rather than feared.** ADR-081 also priced the deletion, and half of that pricing is retired by decision one too. It says a spy repointed from `invalidate` to `flush` weakens the claim it makes, \"because `flush` is also the tick path and the drain\". After decision one `flush` is not the tick path, `flushAtTick` is, so a spy on `flush` observes strictly less than the same spy did before: the seventeen `vi.spyOn(..., \"invalidate\")` spies that move to `\"flush\"` are each at least as strong as they were, and the local variable they bind stays named `invalidate` because the tier's operation was not renamed. The drain half of that worry survives and is why `#scheduleDrain` is left where it is, below.\n\n**Shape, and the ordering that does not move.** The issue's sketch validates and advances the tick before the shared machinery runs. Implemented literally that is a behaviour change and a defect: today the reentrancy answer comes before the tick is consumed, so a flush requested from inside subscriber notification is deferred without moving `#lastTick`, which `publisher-reentrancy` asserts. So the ordering is preserved, and it is preserved by giving each step an owner rather than by placing a conditional carefully. `#deferIfFlushing(seeds)` owns the reentrancy answer for both public verbs and hands back the deferred batch rather than a boolean, because two callers that had to ask and then act could act differently. `#advanceTick(tick)` owns tick validity and the one write to `#lastTick`, and refuses before the field moves, so a rejected tick leaves the runtime on the frame it was already on. `#flushSeeds(seeds)` owns publication mechanics and nothing else, and its one precondition -- not currently flushing -- is stated rather than re-asked, because a second copy of a decided question is a second owner of it. Four owners, no optional parameter at any layer, and the ordering is now written in the two verbs instead of implied by where an `if` sits.\n\n**Why `#flushSeeds` does not take an optional tick.** It would recreate exactly the shape this record is about, one layer down and out of reach of the type system, and the issue says so: the clock transition may not be accepted by the low-level flush operation. The two-line ordering statement each public verb carries is the price of that, and it is a price worth paying because those two lines are the difference between the operations rather than a duplicate of either.\n\n**Why `#scheduleDrain` still spells `flush([])`.** Pointing it at `#flushSeeds` directly is tempting, because it would make a spy on `flush` mean \"one caller-stated publication\" exactly. It is refused: the drain is scheduled work, nothing guarantees a scheduler cannot run it while a flush is in flight, and the deferral fallback that protects it lives in the public verb. `flush([])` there still means \"publish whatever `#pendingSeeds` carried\" rather than \"publish nothing\", which is why an empty caller list remains a project-tier question owned by `ProjectRuntime.#publishSeeds` under ADR-080 and not a short-circuit here.\n\n**Consequences.** No public surface and no behaviour. `GraphRuntime` is not exported from `packages/core/src/index.ts` and reaches no consumer in `apps/` or `packages/react`; no exported type, member, refusal, diagnostic or rule id moves; and every publication in `src` happens for the same seeds at the same sequence as before. `ProjectRuntime` changes one line, `#invalidateSeeds` reaching `this.#graph.flush(nodeIds)`, and keeps every name it had: `#publishSeeds` still owns what an empty seed list means, `#invalidateSeeds` is still the only place a seed list becomes a published recorded batch, `#invalidateOne` still owns the single-node report, and the public `ProjectRuntime.invalidate` verb keeps its name, signature and answer. `LV-5` moves three source-region literals and keeps its count at two, which is the point of the count: one occurrence, one owner. Thirty-one call sites move, all in tests.\n\n**Where the reasoning lives.** `graph-runtime.ts` is under the 30,000 bytes that would oblige a sister document, so both public verbs and all three privates carry their reasoning in source docblocks and the citations there stay legal. ADR-059, ADR-069, ADR-070, ADR-078, ADR-080 and ADR-081 quote the old spelling and are deliberately not edited: this project supersedes a record with a record rather than amending a decided one, exactly as ADR-080 retired ADR-079's premise without touching it.\n\n**Alternatives refused.** Keeping `invalidate` as an alias of `flush`, refused as decision two: after decision one it carries nothing `flush` does not, and seventeen spies pointing at an alias would document a name rather than an operation. Implementing the issue's sketch literally, refused above as an ordering change that moves `#lastTick` for a flush that published nothing. Giving `#flushSeeds` an optional tick, refused above. Making one of the two verbs private so the other is the sole public entry, refused for the reason ADR-081 already recorded: `scheduler-reentrancy` drives the tick seam directly and the project tier drives the no-tick one, so both are public entries with real callers. Refactoring this class's lifecycle into a discriminated-union state machine, which is the issue's closing paragraph and is a real improvement rather than a fashion: `#disposed`, `#flushing` and `#scheduledDrain` are three independent booleans encoding one lifecycle with illegal combinations nothing prevents, and the snapshot memo is a second such state spread over three fields. It is deferred to its own slice rather than declined, because it is a second invariant with a second blast radius across `pendingSeeds`, `tick`, `sequence`, `lastFlushError`, `#report` and `publisher-snapshot-memo`, and landing it on top of thirty-one moving call sites would make one review that cannot separate a rename from a rewrite. `PR-WORKFLOW.md` asks for one meaningful invariant per slice, and this record honours that rather than arguing with it.\n"
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Captured:** 2026-09-14, Asia/Jakarta.",
      "replace": "- **Captured:** 2026-09-16, Asia/Jakarta."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Inspected main:** `b49aec6908e753927303aad5fadd8782fec7600e`, after the publication-owner fix [#370](https://github.com/chahyasantoso/motion5/pull/370). Exact CI and operation evidence belong to their PRs.",
      "replace": "- **Inspected main:** `746f1bb05ddd1e2343e5e34680f50abb3d0fe7ef`, after the stacked pair [#372](https://github.com/chahyasantoso/motion5/pull/372) and [#373](https://github.com/chahyasantoso/motion5/pull/373). Exact CI and operation evidence belong to their PRs."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "Its remainders are the `CITATION_PENDING` ratchet and [#371](https://github.com/chahyasantoso/motion5/issues/371); [#369](https://github.com/chahyasantoso/motion5/issues/369) landed whole in [#370](https://github.com/chahyasantoso/motion5/pull/370) under ADR-079 and is closed.",
      "replace": "Its one remainder is the `CITATION_PENDING` ratchet; [#369](https://github.com/chahyasantoso/motion5/issues/369) and [#371](https://github.com/chahyasantoso/motion5/issues/371) are closed.\n- **[#371](https://github.com/chahyasantoso/motion5/issues/371) landed as a stacked pair and is closed:** [#372](https://github.com/chahyasantoso/motion5/pull/372) and [#373](https://github.com/chahyasantoso/motion5/pull/373) are merged: a seed list that names nothing is answered rather than published, and `GraphRuntime.flush` lost its member-set default, so no caller buys a full recompute by saying nothing. `RA-171`, ADR-080, ADR-081. No public surface in either."
    },
    {
      "path": "docs/SESSION-STATUS.md",
      "find": "- **Unmerged empty-invalidation cost:** [#372](https://github.com/chahyasantoso/motion5/pull/372) implements [#371](https://github.com/chahyasantoso/motion5/issues/371): public `invalidate` publishes through `#publishSeeds` rather than through `#invalidateSeeds`, so a list that names nothing is answered with the empty batch at the sequence the graph is already on instead of paying a publication, a notification for every batch subscriber and a sequence move for an invalidation that touches nothing. One line of source and one public docblock, no new member and no public surface: the verb still always answers a real `PatchBatch`, and it has no caller in either application or in `packages/react`, so this is cost hygiene on unused public API rather than a behaviour fix. `RA-171` is red on the evidence commit. ADR-080 records it and retires the premise ADR-079's third decision reasoned from without reversing that decision, because `#flush` answers nothing and folding a public verb into it stays refused. Review, exact-head CI, and merge remain gates; no landed change is claimed.",
      "replace": "- **Unmerged flush/clock separation:** [#375](https://github.com/chahyasantoso/motion5/pull/375) implements [#374](https://github.com/chahyasantoso/motion5/issues/374): `GraphRuntime.flush(seeds)` loses its optional `tick`, `flushAtTick(seeds, tick)` takes that transition as a required parameter, and three privates own the reentrancy answer, the tick write and the publication machinery both verbs share. `invalidate` retires with it, because decision one deletes the premise ADR-081 kept it on: it was `flush` without the clock's parameter, and `flush` no longer has one. Thirty-one call sites move, every one a test; no public surface or behaviour does. ADR-082 records it and defers the state-machine half of the issue. Review, exact-head CI, and merge remain gates; no landed change is claimed."
    }
  ]
}
