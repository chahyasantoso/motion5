{"message":"fix(runtime): preserve direct release reporting under the teardown owner","target":325,"edits":[{"path":"packages/core/src/runtime/rollback.ts","find":"function report(failures: readonly unknown[], summary: string): void {","replace":"export function report(failures: readonly unknown[], summary: string): void {"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"import { collect, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";","replace":"import { collect, report, rejectAfterRollback, runRollbackSteps, runSettleSteps } from \"./rollback\";"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"   * Release failures are recorded in diagnostics, never thrown; every release step is attempted.","replace":"   * Every release step is attempted. Direct release reports collected failures; a deferred release\n   * records diagnostics without replacing the outcome of the operation it follows."},{"path":"packages/core/src/runtime/project-runtime.ts","find":"  #teardown(): void {\n    this.#pendingTeardown = false;","replace":"  #teardown(): void {\n    const deferred = this.#pendingTeardown;\n    this.#pendingTeardown = false;"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"    // Reporting is inspection-only and happens after every release attempt. Neither a direct\n    // dispose nor a boundary drain may replace the outcome of the caller it is releasing for.","replace":"    // Completion precedes reporting. The existing pending flag distinguishes a direct release\n    // from a drain whose caller already has an outcome; neither call site owns this policy."},{"path":"packages/core/src/runtime/project-runtime.ts","find":"        if (seen.has(failure)) return \"[circular release failure]\";\n        seen.add(failure);\n        const message = describeError(failure);\n        if (!(failure instanceof AggregateError)) return message;\n        return [message, ...failure.errors.map((cause: unknown) => describe(cause, seen))].join(\n          \"; \",\n        );","replace":"        const message = String(describeError(failure));\n        if (!(failure instanceof AggregateError)) return message;\n        if (seen.has(failure)) return \"[circular release failure]\";\n        seen.add(failure);\n        try {\n          return [message, ...failure.errors.map((cause: unknown) => describe(cause, seen))].join(\"; \");\n        } finally {\n          seen.delete(failure);\n        }"},{"path":"packages/core/src/runtime/project-runtime.ts","find":"          severity: \"error\",\n        }),\n      );\n    }\n  }\n  #assertLive(): void {","replace":"          severity: \"error\",\n        }),\n      );\n    }\n    if (!deferred) report(failures, \"Project release failed.\");\n  }\n  #assertLive(): void {"},{"path":"packages/core/src/runtime/project-runtime.md","find":"Issue #312 extends that owner with one failure policy for direct disposal and deferred drains: attempt every independent release step, then record failures in the bounded diagnostics without throwing. No call-site flag or second error boundary decides whether an operation's outcome survives.","replace":"Issue #312 extends that owner with completion and precedence: attempt every independent release step, then record failures in the bounded diagnostics. A direct release reports its failure to the caller; a deferred drain does not throw over the outcome already in flight. This member captures the existing `#pendingTeardown` before clearing it, so neither caller passes a policy flag or gains a second error boundary."},{"path":"packages/core/src/runtime/project-runtime.md","find":"Even a direct `dispose()` returns void rather than introducing a second policy; callers inspect diagnostics after disposal.","replace":"A direct `dispose()` rethrows a single failure by identity, including undefined or a host aggregate, and combines multiple failures in occurrence order using the shared reporter. This preserves Engine's failed-load cleanup attachment (`D-3`), whose caller never receives a runtime to inspect. A deferred release is diagnostics-only so `RA-114`'s disposal refusal and `RA-117`'s accepted handle remain untouched. The distinction belongs here, not in `dispose()`, `#boundary`, or `#apply`."},{"path":"packages/core/test/unit/runtime/structural-commit-path.test.ts","find":"    expect(() => runtime.dispose()).not.toThrow();\n    expect(release).toEqual([`detach ${ADDED_ID}`, `detach ${NODE_ID}`, \"graph dispose\"]);","replace":"    const thrown = thrownBy(() => runtime.dispose());\n    expect(thrown).toBeInstanceOf(AggregateError);\n    expect((thrown as AggregateError).errors.map((error: Error) => error.message)).toEqual([\n      \"detach failed\", \"graph failed\", \"composition failed\",\n    ]);\n    expect(release).toEqual([`detach ${ADDED_ID}`, `detach ${NODE_ID}`, \"graph dispose\"]);"},{"path":"packages/core/test/unit/runtime/structural-commit-path.test.ts","find":"    expect(() => runtime.dispose()).not.toThrow();\n    expect(runtime.diagnostics.entries).toHaveLength(1);","replace":"    expect(thrownBy(() => runtime.dispose())).toBe(failure);\n    expect(runtime.diagnostics.entries).toHaveLength(1);"},{"path":"packages/core/test/unit/runtime/structural-commit-path.test.ts","find":"  it(\"preserves a settle failure when release also fails\", () => {","replace":"  it(\"retains repeated aggregate causes without mistaking them for cycles\", () => {\n    const cause = new Error(\"host cause\");\n    const failure = new AggregateError([cause, cause, undefined], \"host aggregate\");\n    const journal = recorder({ failAt: { disposeComposition: failure } });\n    const runtime = new ProjectRuntime(BASE_PROJECT, journal.options);\n    expect(thrownBy(() => runtime.dispose())).toBe(failure);\n    expect(runtime.diagnostics.entries[0]?.message).toBe(\"host aggregate; host cause; host cause; undefined\");\n  });\n\n  it(\"preserves a settle failure when release also fails\", () => {"}]}