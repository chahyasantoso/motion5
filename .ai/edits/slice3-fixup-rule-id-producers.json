{
  "version": 1,
  "operation": "apply",
  "expected_head": "3b67cd1a3edd6632b829ab6405cea7b2cb647433",
  "expected_blobs": {
    "packages/core/src/runtime/publisher-outcome.ts": "0a1bc7de0beeae697ccfd16af089d08b96f48350",
    "packages/core/src/contract/handle.ts": "483c37ad19d5e935dd36f8f5616f852281fe1a81",
    "packages/core/src/contract/motion-handle.ts": "8c8c829a662829b5cd40a77bc8b84ffeab6eaa47",
    "packages/core/src/contract/track-handle.ts": "6bb7a7deb459db4bc0c2ffc5edaf1966cd1f2bcc"
  },
  "message": "fix(runtime): close the last open rule id producer and the handle error family",
  "edits": [
    {
      "path": "packages/core/src/runtime/publisher-outcome.ts",
      "find": "import type { Diagnostic, Patch } from \"../contract/v5\";",
      "replace": "import type { RuleId } from \"../contract/rule-id\";\nimport type { Diagnostic, Patch } from \"../contract/v5\";"
    },
    {
      "path": "packages/core/src/runtime/publisher-outcome.ts",
      "find": " * that will not let it forget one. See ADR-016.\n */\nexport function publishFailureRule(failure: PublishFailure): string {",
      "replace": " * that will not let it forget one. See ADR-016.\n *\n * The return type is `RuleId` rather than `string`, which is the other half of that. This function\n * is a producer of rule ids, so it is held to the closed set like every other producer, and the\n * three arms answering `composition-failure` are provably members rather than strings that happen\n * to match one. It was the last producer left open, and the publisher forwarding its answer into a\n * `Diagnostic` is where the build said so: a union closed at the field and open at the function\n * that computes the field is closed nowhere. See ADR-097.\n */\nexport function publishFailureRule(failure: PublishFailure): RuleId {"
    },
    {
      "path": "packages/core/src/contract/handle.ts",
      "find": "/**\n * What every capability handle in this package carries, and how all of them fail.",
      "replace": "import type { RuleId } from \"./rule-id\";\n\n/**\n * What every capability handle in this package carries, and how all of them fail."
    },
    {
      "path": "packages/core/src/contract/handle.ts",
      "find": " * `ruleId` is what a caller branches on, in the kebab shape every diagnostic rule id in this project\n * uses. It is abstract here and carried on the instance as well as the constructor by each subclass,\n * so a caught value answers without the class being in scope. Matching a message string is the thing\n * this family exists to make unnecessary. See ADR-056.",
      "replace": " * `ruleId` is what a caller branches on, and it is `RuleId` rather than a kebab-shaped `string`: a\n * field spelled `ruleId` names a rule wherever it sits, so this one is held to the closed set\n * `contract/rule-id.ts` owns, exactly as `Diagnostic.ruleId` is. It is abstract here and carried on\n * the instance as well as the constructor by each subclass, so a caught value answers without the\n * class being in scope. Matching a message string is the thing this family exists to make\n * unnecessary. See ADR-056 and ADR-097."
    },
    {
      "path": "packages/core/src/contract/handle.ts",
      "find": "  abstract readonly ruleId: string;",
      "replace": "  abstract readonly ruleId: RuleId;"
    },
    {
      "path": "packages/core/src/contract/motion-handle.ts",
      "find": "readonly ruleId: string = StaleMotionHandleError.ruleId;",
      "replace": "readonly ruleId = StaleMotionHandleError.ruleId;"
    },
    {
      "path": "packages/core/src/contract/track-handle.ts",
      "find": "readonly ruleId: string = StaleTrackHandleError.ruleId;",
      "replace": "readonly ruleId = StaleTrackHandleError.ruleId;"
    }
  ]
}
