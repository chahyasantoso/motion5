{"version":1,"operation":"apply","expected_head":"d7d68f26b358b8343a69db55c283f6f3774b71a6","expected_blobs":{"docs/SESSION-STATUS.md":"9a1d9795c91fb1ff2749668a29ec097458747f06","packages/core/test/unit/scripts/session-status-shape.test.ts":"1688a37596b9d453c9f621165804a9e52d56d52a","AGENTS.md":"8ed6ef595082761344063bb143f114153a7ec448","docs/ADR-085-the-status-file-states-one-now.md":null},"message":"docs(status): state one Now and one Next in line, and gate the shape rather than the size","edits":[{"path":"docs/SESSION-STATUS.md","find":"The four sections below are the complete shape; `packages/core/test/unit/scripts/session-status-shape.test.ts` enforces it and the byte ceiling.","replace":"The four sections below are the complete shape, **Now** and **Next in line** carry exactly one bullet each, and no bullet states a phase; `packages/core/test/unit/scripts/session-status-shape.test.ts` enforces all three and the byte ceiling."},{"path":"docs/SESSION-STATUS.md","find":"- **Inspected main:** `e17289c9eca86a157368d623c9af5a287a3e78de`, after [#386](https://github.com/chahyasantoso/motion5/pull/386). Exact CI and operation evidence belong to their PRs.\n- **Phase:** live editing of a loaded project. Shared scroll-source lifecycle and walking bootstrap fixes [#353](https://github.com/chahyasantoso/motion5/pull/353) are merged. The IK rest-to-reach implementation in [#351](https://github.com/chahyasantoso/motion5/pull/351) integrates that shared fix, retains both producer test sets, and uses authored FK weights with staged domain intent. Exact-head CI and merge evidence belong to the PRs. Temporary browser workflows are retired; additional manual/target-drag checks are owner-waived, not passed, as recorded in [SCROLL-BROWSER-SMOKE.md](./SCROLL-BROWSER-SMOKE.md). [LIVE-EDIT-COST.md](./LIVE-EDIT-COST.md) owns caller cost; [GUARDRAILS.md](./GUARDRAILS.md) owns standing constraints.","replace":"- **Inspected main:** `d7d68f26b358b8343a69db55c283f6f3774b71a6`, after [#390](https://github.com/chahyasantoso/motion5/pull/390). Exact CI and operation evidence belong to their PRs."},{"path":"docs/SESSION-STATUS.md","find":"- **[#362](https://github.com/chahyasantoso/motion5/issues/362) landed whole and is closed:** [#363](https://github.com/chahyasantoso/motion5/pull/363) through [#368](https://github.com/chahyasantoso/motion5/pull/368) are merged. A loaded project enumerates itself, `subscribe`, `adopt` and `destroyAdopted` are deleted with no alias, and `ProjectHandle.values(recipe)` is a second tier beside `edit` that defers the publication rather than the write. ADR-076 through ADR-078; ADR-020 superseded. Its one remainder is the `CITATION_PENDING` ratchet. Slice narrative lives in those PRs.\n- **Runtime state machines landed:** [#386](https://github.com/chahyasantoso/motion5/pull/386) is merged, implementing [#376](https://github.com/chahyasantoso/motion5/issues/376) and [#384](https://github.com/chahyasantoso/motion5/issues/384): `GraphRuntime` holds its lifecycle and its snapshot memo as one discriminated value each, six fields become two, and a booked drain is a field of the live phases rather than a fourth phase. ADR-083, which also answers ADR-082's deferral. [#375](https://github.com/chahyasantoso/motion5/pull/375) is the flush and clock split underneath it: `flush(seeds)` cannot reach the clock's frame number and `flushAtTick(seeds, tick)` takes that transition as a required parameter. ADR-082. No public surface and no behaviour in either.\n- **[#371](https://github.com/chahyasantoso/motion5/issues/371) landed as a stacked pair and is closed:** [#372](https://github.com/chahyasantoso/motion5/pull/372) and [#373](https://github.com/chahyasantoso/motion5/pull/373) are merged: a seed list that names nothing is answered rather than published, and `GraphRuntime.flush` lost its member-set default, so no caller buys a full recompute by saying nothing. `RA-171`, ADR-080, ADR-081. No public surface in either.\n- **Read-budget headroom and its gate landed:** [#363](https://github.com/chahyasantoso/motion5/pull/363) and [#364](https://github.com/chahyasantoso/motion5/pull/364) are merged: `runtime/project-runtime.ts` keeps its private reasoning in a sister document, and a standalone comment in a mirrored source may no longer cite a decision, an evidence id or an issue number, with a shrinking `CITATION_PENDING` ratchet for the four pairs that still owe the move. No behaviour and no public surface in either.\n- **React DOM binding landed:** [#358](https://github.com/chahyasantoso/motion5/pull/358), [#360](https://github.com/chahyasantoso/motion5/pull/360) and [#361](https://github.com/chahyasantoso/motion5/pull/361) are merged: `useDomPatch` binds one node to one DOM or SVG target through `createDomPatchAdapter`, `useDerivedDomPatch` binds a derivation over several nodes to one target through an `applyValues` entry and the adapter's attribute channel, `ProjectHandle.renderMetadata` answers the serializer channel a renderer needs, every one-to-one, derived-geometry and projection consumer in both demos is migrated, and the adapter owns its composed transform state and the newest revision it accepted per target and node. Only the diagnostic panels still render on a tick. See ADR-073 and ADR-074.\n- **Runtime consistency landed:** [#343](https://github.com/chahyasantoso/motion5/pull/343) is merged: accepted trigger/stagger edits retain the installed definition before completion, and same-ID lifetime recreation is refused atomically. Its generic-arrow rewrite avoids the formatting symptom but does not repair the AI-edit formatter.","replace":"- **The runtime publication seam is the live area, and its three slices have landed:** [#375](https://github.com/chahyasantoso/motion5/pull/375), [#386](https://github.com/chahyasantoso/motion5/pull/386) and [#390](https://github.com/chahyasantoso/motion5/pull/390) are merged, so `flush(seeds)` cannot reach the clock's frame number, `flushAtTick(seeds, tick)` owns that transition as a required parameter, `GraphRuntime` holds its lifecycle and its snapshot memo as one branded discriminated value each, and a deferred publication carries the frame it arrived with while a drain booking is cancelled by the publication that consumes it. ADR-082, ADR-083 and ADR-084. Slice narrative, exact-head CI and measured byte sizes belong to those three PRs."},{"path":"docs/SESSION-STATUS.md","find":"- **Unmerged deferred-publication payload:** [#380](https://github.com/chahyasantoso/motion5/issues/380), [#382](https://github.com/chahyasantoso/motion5/issues/382), [#387](https://github.com/chahyasantoso/motion5/issues/387), [#388](https://github.com/chahyasantoso/motion5/issues/388) and [#389](https://github.com/chahyasantoso/motion5/issues/389) are implemented as one slice, the quality-pass findings over #375 and #386: a deferred publication carries the frame number it arrived with instead of dropping it, a drain booking is a claim about the scheduler and is cancelled when a publication consumes it, both state unions are branded so their illegal combinations are unassignable rather than merely unreached, and `#flushSeeds` asserts the precondition it used to state in prose. ADR-084. Two behaviour changes, both narrow and both pinned; no public surface, no diagnostic and no rule id retires. Review, exact-head CI, and merge remain gates; no landed change is claimed.\n- **Unmerged formatter parity:** [#345](https://github.com/chahyasantoso/motion5/pull/345) addresses [#344](https://github.com/chahyasantoso/motion5/issues/344) by preserving filepath context in AI-edit formatting and validation, with real CLI regressions for `.ts` and `.tsx`. Review, merge, a reviewed runner-pin update and live parity exercises remain gates; no activated correction is claimed.\n- **Unmerged reporting completion:** [#339](https://github.com/chahyasantoso/motion5/pull/339) consolidates failed-preparation diagnostics, retry/recovery retention, legacy evidence completion, and useful error excerpts. Its regression and exact-SHA CI evidence belong to the PR. Review, a single runner-pin rollout, and live failure/correction/replay verification remain gates; no deployed diagnostics fix is claimed.","replace":"- **The quality-pass remainder over #375 and #386 is open and unimplemented:** [#377](https://github.com/chahyasantoso/motion5/issues/377) and [#378](https://github.com/chahyasantoso/motion5/issues/378) are runtime defects, a `Scheduler` port that never says a job runs later and a `#flushSeeds` that loses drained seeds when snapshot derivation throws; [#383](https://github.com/chahyasantoso/motion5/issues/383) is a diagnostic naming a scheduler it may not have and an operation this tier retired; [#379](https://github.com/chahyasantoso/motion5/issues/379), [#381](https://github.com/chahyasantoso/motion5/issues/381) and [#385](https://github.com/chahyasantoso/motion5/issues/385) are test-quality findings over a lexical ownership proof, twenty retargeted publication spies and a stale explanatory comment. No implementation is claimed."},{"path":"packages/core/test/unit/scripts/session-status-shape.test.ts","find":" * Issue #284.\n */","replace":" * Issue #284, and its follow-up #384.\n *\n * The ceiling alone then walked into the failure the paragraph above predicts. The file reached\n * 7,960 of its 8,000 bytes, which is less than one markdown link, so the count correction #375 made\n * spent two of the forty that were left and the follow-up issue it wanted to name could not be\n * linked. The number is not the problem and it does not move. What moves is the shape: **Now** and\n * **Next in line** carry exactly one entry each, and no bullet states a phase.\n *\n * A section that may hold one entry cannot accumulate a log at all, which is the property the\n * closed list above already had for headings, applied one level down to the place all three\n * departed logs actually grew. It also refuses the staleness a ceiling cannot see: **Now** held six\n * entries and five described closed work, and two of the three under **Next in line** called\n * merged pull requests unmerged. The phase bullet was a second Now with a different name and the\n * longest half-life of anything in the file, so it is refused by name. See ADR-085.\n */"},{"path":"packages/core/test/unit/scripts/session-status-shape.test.ts","find":"const SECTIONS = [\n  \"## Now\",\n  \"## Next in line\",\n  \"## Open, and not scheduled\",\n  \"## Where the rest of it lives\",\n];","replace":"const SECTIONS = [\n  \"## Now\",\n  \"## Next in line\",\n  \"## Open, and not scheduled\",\n  \"## Where the rest of it lives\",\n];\n\n/**\n * The two sections that state current state, and therefore the two that may hold one entry each.\n *\n * The other two are not per-slice narrative and neither has ever grown, so the rule is not applied\n * to them: a constraint enforced where it is not needed is the kind that gets deleted for being\n * noise. See ADR-085.\n */\nconst SINGLE_ENTRY = [\"## Now\", \"## Next in line\"];\n\n/**\n * Answers the top-level bullets under `heading`, and nothing about what they say.\n *\n * A nested bullet is deliberately not counted. The rule is one entry, not one line, and an entry\n * that genuinely needs a sub-list is still one entry; forbidding that would be a budget shaping the\n * writing again, one level further down. The slice runs to the next level-two heading or to the end\n * of the file, which is why the section list is asserted before this is trusted.\n */\nfunction bulletsUnder(text: string, heading: string): string[] {\n  const start = text.indexOf(`${heading}\\n`);\n  const rest = text.slice(start + heading.length);\n  const end = rest.indexOf(\"\\n## \");\n  const body = end === -1 ? rest : rest.slice(0, end);\n  return body.split(\"\\n\").filter((line) => line.startsWith(\"- \"));\n}"},{"path":"packages/core/test/unit/scripts/session-status-shape.test.ts","find":"  it(\"stays under a ceiling current state has no honest reason to cross\", async () => {\n    const { size } = await stat(STATUS);\n    expect(size).toBeGreaterThan(0);\n    expect(size).toBeLessThanOrEqual(CEILING);\n  });","replace":"  it(\"stays under a ceiling current state has no honest reason to cross\", async () => {\n    const { size } = await stat(STATUS);\n    expect(size).toBeGreaterThan(0);\n    expect(size).toBeLessThanOrEqual(CEILING);\n  });\n\n  it(\"states one Now and one Next in line, so neither can become a log\", async () => {\n    const text = await status();\n    for (const heading of SINGLE_ENTRY) {\n      // Exactly one rather than at most one: a project with nothing queued still has to say so in a\n      // sentence a reader can check, and an empty section reads the same as an unupdated one.\n      expect(bulletsUnder(text, heading), `${heading} states exactly one entry`).toHaveLength(1);\n    }\n  });\n\n  it(\"states no phase, because a phase is a second Now with a longer half-life\", async () => {\n    const bullets = (await status()).split(\"\\n\").filter((line) => line.startsWith(\"- \"));\n    expect(bullets.filter((line) => /^- \\*\\*Phase[:*]/.test(line))).toEqual([]);\n  });"},{"path":"AGENTS.md","find":"1. [docs/SESSION-STATUS.md](docs/SESSION-STATUS.md) is the only document allowed to claim what has landed. Every plan, ADR, and architecture note describes intent unless it says otherwise. It states current state only, in four sections, and you **rewrite** it rather than appending to it: replace the entry your slice makes stale instead of adding one beside it. A gate holds it to that section list and to a byte ceiling, so a fifth section and a returning log both fail `CI`.","replace":"1. [docs/SESSION-STATUS.md](docs/SESSION-STATUS.md) is the only document allowed to claim what has landed. Every plan, ADR, and architecture note describes intent unless it says otherwise. It states current state only, in four sections, and you **rewrite** it rather than appending to it: replace the entry your slice makes stale instead of adding one beside it. **Now** and **Next in line** carry exactly one entry each, and there is no phase bullet. So your slice replaces the one entry that is there, and if that feels lossy, what you are about to lose is history and it already lives in the pull request that landed it. Verify the claim before you write it: two entries called merged pull requests unmerged for nine days, and no gate in this repository can catch that, because this file is the only place the claim exists. The gate holds the section list, the one-entry rule, the absent phase, and a byte ceiling, so a fifth section, a second **Now** entry and a returning log all fail `CI`. See ADR-085."},{"path":"docs/ADR-085-the-status-file-states-one-now.md","create":"# ADR-085: The status file states one Now, and the gate reads its shape rather than its size\n\n**Status:** Accepted, 2026-09-16\n\n**Context.** `docs/SESSION-STATUS.md` is the one document allowed to claim what has landed, and it is also the file every pull request is asked to update. Those two facts made it append-only once already: issue #284 cut it from 99,180 bytes to four sections and gave it a shape gate plus an 8,000-byte ceiling. [Issue #384](https://github.com/chahyasantoso/motion5/issues/384) reports what happened next. The file reached 7,960 bytes, forty of them left, which is less than one markdown link and about half a sentence. Its own gate's comment had already predicted that in words: a status file edited under a budget it can breach in one honest sentence gets edited to satisfy the budget instead of to state the status. [#375](https://github.com/chahyasantoso/motion5/pull/375) wanted to name the follow-up issue carrying the deferred half of #374 and could not afford the link; the count correction it did make spent two of the forty.\n\nThe issue's follow-up comment names the defect rather than the symptom, and the file confirms all three parts of it. **Now** held six entries and five of them described merged, closed work, one of them a phase of the project that had already ended. **Next in line** held three, and two of them called [#345](https://github.com/chahyasantoso/motion5/pull/345) and [#339](https://github.com/chahyasantoso/motion5/pull/339) unmerged nine days after both were merged. **Phase** was a seventh entry with a different label, still describing the IK and scroll-source work of two slices ago. So the file was not too large. It was a log wearing a shape gate, and a ceiling cannot say so, because a ceiling cannot tell a current entry from a stale one.\n\n**Decision one. Now and Next in line carry exactly one entry each, and the gate counts them.** A section that may hold one entry cannot accumulate a log at all. That is the property the closed section list already had for headings, applied one level down to the entries inside a section the list allows, which is where every one of the three logs #284 removed actually grew. It also makes the rewrite rule enforceable rather than merely stated: an editor who has to delete the entry in front of them cannot quietly add one beside it, and what they delete is history that already lives in the pull request that landed it.\n\nExactly one, rather than at most one, and that is deliberate. A project with nothing queued still has to say so in a sentence a reader can check, and an empty section reads the same as a section nobody updated.\n\nTop-level bullets are counted and nested ones are not. The rule is one entry, not one line: an entry that genuinely needs a sub-list is still one entry, and forbidding that would be the budget shaping the writing again, one level further down.\n\n**Decision two. There is no phase bullet.** It was a second Now with a different name and the longest half-life of anything in the file, which is exactly why it was the stalest thing in it. Where the project sits in its plan is [IMPLEMENTATION-PLAN.md](./IMPLEMENTATION-PLAN.md)'s question and it is answered there; what is happening now is the one **Now** entry's, and two owners for one question is the defect this project cares about most. The gate refuses the bullet by its own label rather than trusting that nobody reintroduces it.\n\n**Decision three. The ceiling does not move.** #384 refuses raising it and gives the reason, and lowering it to the rewritten file's size is the same mistake pointed downwards: a ratchet that decides what the status may say. `CEILING` stays at 8,000 and stops being the binding constraint, because the one-entry rule refuses growth on structure before bytes are ever the problem. The ceiling keeps the job it is good at, which is catching a single entry that grows into an essay.\n\n**Decision four. The rule is written where the implementor reads it.** `AGENTS.md` names the one-entry rule, the absent phase, and the thing no gate can check: verify a claim before you write it. Two entries called merged pull requests unmerged for nine days, and nothing automated in this repository could have caught it, because this file is the only place that claim exists.\n\n**Failing-first evidence.** The two cases added to `packages/core/test/unit/scripts/session-status-shape.test.ts` are red before the rewrite, for the honest reason that the file has six entries under **Now**, three under **Next in line**, and a phase bullet above both. The three existing cases are green on both sides: the section list, its order, and the two documents the file hands its departed logs to are all untouched.\n\n**Consequences.** No source, no public surface, no behaviour, no diagnostic and no rule id moves. `docs/SESSION-STATUS.md` loses the phase bullet, five landed **Now** entries and all three **Next in line** entries, two of which were false; it keeps four sections in order, and it states one live area and one queued one. The measured size drops well under the unchanged ceiling, which is the headroom #384 asked for and the reason it is a shape change rather than a trim: the room is a consequence of the rule, not the point of it.\n\n**Alternatives refused.** Trimming the landed narrative and reporting the new byte count, which is what #384's body asks for on its own: refused as a fix that lasts until the next slice, because it corrects the bytes and leaves the mechanism that spent them, and #284 already bought that fix once at ninety-one thousand bytes. Raising `CEILING`: refused by #384, correctly, and its own comment says why. Lowering `CEILING` to the rewritten size: refused above. An expiry policy, where an entry goes stale after some number of days: refused because it needs a clock the gate does not have, and it would let six entries coexist as long as all six were recent. Moving **Now** into `GUARDRAILS.md` or a new document: refused because it moves the file rather than the habit. Enforcing one entry in all four sections: refused, because **Open, and not scheduled** and **Where the rest of it lives** are not per-slice narrative and neither has ever grown.\n"}]}