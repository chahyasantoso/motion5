{
  "version": 1,
  "expected_head": "607d1279bf32e2d2c87ca48eae79783800d214d1",
  "expected_blobs": {
    "packages/core/src/index.ts": "991a82161cdea1720638f06005295339a2f734b7",
    "packages/core/src/engine.ts": "737f6583887e78ecab80fff874eebc571e9f6e7a",
    "packages/core/src/runtime/patch-registry.ts": "a2ad8462d0acb65df2a301f1b5fe0c1a88322f28"
  },
  "message": "feat(runtime): a patch a reader can still ask for is a live one",
  "edits": [
    {
      "path": "packages/core/src/index.ts",
      "find": "  PatchStatus,\n  Patch,\n  PatchBatch,\n  PatchListener,\n} from \"./contract/v5\";",
      "replace": "  PatchStatus,\n  Patch,\n  LivePatch,\n  PatchBatch,\n  PatchListener,\n} from \"./contract/v5\";"
    },
    {
      "path": "packages/core/src/engine.ts",
      "find": "import type {\n  MotionDefinition,\n  Patch,\n  PatchBatch,",
      "replace": "import type {\n  LivePatch,\n  MotionDefinition,\n  PatchBatch,"
    },
    {
      "path": "packages/core/src/engine.ts",
      "find": "  get(nodeId: string): Patch | undefined;",
      "replace": "  /**\n   * The patch this node last published, or nothing if it has published none.\n   *\n   * `LivePatch` rather than `Patch`: a terminal patch is delivered to `subscribeNode` once and is\n   * never readable back, because eviction drops the registry entry before it notifies. A reader\n   * here accounts for `ready`, `blocked` and `error` and cannot receive `destroyed`, so the\n   * declaration states that instead of asking every call site to narrow past a variant it cannot\n   * get. See ADR-098.\n   */\n  get(nodeId: string): LivePatch | undefined;"
    },
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "  LivePatch,\n  Patch,\n  PatchBatch,\n  PatchListener,\n  ReadyPatch,\n} from \"../contract/v5\";",
      "replace": "  LivePatch,\n  PatchBatch,\n  PatchListener,\n  ReadyPatch,\n} from \"../contract/v5\";"
    },
    {
      "path": "packages/core/src/runtime/patch-registry.ts",
      "find": "  get(nodeId: string): Patch | undefined {",
      "replace": "  /**\n   * The patch this node last published, or nothing if it has published none or is gone.\n   *\n   * `LivePatch` rather than `Patch`, which is the narrowing ADR-098 named as owed and deferred. The\n   * map behind it is already `Map<string, LivePatch>`, and `evict` deletes a node's entry before\n   * `#notifyTerminal` delivers its terminal patch, so a destroyed patch reaches a listener exactly\n   * once and is never readable back out of here. The type states that now instead of leaving every\n   * caller to narrow past a variant this member cannot answer.\n   *\n   * The asymmetry with `subscribeNode` is the point rather than an oversight: a listener sees every\n   * publication a node makes including its last, and this sees only the ones it can still be asked\n   * about. A reader that wants the terminal event subscribes; a reader that wants current state\n   * asks here. `D1` in `patch-registry.test.ts` is the case that pins both halves together.\n   */\n  get(nodeId: string): LivePatch | undefined {"
    }
  ]
}
