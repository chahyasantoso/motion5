# Session status

Current state only, and deliberately small. This file says where the work is, what the next implementor picks up, and which rules the pipeline enforces. Per-slice reasoning lives in the ADR that decided the slice, in the pull request that landed it, and in `packages/core/test/unit/scripts/evidence-case-ids.test.ts`, which is the one owner of what every evidence-case-id series covers. Revisions of this file before 2026-08-30 carry the full narrative and are reachable through git history; it is not reproduced here, because a status file that grows with every slice stops being read, which makes it worse than a short one that is.

- **Captured:** 2026-08-31, Asia/Jakarta.
- **Verified on:** `main` at `b5daeda`, with slices A1 and A2 of [issue #223](https://github.com/chahyasantoso/motion5/issues/223) landed and slice A3 open on `feat/ra-03-interpolated-resolves-its-track`.
- **Phase:** live editing of a loaded project. The runtime mutation model, trigger drivers, compiled Track ownership, edge identity and ordering, teardown ownership, single-track mutation atomicity, declared package entrypoints, clock tick error attribution, time loop semantics, plugin-named keyframe groups with per-plugin key ownership, plugin-owned input requirements, transactional Track replacement, the single composition input channel, the bare authored leaf, the analytic and iterative solvers with goal addressing and per-member weight, the uniform stale-handle refusal, the incremental builder's cache residency, the single structural commit path, the flush every structural commit seeds, and live value writes on both static and animated keys are all shipped. See `docs/DECISIONS.md` and ADR-028 through ADR-060.

This document reports implementation reality. A plan or an audit describes intent unless this file says it landed.

## Where the work is

- **Open.** The runtime authoring surface, [issue #223](https://github.com/chahyasantoso/motion5/issues/223). The plan is reverified against `e7a6d3f` in the issue thread and all ten of its premises still hold; four things changed, and one of them retires a rule id the plan proposed, because #231 landed the animated live value and there is no animated-key refusal left to reach. Slice A3 is open and it is the last of the A slices: `interpolated` resolves the compiled map per call, exactly as `compose` two lines above it always did, so a publisher node that outlives a recompile reads the Track every other surface reads. `RA-14` through `RA-17` are its evidence and `clearPublisherCache()` is deleted with it. B onward is unstarted: `Handle`, the `StaleHandleError` family, `MotionHandle`, and the six tier-1 primitives.
- **Just landed.** Slice A2, one flush per structural commit. `SchemaPlan` gained `touched` and `#commit` seeds it after the graph accepted the candidate and the settle steps ran, because one of those steps is the mount and a node the members do not contain yet cannot publish. `RA-8` through `RA-13` are its evidence. It corrected the plan rather than following it: a commit with nothing to seed calls no `invalidate` at all, because an empty seed set costs a whole batch.
- **Also landed.** Slice A1, one transaction owner for every structural change. Five hand-copied transactions in `ProjectRuntime` became plan builders over one `#commit(plan)`, so `replaceGraph` and `rejectAfterRollback` have one call site each and the ordering that ADR-031, ADR-035 and ADR-045 each made load-bearing has one owner instead of five copies. `RA-1` through `RA-7` are its equivalence evidence, landed before it and green on both sides. It changed no behavior, deliberately, which is what made A2 provable as a separate slice.
- **Landed before those.** An animated live value on both entry points, [issue #231](https://github.com/chahyasantoso/motion5/issues/231), with [ADR-060](./ADR-060-animated-live-values.md) and cases `PK-1` through `PK-19`. `InterpolationTimeline` gained one optional member, `patchKeys(overlay, rebase?)`, taking the whole overlay rather than one key. The GSAP timeline adapter retains the compiled base record and one handle list per key, obtained through the new `GsapTimelineLike.recent()`, and rebuilds only the keys whose compiled stops differ from what is on the timeline. `ProjectRuntime` validates the rewritten definition when an animated key is named, writes through one renamed `writeValues` hook, retains the animated half of the write, and escalates to `stageTrack` plus one re-seek when the backend declines.
- Three decisions inside that slice are worth carrying forward rather than rediscovering. The member is record-shaped because a per-key one cannot express "this key is no longer overridden": only the adapter still holds the record that answers it, and a companion `restoreKey` would be a second refusal set to keep in agreement with the first. `false` means escalate and nothing else, which is what makes the optional member Liskov-safe on failure as well as on success. And the mask is not the definition of an override: the retained definition is, which is why `overrideValues` could be lifted at all.
- **The refusal set narrowed and one union member is gone.** `LiveValueRefusal` is `"unknown" | "kind" | "prepared"`. `"animated"` is removed rather than left declared, because no code path can produce it once a decline escalates, and that is a break for a caller branching on `error.reason`. `LV-11` and `LV-12` retire with it, both halves of the first and all of the second, and `PK-18` replaces `LV-12` with the inverse gate.
- **Still open.** [Issue #176](https://github.com/chahyasantoso/motion5/issues/176) tracks transactional `replaceTrack` ordering after a failed recompile. Issue #222's stateful-`Interpolator` alternative is not a candidate for any slice: a record-shaped member with a base and an overlay is further from that adapter than a per-key one was, because the state it keeps is two compiled records inside one `create()` closure, keyed by nothing.

## What a live edit costs today

- `replace(next)` is for topology. It validates a whole `TrackDefinition`, stages a replacement Track, rebuilds the graph, and rolls back transactionally when refused.
- Every structural change runs through one `#commit(plan)`. A plan carries the maps the graph is asked to accept, the effects that must be in place before it is asked, and the steps that settle once it accepted. Effects are reverted in apply order rather than reverse, because the displaced compiled Track has to be republished before the Motion entry is restored, and an effect counts as applied only once its call returned, so a hook that refused before writing anything is not restored. A commit adopts the accepted maps wholesale from the same pair that built the snapshot, so the committed graph and the retained definitions cannot drift.
- A structural commit ends at one flush, seeded with the nodes it touched and run after the settle steps, because one of those steps is the mount. An add and a replacement seed the edited node; a motion add or destroy and a removal seed nothing, because the first two derive no node at all and a removal's node is gone with nothing left depending on it. An empty seed set is not a cheap flush -- it still opens a batch, notifies every batch subscriber and moves the sequence -- so a commit with nothing to publish does not call `invalidate` at all. `addObserve` on a manual clock with no tick publishes now, and a new node whose sources have not published yet lands on `blocked` with a pending diagnostic rather than on nothing.
- So a runtime add publishes at rest and `Engine.load()` does not: a loaded project publishes nothing until something ticks. That asymmetry is real and `T-1` owns it, restated as parity of the progression a driver produced rather than of the batches it arrived in. Whether load should seed a flush too is unasked and unanswered; it has no members yet when it would.
- The compiled map is the one owner of a `Track`, and every seam that reads one resolves it per call: ordinary composition, the publisher's `MemberState` for a solver member, the progress hook, and `Motion`. Nothing captures an instance, because an escalation replaces an entry without touching the graph, and the publisher caches one node per graph node: a captured Track was the disposed one from that commit on, and a solver was the one reader that had captured one.
- `replaceGraph` still clears the publisher node cache, and that clear is residency rather than staleness. The map is keyed by the graph node object and holds it strongly, so without it every node a rebuild replaced would be retained for the life of the runtime. It is not what makes a recompile visible, and deleting it on that reading would trade a correctness fix for a leak.
- `overrideValues(values)` is revertible and leaves the retained definition alone. `setValues(values)` is sticky and rewrites it, so `handle.track` and the live composition cannot disagree. Both stage no Track for a static write, rebuild no graph on either path, and return the `PatchBatch` of their one `invalidate`.
- Either member takes an animated key. A static key is masked over the interpolated state; an animated key has its tweens replaced on the still-live timeline against a base the interpolator retained, at the same progress. An interpolator with no `patchKeys` escalates to a recompile through `stageTrack` and one re-seek, and publishes the same values, so the two backends are observably identical.
- A write is replaced wholesale rather than accumulated. An empty record is the clear, back to the authored values or to whatever the last `setValues` wrote, and a real `replace()` drops it by construction.
- An animated write validates the rewritten definition through `validateTrackDefinition` before anything mutates, because an authored stop list is definition-shaped input. A static-only write skips that and costs exactly what it did before.
- Three refusals and no fourth. `LiveValueKeyError`, rule id `live-value-key`: `unknown` for a key the track does not author, `kind` for a leaf shape change, and `prepared` for a key a plugin already decided the value of.
- `seek(nodeId, progress)` is leaf-level scrubbing. On a driver-backed Motion the next driver emission overwrites a seeked value.
- Owner-based `adopt` and `destroyAdopted` remain available for compatibility. New consumer code uses `addTrack` and `TrackHandle`.

## Working constraints

- `format:check` runs before `npm test` inside `quality`, so a formatting slip means the suite never ran and the run proves nothing. Prettier is configured `printWidth: 100`, `singleQuote: false`, `trailingComma: "all"`, `proseWrap: "preserve"`.
- No Markdown tables in new prose, in test comments, or in an ADR. Anything that wants to be a table becomes a bullet list or prose. Prettier formats fenced `ts` blocks too, so pseudo-code goes in a `text` block. It normalizes asterisk-delimited emphasis in Markdown to underscores as well, so prose that wants emphasis is rewritten without it.
- Formatting never shares a commit with behavior.
- Six checks gate a merge: `quality`, `integration`, `boundaries`, `build`, `end-to-end`, and the advisory `performance`. Squash merge once they are green.
- A failed or timed-out `CI` or `Recovery audit` run archives to the `ci-logs` branch at `logs/<run-id>/`, with `README.md`, `run.json` and `failed-jobs.log`. Cite the Actions URL as primary and read the archive for durable failing-first evidence. `archive-ci-logs.yml` is deliberately not branch-filtered, so a `feat/**` head ref needs nothing dispatched by hand; manual `workflow_dispatch` with a `run_id` stays available for a run the `if:` gate skipped.
- A test file naming a member that does not exist yet fails `typecheck` and stops `quality` before `npm test`. Declare the not-yet-existing seam locally in the red file and cast, then delete that declaration in the commit that lands the source. `StaleSeam`, `ComposeSeam` and the `LV-` locals are the precedent. A case that asserts a member is **gone** needs none of that: `"name" in object` typechecks before and after the deletion, which is what `RA-16` uses.
- `CI` triggers on `pull_request`, so several commits pushed together produce one run on the head rather than one per commit. Evidence that has to be cited by run id therefore ships in its own push, and an equivalence slice whose evidence must be green on both sides of a refactor ships that evidence as its own pull request. A3 is the first slice cut that way from the start: the evidence commit was pushed, the pull request opened on it, and the source pushed after, which is what A1's retro asked for.

## Guardrails

- One owner per state transition, and one owner per normalization.
- One question, one mechanism. Where two mechanisms answer one question, the weaker one is deleted rather than documented as discouraged.
- One condition has one failure contract. Where half a surface refuses loudly and half returns silently, the silence is deleted and a non-throwing probe is added for the callers whose second call is expected rather than mistaken. A nullable return is the same silence with a value nobody checks.
- No compatibility flags, facades, placeholder tests, renderer imports, or core GSAP imports.
- Every behavioral slice starts with failing-first evidence, and failing-first means failing assertions. A run that failed `typecheck` or `format:check` ran no test at all, so it is a broken file rather than evidence.
- A green case is only evidence of what it would fail without. A test whose assertions survive the fix being reverted, or its twenty inputs collapsing to one, is rewritten rather than counted.
- A case that goes red because a slice moved the point at which something happens is read before it is rewritten. Where the assertion was reading the right fact in the wrong place, the read moves and gains the assertion that the old place is now empty, because published-early and published-twice look the same otherwise. Where the case owned the timing itself, the invariant is restated and the difference it no longer carries is asserted somewhere, in one place. Neither is a licence to loosen an assertion to reach green.
- Docs, public types, tests, and status move together. A cited evidence case id names exactly one test in the suite.
- A public error type is declared where the package entry may name it. A caller cannot `instanceof` a type it cannot name, and the declaration-surface gate refuses a `runtime/` or `graph/` module in the entry's declaration closure, so a shared no-behavior layer is where such a type lives while ownership stays put. The same rule reaches a parameter type: a member the entry exposes is unusable if its argument type cannot be imported.
- An optional port member is a segregation decision, not a convenience. It is optional when an implementation genuinely cannot honor it, and the caller's answer has to be total on success and on failure, or the two implementations are not substitutable.
- An optional hook and a total answer are two separate questions. A supplier may be absent, and the answer it gives when present may still be required to be total: a provider that could answer "this node has nothing" made the publisher report a missing seam instead of a missing node, so the provider is optional and its return is not.
- A boolean return that means two incompatible things is one defect. Where a member can answer both "your input was bad" and "I cannot do this cheaply", the first is moved to the owner that can refuse before anything mutates and the second is all the boolean keeps.
- An authored section is reserved by name rather than inferred from shape. A wrapper that exists only to hold one value is deleted, and the thing it held becomes the value.
- A field with no consumer is removed and then refused, never left declared or silently ignored. A union member is held to the same rule, and so is a method: `clearPublisherCache()` existed for a staleness that had one real cause, had zero callers for as long as it existed, and is deleted rather than kept as the escape hatch for the next one.
- A rule answers for the scope that owns the thing it refuses. A rule that reserves an authored key across the whole registry is the same defect wearing the opposite shape.
- A misconfiguration with no symptom is refused by name at load time. A rig that holds a broken pose silently is worse than one that fails to load.
- A cache is keyed on everything its answer depends on. Where widening a key would reproduce another owner's key, the cache is deleted instead. Its residency is the owner's own question too: a cache that only ever grows is answered inside the layer that holds it rather than by three callers that have to stay in step. And a line that maintains a cache is read for what it is actually for: the publisher cache's clear looked like a staleness fix and was residency, so the staleness was fixed at the closure and the clear stayed.
- A helper that already exists and already does the arithmetic is called, not copied.
- A gate reads code, never prose. A scan for a retired form is narrowed to what that form can be written in; product text is not reworded to satisfy an assertion.
- A refactor that only exists to unblock a later slice still ships alone, with its own equivalence evidence.
- A default that exists to keep every prior rig byte-identical is proved by the prior rigs, not by a new test.
- An audit's claim about callers is a measurement, not a premise. A plan's claim about the code is the same: a finding verified against an older `main` is re-verified before it is cited, because a stale finding is how a decision gets made for a reason that no longer holds. A plan's own conclusion is held to it too: plan v2 of issue #222 verified finding 4 correctly and drew the wrong conclusion from it, which is how a capability was priced as impossible instead of being priced. A plan's prescription is held to it as well: A2 was planned to end at one unconditional `invalidate`, and an empty seed set turned out to cost a whole batch, so the plan was corrected in the slice rather than followed into it.

## Evidence anchors

- The single structural commit path: `RA-1` through `RA-7` in `packages/core/test/unit/runtime/structural-commit-path.test.ts`, landed in the commit before the refactor and green on both sides of it. An equivalence slice has no red run by construction; the two green runs are the evidence, and the pull request body owns both ids.
- The flush a structural commit seeds: `RA-8` through `RA-13` in `packages/core/test/unit/runtime/structural-commit-flush.test.ts`. Red run [33346690174](https://github.com/chahyasantoso/motion5/actions/runs/33346690174), archived at `logs/33346690174/` on `ci-logs`. `typecheck` and `format:check` passed first, then 4 failed and 793 passed: `RA-8`, `RA-9`, `RA-11` and `RA-13` on assertions, with `RA-10` and `RA-12` green on purpose because they are the two guards the change must not break. Green run [33347372844](https://github.com/chahyasantoso/motion5/actions/runs/33347372844) on `e1896fa`, all six checks.
- The compiled Track a solver member reads: `RA-14` through `RA-17` in `packages/core/test/integration/solver-member-cache.test.ts`. Red run [33350313599](https://github.com/chahyasantoso/motion5/actions/runs/33350313599), archived at `logs/33350313599/` on `ci-logs`. `typecheck` and `format:check` passed first, then 2 failed and 799 passed: `RA-14` on the solver publishing `error` where it owes `ready`, because `interpolated()` refuses on the disposed Track the cached closure had captured, and `RA-16` on `clearPublisherCache` still being declared. `RA-15` and `RA-17` are green on purpose, because a replacement derives a new graph node either way and ordinary composition already resolved the map per call. Green run [33350711407](https://github.com/chahyasantoso/motion5/actions/runs/33350711407) on `0418f1c`, all six checks.
- Animated live values: the failing-first and green runs are the `CI` runs on the pull request that landed issue #231, archived at `logs/<run-id>/` on `ci-logs` for any run that failed. The pull request body is the owner of both ids.
- Live value overlay red run: [33310177625](https://github.com/chahyasantoso/motion5/actions/runs/33310177625), archived at `logs/33310177625/` on `ci-logs`. `typecheck` and `format:check` passed first, then 14 failed and 752 passed. Every `LV-` case failed except `LV-12`, which asserted the `Interpolator` port grew no member and was green on purpose, and `SH-1` failed with them because `MEMBER_ARGUMENTS` declared two members the handle did not have yet. Green run: [33311740234](https://github.com/chahyasantoso/motion5/actions/runs/33311740234) on `b0b8080`, all six checks.
- Every earlier slice's red and green run ids live in the pull request that landed it. They were duplicated here for two dozen slices, which is what made this file unreadable; the pull request is the owner.
