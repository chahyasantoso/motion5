# Session status

Current project state only. Replace stale entries rather than append history. The four sections below are the complete shape, **Now** and **Next in line** carry exactly one bullet each, and no bullet states a phase; `packages/core/test/unit/scripts/session-status-shape.test.ts` enforces all three and the byte ceiling.

- **Captured:** 2026-09-17, Asia/Jakarta.
- **Inspected main:** `cee36af4470df0e02613e8f4611d6439866d2b1e`, after [#435](https://github.com/chahyasantoso/motion5/pull/435). Exact CI and operation evidence belong to their PRs.

## Now

- **The read side of the three runtime state records is open in review, and the decision index it would have been recorded in is retired:** every batch of [#426](https://github.com/chahyasantoso/motion5/issues/426) is on `main` through [#435](https://github.com/chahyasantoso/motion5/pull/435), which closed [#424](https://github.com/chahyasantoso/motion5/issues/424) and [#426](https://github.com/chahyasantoso/motion5/issues/426) and carried ADR-087 through ADR-091. What is not on `main` is [#437](https://github.com/chahyasantoso/motion5/issues/437): the three unions in `graph-runtime-state.ts` refuse every illegal combination at the mint and were read by `if` chains whose last arm was a ternary, so a variant nobody named resolved to `idle`, `warm` or `nothing`, and adding `disposing` in [#408](https://github.com/chahyasantoso/motion5/issues/408) needed one guard hand-written at five sites with no gate naming any of them. [#438](https://github.com/chahyasantoso/motion5/pull/438) converts all ten reads to switches that name every variant and end at one `never` sink in `domain/exhaustive.ts`, states the six live phases as one table, deletes those five disjunctions, and pins the whole thirty-five-row transition table as evidence green on both sides; `graph-runtime.ts` needs no change, because it reads no discriminant. ADR-092, which also records the linter and the opaque-type halves of the issue as refused. Stacked on it, [#439](https://github.com/chahyasantoso/motion5/issues/439) extracts ADR-001 through ADR-027 out of `docs/DECISIONS.md` into one file each, deletes that file and the duplicate ADR-051 it had acquired, and moves the pre-flight search rule it carried into [the documentation map](./README.md#decisions), so a number names exactly one file for every record rather than for two thirds of them; the issue asked for ninety-one extractions and twenty-seven is the honest count, because ADR-028 onward were already individual files. ADR-093. Neither is merged, and no CI result is claimed here. Beside them, phase A of [#443](https://github.com/chahyasantoso/motion5/issues/443) is open in review: `refusal.ts` states the thirteen refusals of the project runtime as one branded union with one describer, one error switch and byte-identical messages, `results.ts` states validation, the live-write seam and handle liveness as three more, and `schema-refusals.ts` keeps its eight names as one call each so no call site moves. Step 3 states that lifecycle as `project-phase.ts`: one branded union over the open scope, the commit depth and whether teardown has begun, with `admit` the one total answer to what each phase admits, pinned against a transcription of the guards it replaces. Step 3b decodes the runtime's validations through `expectValid` and its handle resolutions through `resolveToken` and `expectLive`, so fifteen inline throws, eight retyped diagnostic templates and both stale-handle constructions have one owner each. Step 3c retires all five of `#open`, `#valueSeeds`, `#disposed`, `#inFlight` and `#pendingTeardown` into one `#phase`, so the two guard rungs, `#commit`'s three checks and the clears `#teardown` wrote by hand have one owner too. Step 4 does the same for `TrackEntry`'s `overlay` and `liveWrite`, whose four combinations included one nothing could reach: `value-state.ts` states the three that remain, the emptiness test three members spelled becomes one mint, and the second map write that insured a boolean across a throwing getter is gone with it. Steps 5 through 8 are ordered in that pull request body and none of them is on `main`. Slice narrative, exact-head CI and byte sizes belong to the PRs.

## Next in line

- **The mutation baseline [#419](https://github.com/chahyasantoso/motion5/issues/419) is owed and nothing else is queued:** the `mutate` list now names `graph-runtime-state.ts`, `diagnostic-report.ts` and `ports/scheduler.ts`, and no Stryker run is reported before or after that widening, so `break` at 65.42 is an honestly stale number rather than a measured one. The next slice measures it and re-pins the thresholds in the pull request that measures them; [TESTING-STRATEGY.md](./TESTING-STRATEGY.md) owns that rule and says never to move a threshold to keep a run green. Not started, and no run is claimed.

## Open, and not scheduled

- [Issue #328](https://github.com/chahyasantoso/motion5/issues/328) remains open for remaining activation/failure-recovery exercises, lifecycle limits, and maintenance usage/replacement validation before any separately confirmed retirement. Historical workflows and the manual formatting escape hatch are retained; dependency maintenance through the bounded API protocol remains disabled.
- [Issue #411](https://github.com/chahyasantoso/motion5/issues/411) stays open on a recorded refusal rather than a fix: a drain replays at the frame it deferred, so the assertion it recommends cannot distinguish the two implementations it is about. `deferred-frame-loss.test.ts` says what each of its cases actually pins, and #417's unreached-frame case is the witness it asked for.
- [Issue #349](https://github.com/chahyasantoso/motion5/issues/349) holds the 2D IK/FK contract study that precedes any separate 3D plugin.
- Phase 6 packaging follows the current phase. [IMPLEMENTATION-PLAN.md](./IMPLEMENTATION-PLAN.md) owns scope; no implementation is claimed here.

## Where the rest of it lives

- Slice narrative, exact SHAs, CI links, and red/green evidence: the owning PR and ADR, not this file.
- Standing rules: [GUARDRAILS.md](./GUARDRAILS.md). Caller cost: [LIVE-EDIT-COST.md](./LIVE-EDIT-COST.md).
- Contributor and status discipline: [PR-WORKFLOW.md](./PR-WORKFLOW.md). API navigation and activation contracts: [API-CAPABILITIES.md](./API-CAPABILITIES.md).
- Earlier long-form history remains in Git at this path; do not duplicate it into another status database.
