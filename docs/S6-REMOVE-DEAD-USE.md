# S6: remove dead plugin use

Rerun marker after correcting the remaining duplicate-registration error-string quote. `TrackDefinition.use` is removed and legacy `use` entries are rejected with `plugin-contribution-unsupported-entry`.